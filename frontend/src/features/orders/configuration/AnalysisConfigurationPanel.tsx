import { zodResolver } from '@hookform/resolvers/zod'
import { useMutation, useQueryClient } from '@tanstack/react-query'
import { Plus } from 'lucide-react'
import { useState } from 'react'
import { useForm } from 'react-hook-form'
import { z } from 'zod'

import { getOrderErrorMessage, saveAnalysisDefinition, type AnalysisDefinition, type OrderConfiguration } from '#/api/order-management'
import { Alert, AlertDescription, AlertTitle } from '#/components/ui/alert'
import { Badge } from '#/components/ui/badge'
import { Button } from '#/components/ui/button'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '#/components/ui/card'
import { Checkbox } from '#/components/ui/checkbox'
import { Dialog, DialogClose, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from '#/components/ui/dialog'
import { Input } from '#/components/ui/input'
import { Label } from '#/components/ui/label'

const schema = z.object({ qboCatalogItemId: z.string().uuid(), name: z.string().trim().min(1).max(255), description: z.string().max(2000), submissionInstructions: z.string().max(4000), requiredIntakeFieldsJson: z.string().refine(isJson), resultContractJson: z.string().refine(isJson), isActive: z.boolean(), isSynthetic: z.boolean() })
type Values = z.infer<typeof schema>
const empty: Values = { qboCatalogItemId: '', name: '', description: '', submissionInstructions: '', requiredIntakeFieldsJson: '[]', resultContractJson: '[]', isActive: false, isSynthetic: false }

export function AnalysisConfigurationPanel({ configuration }: { configuration: OrderConfiguration }) {
  const client = useQueryClient()
  const [editing, setEditing] = useState<AnalysisDefinition | null | undefined>(undefined)
  const form = useForm<Values>({ resolver: zodResolver(schema), defaultValues: empty })
  const mutation = useMutation({
    mutationFn: (values: Values) => saveAnalysisDefinition(editing?.id ?? null, { ...values, version: editing?.version ?? 0 }),
    onSuccess: async () => { await client.invalidateQueries({ queryKey: ['order-configuration'] }); setEditing(undefined); form.reset(empty) },
  })
  function open(item: AnalysisDefinition | null) { setEditing(item); form.reset(item ? { qboCatalogItemId: item.qboCatalogItemId, name: item.name, description: item.description, submissionInstructions: item.submissionInstructions, requiredIntakeFieldsJson: item.requiredIntakeFieldsJson, resultContractJson: item.resultContractJson, isActive: item.isActive, isSynthetic: item.isSynthetic } : empty) }
  return <><Card><CardHeader><div className="flex items-start justify-between gap-3"><div><CardTitle>Analysis definitions</CardTitle><CardDescription>Scientific intake and result contracts linked to synchronized QuickBooks service items.</CardDescription></div><Button type="button" onClick={() => open(null)}><Plus data-icon="inline-start" />Add analysis</Button></div></CardHeader><CardContent><div className="divide-y">{configuration.analyses.map((item) => <button type="button" key={item.id} onClick={() => open(item)} className="flex w-full cursor-pointer items-center justify-between gap-3 py-3 text-left focus-visible:ring-3 focus-visible:ring-ring/50 focus-visible:outline-none"><div><span className="font-medium">{item.name}</span><span className="mt-1 block text-xs text-muted-foreground">{item.description || 'No description'}</span></div><div className="flex gap-2">{item.isSynthetic ? <Badge variant="outline">Synthetic</Badge> : null}<Badge variant={item.isActive ? 'secondary' : 'outline'}>{item.isActive ? 'Active' : 'Inactive'}</Badge></div></button>)}</div>{!configuration.analyses.length ? <p className="py-8 text-center text-sm text-muted-foreground">No analyses configured.</p> : null}</CardContent></Card><Dialog open={editing !== undefined} onOpenChange={(open) => !open && setEditing(undefined)}><DialogContent><DialogHeader><DialogTitle>{editing ? 'Edit analysis' : 'Add analysis'}</DialogTitle><DialogDescription>Production activation requires Phaeno scientific approval. Synthetic definitions remain test-only.</DialogDescription></DialogHeader><form id="analysis-form" onSubmit={form.handleSubmit((values) => mutation.mutate(values))} className="max-h-[60vh] space-y-4 overflow-y-auto px-1"><Field label="QuickBooks item" id="analysisItem"><select id="analysisItem" {...form.register('qboCatalogItemId')} disabled={Boolean(editing)} className="h-9 w-full rounded-lg border border-input bg-background px-3 text-sm"><option value="">Select item</option>{configuration.catalogItems.filter((item) => item.isActive).map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></Field><Field label="Name" id="analysisName"><Input id="analysisName" {...form.register('name')} /></Field><Field label="Description" id="analysisDescription"><textarea id="analysisDescription" {...form.register('description')} className="min-h-20 w-full rounded-lg border bg-background px-3 py-2 text-sm" /></Field><Field label="Submission instructions" id="analysisInstructions"><textarea id="analysisInstructions" {...form.register('submissionInstructions')} className="min-h-24 w-full rounded-lg border bg-background px-3 py-2 text-sm" /></Field><Field label="Required intake fields (JSON)" id="analysisIntake"><textarea id="analysisIntake" {...form.register('requiredIntakeFieldsJson')} className="min-h-20 w-full rounded-lg border bg-background px-3 py-2 font-mono text-sm" /></Field><Field label="Result contract (JSON)" id="analysisResult"><textarea id="analysisResult" {...form.register('resultContractJson')} className="min-h-20 w-full rounded-lg border bg-background px-3 py-2 font-mono text-sm" /></Field><div className="flex items-center gap-2"><Checkbox id="analysisActive" checked={form.watch('isActive')} onCheckedChange={(value) => form.setValue('isActive', value === true, { shouldDirty: true })} /><Label htmlFor="analysisActive" className="cursor-pointer font-normal">Active for production orders</Label></div><div className="flex items-center gap-2"><Checkbox id="analysisSynthetic" checked={form.watch('isSynthetic')} onCheckedChange={(value) => form.setValue('isSynthetic', value === true, { shouldDirty: true })} /><Label htmlFor="analysisSynthetic" className="cursor-pointer font-normal">Synthetic test definition</Label></div></form>{mutation.error ? <Alert variant="destructive"><AlertTitle>Analysis was not saved</AlertTitle><AlertDescription>{getOrderErrorMessage(mutation.error, 'Review the definition and try again.')}</AlertDescription></Alert> : null}<DialogFooter><DialogClose asChild><Button type="button" variant="outline">Cancel</Button></DialogClose><Button type="submit" form="analysis-form" disabled={mutation.isPending}>{mutation.isPending ? 'Saving…' : 'Save analysis'}</Button></DialogFooter></DialogContent></Dialog></>
}

function Field({ label, id, children }: { label: string; id: string; children: React.ReactNode }) { return <div><Label htmlFor={id}>{label} <span className="text-[var(--ruby-red,#b4233c)]" aria-hidden="true">*</span></Label><div className="mt-2">{children}</div></div> }
function isJson(value: string) { try { JSON.parse(value); return true } catch { return false } }
