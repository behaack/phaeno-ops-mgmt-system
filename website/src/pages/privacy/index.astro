---
import { Content } from '@/content/privacy.md';
import { Content as ArabicContent } from '@/content/privacy.ar.md';
import { Content as FrenchContent } from '@/content/privacy.fr.md';
import { Content as SpanishContent } from '@/content/privacy.es.md';
import { Content as SimplifiedChineseContent } from '@/content/privacy.zh-Hans.md';
import { Content as JapaneseContent } from '@/content/privacy.ja.md';
import { Content as GermanContent } from '@/content/privacy.de-DE.md';
import { Content as ItalianContent } from '@/content/privacy.it.md';
import Layout from '@/layouts/Layout.astro';
import TopPageBanner from '@/components/TopPageBanner.astro';
import SEOMeta from '@/components/meta-data-helpers/SEOMeta.astro';
import type { SupportedLocale } from '@/i18n/locales';
import { getPageCatalog } from '@/i18n/pageCatalogs';
interface Props { locale?: SupportedLocale }
const { locale = 'en-US' } = Astro.props
const content = getPageCatalog(locale).privacy
const PrivacyContent = {
  'en-US': Content,
  ar: ArabicContent,
  fr: FrenchContent,
  es: SpanishContent,
  'zh-Hans': SimplifiedChineseContent,
  ja: JapaneseContent,
  'de-DE': GermanContent,
  it: ItalianContent,
}[locale]
---
<Layout locale={locale}>
  <SEOMeta 
    slot="head"
    title={content.metaTitle}
    description={content.description}
    documentType="Web Page"
    searchTitle={content.searchTitle}
    locale={locale}
  />    
  <main id="main">
    <TopPageBanner title={content.banner} image="image-1" locale={locale}/>
    <section>
      <PrivacyContent />
    </section>
  </main>
</Layout>
