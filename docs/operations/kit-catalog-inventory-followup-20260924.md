# Kit catalog and inventory follow-up — September 24–25, 2026

The owner asked that the remaining valid changes be committed and deployed and that the change tree be empty. Commit `d5533e1f0a8e236d32e14c894b0c84cbfb43b12e` contains the supplier-catalog SKU and unit controls, the transportation-kit inventory filters, related help and regression coverage, generated documentation, and the planning-only POMS–DPS MQTT direction. Two unused master-mix UI drafts were removed before commit. The later branch commit `3048233a` changes only `.gitignore`; the application release source remains `d5533e1f`.

The focused supplier and stock-kit frontend run passed 43 tests across three files. The full frontend run passed 1,218 tests and had two failures in unchanged CRM and shipping-configuration files; both files passed on isolated rerun (42 tests). Typecheck, lint, documentation consistency for all 56 guides, and the production Vite build passed. The Release backend suite passed 760 tests with 348 database-dependent skips. The new kit-SKU PostgreSQL case separately passed against the migrated local database with its transaction rolled back. The changed kit/catalog browser acceptance cases remain unrun.

[Deploy Portal Green run 36080422018](https://github.com/behaack/phaeno-ops-mgmt-system/actions/runs/36080422018) succeeded at `d5533e1f` with image `sha-d5533e1f0a8e-run-36080422018-1`. Its recorded `migrations_requested=false` matches the absence of a new EF migration in this follow-up. The isolated source checkout produced Ready Vercel preview [`dpl_Bx9RwFBtt3mcEGTP18tWC9q5A2c3`](https://vercel.com/cadexgenomics/phaeno-ops-mgmt-system/Bx9RwFBtt3mcEGTP18tWC9q5A2c3) and Ready production deployment [`dpl_py76EZ8EnBubLFFwmskqrBkVCuTZ`](https://vercel.com/cadexgenomics/phaeno-ops-mgmt-system/py76EZ8EnBubLFFwmskqrBkVCuTZ). Vercel inspection resolved `portal.phaenobiotech.com` to that production deployment. Its build reported documentation corpus hash `b74bb20693cf`.

Fresh public requests returned HTTP 200 for API health, HTTP 204 for database ping, and HTTP 200 for the Portal root. These checks establish deployment and basic availability; signed-in kit/catalog behavior and physical kit acceptance remain separate evidence.
