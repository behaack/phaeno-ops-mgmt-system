# Barcode identity and DataMatrix release — September 23, 2026

## Release scope

The owner requested deployment to the environment named `production`, which is used as a staging area. This release adds DataMatrix printing and scan-back for POMS-generated laboratory tubes, manufacturer-scoped tube barcodes selected through the supplier product at assembly, a primary barcode registry, and ambiguity reporting for unscoped scans. It removes redundant on-screen QR images after scans while retaining printable symbols on physical kits, packets and trays. The [Lab plan](../plans/LAB-OPERATIONS-PLAN.md#physical-container-barcode-identity--september-23-2026) and [shipping plan](../plans/SAMPLE-SHIPPING-AND-INTAKE-PLAN.md#september-23-2026--manufacturer-barcode-namespace) record the product behavior.

The release branch already contains the material-tracking deployment and the subsequent API/UI consistency corrections. The last recorded matching API and Portal deployment was `b056528aa59cbec9f2ebc83d407b08211a7c08da`; the Portal alias has since advanced to `65567b3e561da5a6ba752c1aad3c3444e17243ba`. The live API revision still needs a hosted manifest check. A fresh public API health check returned HTTP 200 and its database ping returned HTTP 204 before this release.

## Database change and activation boundary

The single new migration is `20260923223407_AddContainerBarcodeNamespaces`. It adds barcode namespaces to stock tubes, stock kits, return kits, registered sample tubes and laboratory containers; creates `lab_ops.lab_container_barcodes`; backfills supplier namespaces from selected supplier products or a uniquely matching historical supplier name; gives POMS-generated containers the `PHAENO` namespace; and preserves unresolved history under `LEGACY`. It replaces global barcode uniqueness with namespace-plus-value uniqueness. Existing global unique indexes guarantee that the backfill cannot introduce a namespace collision among existing rows in each table. Historical barcode symbology is recorded as `Unknown` because it was not persisted.

Target: the shared `phaeno_portal_green` database on the retained PostgreSQL 18.6 service. The last recorded production migration is `20260923172325_AddReturnKitProductExpiration`. Before activation, compare the live migration history with the release artifact and verify that no unexpected migration or schema drift occurred. The migration uses table updates and index creation; duration depends on row counts and lock waits. The guarded deployment procedure creates a database backup, proves an isolated restore, encrypts the recovery copy and verifies its key before applying migrations. Preserve the existing Local storage, scanner, runtime settings and disabled assembly worker.

Apply this migration only after explicit shared-environment migration authorization under the [operations policy](../operations-readiness.md#database-migrations). The owner’s deployment request does not by itself record a separate migration decision. The previous API can run against the expanded schema during the brief transition, but the new API needs the migration before activation. Once duplicate printed values exist across manufacturers, returning to an older API that resolves bare scans by value is unsafe. Prefer an assessed forward fix; do not run the destructive `Down` migration against newly created barcode history. If disaster recovery is required, use the verified encrypted pre-migration snapshot with its matching application image and runtime record.

After activation, verify the exact migration ID, primary-barcode row count against laboratory container count, namespace backfill by source, uniqueness indexes, preserved stock/registered-tube counts and sample known identifiers. Verify the API image and Portal alias use the same source revision, then check API health, database ping, a fresh browser sign-in load and recent runtime errors. Signed-in manufacturer-duplicate scans, POMS label print/scan-back, physical thermal label readability and scanner decoding remain acceptance checks in staging.

## Local verification and release state

Before release preparation, the complete backend run recorded 750 passed, 0 failed and 341 environment-dependent skips; the complete frontend run recorded 1,214 passed in 189 files. TypeScript, full ESLint, documentation consistency across 56 guides and focused Chromium print-preview checks passed. Three connected PostgreSQL journeys covered shared-barcode stock binding, split shipment receipt and label history, and biological transfer with manufacturer selection and scan-back. The new migration was applied to the verified local development database. These checks do not claim physical scanner/printer or hosted authenticated acceptance.

The new migration is not yet authorized or applied to the shared target. No new API or Portal source revision is claimed deployed by this record until the workflow and live checks are complete.
