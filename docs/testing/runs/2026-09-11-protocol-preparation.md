# Laboratory test-protocol preparation — September 11, 2026

## Scope and result

### Latest resource-preparation checkpoint


**Latest: specimen/source attempt implementation, September 11, 2026.** Implemented the approved non-blocked [tube-attempt plan](../../plans/SPECIMEN-TUBE-ATTEMPT-PLAN.md): order/Trial policy snapshots, V2 authorization with V1 replay compatibility, explicit legacy policy confirmation, atomic source selection and Planned-stage adoption, same-attempt progression/holds/QC repeats, explicit failure and reserve restart, confirmed exhaustion, lineage/downstream guards and specimen detail workspace. Transition controls use Actions; tube review precedes Execution. Customer-requested holds remain blocked.

Applied only local `20260911200711_AddSpecimenTubeAttempts`. EF reports no pending model changes. ERD updated (174 tables, 2646 fields, 404 foreign keys). Backend API and test project build with zero errors/warnings; test suites were not run. Frontend typecheck and scoped lint passed; existing execution E2E source was updated for the attempt-summary contract, meaningful labels and Actions menu, but not run. Help regenerated: 56 guides, corpus `0bfbb9f48b47`. Local API runs `CodexTubeAttemptsFinal`, PID 6640, health HTTP 200 on port 44399. No Git mutation or deployment.

Connected Edge read-only inspection verified readable TEST-001 through TEST-009 rows, counts, TEST-008's actual source tube TEST-HS5Y7DB7-016, the legacy instruction blocker and link to the original Planned execution. The confirmation dialog identifies the instruction as applying to all specimens in HS5Y7DB7, shows required reason/attestation and uses the available width without horizontal overflow. Cancellation returned focus to Actions; no policy confirmation was submitted. Execution shows both tube-acceptance and source-selection prerequisites; Actions → Start execution is disabled. A transient browser command timeout was resolved by re-reading state and retrying Cancel; no mutation was submitted.

Read-only database preservation: HS5Y7DB7 remains Received v4, pinned workflow `0b35c7ed-a610-490c-b12e-4e017a6762ff`, nine specimens, 18 tubes, one reviewed tube, zero attempts and one execution with zero started. Original execution `9c32cbfc-46d6-49cb-8967-4c2a172c4e19` remains Planned for TEST-008 with null attempt/start. The owner's earlier acceptance of a different specimen's tube is preserved. No specimen receipt, policy adoption, source choice, step, failure or reserve action was saved by Codex during implementation.

**Next paced walkthrough:** review the job-wide instruction as Supervisor, explicitly confirm it with reason, then review TEST-008's tube and select that exact source to adopt the existing Planned execution. Do not repeat receipt/accession or create another execution. LAB-09 persisted success/fallback/exhaustion, concurrent operators, replay/rollback and full responsive/dark acceptance remain Not run. These checks are distinct from local implementation and read-only verification.

---

Implemented the approved pre-execution navigation slice. Tubes & lineage now precedes Execution with existing lineage URLs preserved. Planned specimen execution detail exposes TubeAcceptanceRequired using the same accepted/available-tube predicate as Start; the UI displays the prerequisite and Review tubes link and disables Start while blocked. Connected Edge verified HS5Y7DB7's Planned execution shows the notice before any start attempt, Start is disabled and associated with its explanation, Enter on Review tubes opens the same job's lineage tab, and ArrowRight moves from that tab to Execution. Desktop pages have no horizontal overflow at 1912px. All 18 tubes remain Not reviewed; the job remains Received v3 with its single Planned execution. API build (CodexTubeNavigation, zero warnings/errors), frontend TypeScript, scoped ESLint, help and diff checks passed. Local API now process 40616 with unchanged Development/HTTPS44399/NativePSeqAccountsReceivable arguments. Help corpus 8a3a0f986946. No automated tests, saved reviews, execution transitions, migrations, or Git mutations in this slice. Accepted-ready, concurrent change, narrow viewport and dark-theme acceptance remain Not run. Wider attempt/fallback work remains proposed and customer-requested holds remain blocked.

Implemented owner's tube-level acceptance correction: routine tube accession defaults to Accepted after receipt checks; exception decisions use a server-validated predefined reason list, Other requires explanation, and accepting a held/rejected tube requires resolution notes. Specimen acceptance is derived from submitted tubes (any Accepted wins; otherwise a held tube yields OnHold, else Received awaiting acceptable input), with no independent specimen Disposition action. Lineage offers Review tube and latest intake details. Start requires an available Accepted tube; tube reviews after specimen processing history are blocked. Applied local additive migration `20260911192043_AddTubeIntakeReview` (five nullable container columns), ERD now 172 tables/2605 fields/395 foreign keys. API/test project builds, TypeScript, scoped lint, help/diff checks passed; new domain tests and adjusted existing tests compile but were not run. Local API process 39336 uses `backend/app/bin/CodexTubeIntake/net10.0/PSeq.Operations.Api.exe`, Development HTTPS 44399 and the prior NativePSeqAccountsReceivable argument. Help corpus `442cd3d6cecc`. Connected Edge verified no specimen Disposition buttons, 18 Not reviewed tubes, routine Accepted without reason, predefined hold reasons and required Other explanation; cancelled without saving. Read-only DB confirms 18 tubes/zero reviewed, job Received/version 3, existing execution Planned/unstarted. Tube decisions have not been fabricated or backfilled. Next walkthrough remains explicit review of the intended source tube before starting the saved execution. Attempt/fallback plan remains otherwise unimplemented.

Owner requested consistent header/item contrast across all six work-order tabs. Applied shaded divided headers, padded list bodies and separate bordered rows to Specimens, Execution, Lineage, Libraries, Exceptions and Review, with explicit empty states for containers/libraries/exceptions. Connected Edge verified each tab's 1px header divider, nine specimen cards, one Planned execution card, eighteen container cards, and empty Libraries/Exceptions/Review messages. Scoped lint and TypeScript passed. No records changed or execution started; planned execution `9c32cbfc-46d6-49cb-8967-4c2a172c4e19` remains the next walkthrough checkpoint.

On the next Proceed, verified Stage 1 and specimen selections and saved the assignment once. Execution tab shows exactly one **Planned** execution `9c32cbfc-46d6-49cb-8967-4c2a172c4e19`. Read-only DB confirms specimen `1a7a4ae2-2a06-4820-a25a-0157914f4f63`, protocol v1 `3cc6216f-c8ca-4e5c-b213-7c5702cd2bbf`, workflow stage `b20c80cb-865d-4a4f-8fef-fd877c3f370b`, no assigned operator and NULL started time. HS5Y7DB7 is now pinned to workflow v1 `0b35c7ed-a610-490c-b12e-4e017a6762ff`, remains Received, and advanced to version 3. No execution was started or specimen disposition changed. Left the Execution tab open. Next paced checkpoint is opening the planned execution and checking its readiness/start requirements.

On the next Proceed, selected **1. Extracted RNA readiness** (stage `b20c80cb-865d-4a4f-8fef-fd877c3f370b`) and the first existing specimen **ACC-1a7a4ae22a064820a25a0157914f4f63** (`1a7a4ae2-2a06-4820-a25a-0157914f4f63`) in HS5Y7DB7's assignment dialog. Left optional Assigned operator blank. DOM verified both selections and that only Stage 1 is offered. **Save remains unclicked**; no execution, workflow pin, disposition or specimen record changed. The selected specimen is still Received, not asserted scientifically accepted. Next paced checkpoint is saving the planned specimen-scoped assignment and checking the resulting execution and workflow pin.

After promotion, opened HS5Y7DB7 (Received, nine specimens, version 2) and Assign protocol. The dialog now offers **TEST ONLY — Two-stage library preparation v1** with only **1. Extracted RNA readiness** eligible; Stage 2 is withheld pending the earlier required stage. Source inspection of CreateExecution in LabOperationsController.Work.cs confirms a previously unpinned job resolves the service's Production workflow and pins it when the first assignment is saved, with stage membership and prior-required-stage checks in the same operation. Thus no separate repair or new assignment feature is needed for this unpinned job. Left the dialog open with no stage selected, Specimen None, no operator and Save unclicked. Next paced checkpoint is choosing Stage 1 and an existing specimen for the synthetic execution; no job or specimen state changed.

On the next Proceed, verified the named confirmation and clicked Promote to production once. Connected Edge returned **TEST ONLY — Two-stage library preparation v1**, **Production**, two stages. Read-only local DB confirmed workflow version `0b35c7ed-a610-490c-b12e-4e017a6762ff` Production at September 11, 2026, 11:37:52 Pacific, promoted by Bill (`9bc0a52c-7344-4482-b34e-f14ec6abfff2`), with its approval retained. Both exact protocol v1 records are now Active and retain William's independent approvals. Both existing PSeq jobs remain Received, version 2, with NULL workflow pins; HS5Y7DB7 was not assigned automatically. This verifies the local positive promotion path for a protocol author after independent approval. Other role/negative/concurrency cases remain Not run. Next checkpoint is examining the supported workflow-assignment path for existing HS5Y7DB7 without database repair or duplicate work.

On the next Proceed, opened the original Approved workflow's **Actions → Promote to production** confirmation in connected Edge. Dialog identifies **TEST ONLY — Two-stage library preparation · v1** and explains new-job use, included protocol activation, and retirement of any replaced production versions. Left **Promote to production unclicked** for the paced review checkpoint. No status or job assignment changed. This is the local development POMS instance; next checkpoint is confirming promotion, then verifying workflow/protocol status and unchanged existing job pins.

Owner approved the promotion policy correction: any authorized Protocol Administrator may promote once the workflow and every included protocol have independent recorded approvals, including when the promoter authored either. Removed promoter-versus-author guards; added independent approval evidence checks before protocol activation/replacement, including already Active stages. Self-approved audit-only versions cannot pass promotion. API and test projects built successfully with zero warnings/errors in CodexPromotionPolicy; regression cases added/updated but not run. Updated plan, LAB-08 and help (corpus `f39c2ba04c95`). Local API restarted as process 35828 from `backend/app/bin/CodexPromotionPolicy/net10.0/PSeq.Operations.Api.exe` on HTTPS 44399 with the previous Development/NativePSeqAccountsReceivable arguments. Connected Edge Refresh returned the original workflow still Approved v1 with two stages. No promotion, assignment, execution, migration or Git mutation occurred. Next paced checkpoint is the promotion confirmation using the now-eligible Bill account.

Promotion preflight (read-only): local DB confirms both intended protocol versions remain Approved, authored by Bill and approved by William; the workflow v1 remains Approved, authored by William and approved by Bill. Both existing PSeq Lab Service jobs are Received and have no workflow version pin, including HS5Y7DB7. Source review shows promotion activates included Approved protocols and makes the workflow Production, without updating existing job pins. It checks both protocol-author and workflow-author conflicts: with enforcement enabled neither current actor could promote this combined fixture. Correction to the earlier unconditional statement about author approval: checked-in configuration is DualControlAuditOnly=true and DualControlEnforced=false; runtime overrides have not been established, so an enforced live rejection must not be claimed. No promotion attempted, no settings or records changed. Next decision is resolving the combined promotion actor rule before attempting promotion; do not bypass it or silently repin existing jobs.

On the next Proceed after the account switch, confirmed connected Edge was signed in as **Bill Haack**, reopened the exact workflow Draft v1 and re-read all eight stage values without editing. Submitted Approve once for version `0b35c7ed-a610-490c-b12e-4e017a6762ff`. Refresh retained **Approved**, two stages, with Promote to production and Withdraw approval actions. This records the synthetic distinct-account workflow approval (William authored, Bill approved), not scientific validation. No promotion, protocol activation, work assignment or execution occurred. Next checkpoint is reviewing local production-promotion impact and actor eligibility before any promotion; existing work pins must remain unchanged.

On the next Proceed, saved the two-stage workflow once with Create workflow draft. The service-workflow list shows **TEST ONLY — Two-stage library preparation**, Draft v1, two stages; version ID `0b35c7ed-a610-490c-b12e-4e017a6762ff` under identity `fab61845-c3f4-4a5a-9b8d-f196943241b2`. Reopened Continue editing and compared all eight form values exactly equal before save and after reopening: stage names, exact approved protocol version IDs, Required flags and complete handoff criteria in the intended order. Left the saved editor open without further edits. No approval, promotion, assignment or execution occurred. Next checkpoint is workflow approval review, respecting the distinct-author approval rule; this draft was created in William's synthetic account.

On the next Proceed, added Stage 2 **Library preparation and QC**, exact Approved v1 `2c7f3cd4-7123-4917-8b1f-722b8b9f8bea`, Required. Its handoff prose requires the same specimen's readiness first, derived library/source-parent linkage, qualified lot and equipment traceability, optional observation or skip reason, and Supervisor-confirmed passing current simulated library QC with no unresolved Hold/Fail. DOM inspection confirmed both stages in the intended order, all eight values, and unchanged Stage 1. **Both stages remain unsaved in the new-version editor**; Create workflow draft has not been clicked. Next paced checkpoint is saving and reopening the two-stage Draft v1 to verify persistence. Handoff text is operator guidance, not evidence of automatic evaluation; no approval, promotion, assignment or execution occurred.

On the next Proceed, entered Stage 1 **Extracted RNA readiness**, selected exact approved protocol version `3cc6216f-c8ca-4e5c-b213-7c5702cd2bbf` (v1), and retained Required. Handoff prose requires the same specimen's readiness execution to complete before library preparation, identity confirmation, passing current simulated input QC, and conditional Supervisor review or a recorded skip reason, with no unresolved Hold/Fail. This is synthetic software acceptance prose, not scientific validation or proof of automatic criteria evaluation. Read-only DOM inspection confirmed all four values. **Stage 1 remains unsaved**; no second stage or workflow version has been created. Next checkpoint is adding Library preparation and QC v1 as the second required stage.

On the next Proceed, verified the prepared dialog and clicked Create workflow once. The list returned **TEST ONLY — Two-stage library preparation**, service `pseq-lab-service`, identity `fab61845-c3f4-4a5a-9b8d-f196943241b2`, with no version yet. Opened its Add version link and confirmed the workflow v1 editor offers both intended Approved v1 protocols. Left the initial Stage 1 blank with Requirement Required; **Create workflow draft has not been clicked**. Next paced checkpoint is entering Extracted RNA readiness as stage 1, then Library preparation and QC as stage 2, with handoff criteria. No version, promotion, assignment or execution was created.

Opened Create service workflow in connected Edge and filled **PSeq Lab Service** (`pseq-lab-service`), **TEST ONLY — Two-stage library preparation**, and the synthetic two-stage description naming Extracted RNA readiness v1 followed by Library preparation and QC v1. Read-only local database inspection confirmed HS5Y7DB7 uses the same service key, remains Received, and has no workflow version pin. DOM inspection confirmed all three dialog values. **Create workflow has not been clicked**; the dialog remains open and unsaved. Next paced checkpoint is saving this workflow identity, then adding the two exact approved protocol versions in order. No workflow promotion, assignment or execution occurred.

Confirmed William Agnew (`wsa+clerk_test@example.com`) in connected Edge, reviewed the complete three-step preview for **TEST ONLY — Library preparation and QC**, and approved version 1 once. UI reports Approved Sep 11, 2026, 11:00 AM; Refresh retained Approved v1 and Actions now offers only Create new version and Retire protocol. Both walkthrough protocols are now Approved v1 under the synthetic second-account workflow. No independent human/scientific validation is asserted. No workflow, assignment, execution, material or equipment record changed. Next paced checkpoint is creating the intended two-stage service workflow, with exact approved version pins; existing retirement/invalidation test workflows are unrelated fixtures.

Removed Show discarded drafts per owner direction. Connected Edge verified discarded-only Test 1-2-3 is absent with Show retired unchecked and checked, while retired protocols remain available through that single filter. Discarded revisions remain read-only history under their associated protocol. No protocol data, approval, workflow or execution changed. Existing visibility tests and frontend/E2E/LAB-07 plans updated; automated tests were not run. Help corpus regenerated as `4294b58a710c`. Original walkthrough remains Extracted RNA readiness Approved v1 and Library preparation and QC Draft v1.

Implemented manual protocol retirement and Show retired per owner request; see LAB-OPERATIONS-PLAN.md for complete rules/evidence. Applied local migration `20260911172654_AddProtocolRetirement`; API now process 4092, CodexProtocolRetirement, HTTPS 44399. On separate synthetic protocol `cbcb7e3f-d7bf-4f84-98b9-0ac58223e710`, verified reason-required error and named Draft workflow dependency, discarded only fixture workflow `049f9d27-1300-44dd-aed0-c539f67a4d34` via UI, then retired the fixture as William. Default hiding and Show retired inclusion/Refresh retained reason/date and approval history; DB verified actor/time/version 2. This is synthetic software acceptance only. Original **Extracted RNA readiness v1 remains Approved**, **Library preparation and QC v1 remains Draft**, and neither is retired. No operational service workflow exists yet; the new discarded workflow is only a retirement test fixture. Next walkthrough checkpoint remains review/approval of Library preparation and QC v1.

Approved **TEST ONLY — Extracted RNA readiness**, version 1, through the connected Edge UI after confirming the active user was William Agnew (`wsa+clerk_test@example.com`). Re-read all three steps in the exact-version preview, checked the attestation and submitted once. UI shows Approved Sep 11, 2026, 10:15 AM; Refresh retained Approved v1. Its Actions menu now offers only Create new version, with no draft editing or deletion. This is synthetic distinct-account workflow evidence, not independent human/scientific review. **TEST ONLY — Library preparation and QC** remains Draft v1 and is the next paced approval checkpoint. No execution or service workflow was created.

Confirmed the connected Edge session is William Agnew (`wsa+clerk_test@example.com`) after the owner signed in. Opened Review and approve for **TEST ONLY — Extracted RNA readiness**, version 1. Its preview retains the three saved steps: identity verification, repeatable simulated input QC, and conditional Supervisor review after resolved Hold/Fail history. Left the attestation unchecked and Approve version 1 disabled for the owner's paced review checkpoint. Neither draft has been approved.

The owner authorized a fake William Agnew reviewer account without an invitation for the synthetic approval walkthrough. Clerk Development rejected the requested `wsa@phaenobiotech.local` as an invalid email; used its documented test-address format `wsa+clerk_test@example.com` instead. Created development sign-in and local user `ec4b36b6-e143-4173-8c00-2319c5078cf3`, active non-admin Phaeno Inc. membership and General Department access, with only ProtocolAdministrator. A temporary setup helper used the existing domain constructors and audit interceptor against localhost/phaeno_ops, recording four audit events under `TEST-ONLY-reviewer-fixture-20260911-owner-authorized` with no fabricated human actor. Read back the linked active user, membership and single active role. No invitation was created or sent; credentials remain outside the repository. Neither protocol has been approved yet. Next paced checkpoint is sign-in as this test reviewer and review of the first exact draft version. This demonstrates distinct-account enforcement only, not independent human review or scientific validation.

Implemented the owner's equipment-retirement request and explicit unchecked-by-default Show retired filter. Applied `20260911164357_AddEquipmentRetirement` to local phaeno_ops and regenerated the ERD. Started the updated API locally on HTTPS 44399 because it was stopped; process 9720 uses `backend/app/bin/CodexRetirement/net10.0/PSeq.Operations.Api.exe` and Development configuration, with logs in the Windows temp directory. Created and retired only **TEST ONLY retirement verification**, asset **PH-EQP-20260911-2NH6C3WJ**, using reason “TEST ONLY. Retired after verifying the equipment retirement workflow; no physical equipment or laboratory work involved.” Verified blank-reason validation, default hiding, checkbox inclusion, retained reason/date after Refresh and database actor/timestamp/version 2. The preparation asset remains Active/version 1. Required roles and retired-use/concurrency enforcement were source-reviewed; no execution or role-denial test was submitted. Builds, TypeScript and lint passed; domain regressions added but not run. The owner then requested menu width correction; changed it to 192px and verified Retire equipment occupies one line. No protocol approval or other walkthrough record changed. Next remains independent protocol approval; do not retire the preparation asset or recreate the verification asset.

After equipment save, the owner requested clearer header/item separation and supplied Materials as the list template. Equipment now matches its shaded divided header, inset bordered/shadowed rows, compact name/asset-code line and metadata, and trailing status. Connected Edge confirmed preserved saved asset details, 1px borders and 16px row/body spacing. Scoped ESLint passed. No asset data or workflow state changed; no automated suite was run.

On the next Proceed, verified the filled equipment dialog and submitted Create equipment once. Saved asset **PH-EQP-20260911-SVDMBJUH**, ID `1f823028-1275-4373-bb24-630affbc87a9`, name TEST ONLY library-preparation asset, type TEST ONLY preparation equipment, Test location, **Active**, version 1. The refreshed UI retained its asset code, name, type, location and due date December 31, 2026. A narrowly scoped read-only query confirmed both persisted calibration dates: last September 11, 2026 and due December 31, 2026. These are synthetic fixture dates, not physical calibration evidence. No duplicate or unsaved dialog remains. Both protocol drafts remain unapproved; material lot is Passed with 100 mL. Resource records are prepared, while independent protocol approval, workflow setup, specimen eligibility and actual execution remain pending.

At the owner's request, Equipment type and Location now occupy separate full-width rows. Live Edge measurements confirmed each at 478px, aligned with Name, with different vertical positions and all five values preserved. Scoped ESLint passed. The asset remains unsaved. Explained that types are derived from existing assets and every protocol version (including draft/discarded), while locations combine existing equipment location strings and active storage-location records. Inline equipment type/location creation is a name saved on the asset, not standalone reference-catalog management.

The owner reported equipment modal spacing before saving. Removed duplicate vertical margins from the field grid: shared body padding remains 16px, child margins changed from 20px top/bottom to zero, body height changed from 294px to 254px. Connected Edge comparison verified all five unsaved fields unchanged. Scoped ESLint passed. The equipment dialog remains filled and unsaved; next is the same save/persistence checkpoint. No asset or calibration record was created during the spacing fix.

On the following Proceed, rechecked that Equipment lists no assets and opened New equipment. Prepared **TEST ONLY library-preparation asset**, existing type **TEST ONLY preparation equipment**, existing **Test location**, synthetic last-calibration date **2026-09-11** and due date **2026-12-31**. Both dates are visible in the form, but persistence remains unverified. A detached response during the name fill was inspected; the name was already entered and work resumed without duplication. **Create equipment has not been clicked.** The owner remains on the filled dialog for review. These dates represent simulated test data, not physical calibration. Next paced checkpoint is saving once and verifying the generated asset code, status and dates; preserve the now-Passed material lot and both unapproved protocol drafts.

On the next Proceed, verified the staged Pass QC / September 11, 2026 result for TEST-LIBPREP-20260911-001, submitted Record QC outcome once, and refreshed the Materials list. The saved lot shows **QC: Passed**, **QC Sep 11, 2026**, **expiration/retest Dec 31, 2026**, and **100 mL**. This is a synthetic software-fixture QC outcome, not physical reagent qualification. The original failed lot remains unchanged. No consumption, equipment creation, protocol approval or execution occurred. No unsaved dialog remains; next paced checkpoint is preparation of TEST ONLY equipment. The earlier pending/unsaved QC entries below are historical.

On the next Proceed, opened Record QC for TEST-LIBPREP-20260911-001 and selected **Pass QC** with the default QC date **2026-09-11**. This is a simulated fixture result, not physical reagent qualification. The form exposes date and Pass/Fail choices, with no passing-result notes field. **Record QC outcome has not been clicked.** The dialog remains open for review; saved lot status is still Pending, quantity 100 mL, expiration/retest December 31, 2026. Next paced checkpoint is submitting this synthetic result and verifying persistence before equipment setup.

After **Please fix**, added a submission guard reading the native date control before the form library validates/submits. This protects against a visible date differing from form state; the original event failure was not reproduced and is not claimed as proven. Invalid/past native dates now produce a field error. Added a focused stale-date regression case without running the suite; lint and TypeScript passed.

Performed the explicitly authorized local repair on `phaeno_ops` at localhost, one row only: lot `a7363dfd-255a-48d4-905c-fb80b3d35b57` / TEST-LIBPREP-20260911-001. Guarded the update by version 1, NULL date, Pending QC, 100 mL and the exact ID/lot number. Set expiration/retest to 2026-12-31, incremented version to 2 and stamped the update time. This was an automated maintenance correction, so updated_by_user_id was set to NULL rather than attributing it to a person. Original update stamp was 2026-09-11 09:15:54.031372-07 with actor 9bc0a52c-7344-4482-b34e-f14ec6abfff2. The transaction returned UPDATE 1 and committed. No quantity, QC, identity, receipt or other lot changed. Connected Edge's refreshed Materials list now shows Dec 31, 2026, 100 mL and QC Pending. Next paced checkpoint is reviewing the synthetic lot's Record QC form; no QC has yet been submitted.

Investigation after the owner's next Proceed confirmed by a narrowly scoped read-only query of local phaeno_ops that lot `a7363dfd-255a-48d4-905c-fb80b3d35b57` has NULL expiration_or_retest_date, quantity 100 mL, and Pending QC. The missing date is therefore not only a display issue. Current creation form, request DTO, constructor, EF mapping and dashboard projection all carry the date; Record QC records a separate performed date and does not set expiration. The precise cause of the lost entry has not been established; no request-payload evidence was captured during creation. Do not claim an application defect or successful correction without reproduction. No database write, duplicate lot, QC submission or code change was made during this investigation.

On the next Proceed, verified the filled lot dialog and clicked Create material lot once. The saved Materials list, including after Refresh, shows TEST ONLY library-preparation reagent / TEST-LIBPREP-20260911-001, generated key test-only-library-preparation-reagent, 100 mL, Test location, Test supplier, and **QC: Pending**. The original failed lot remains unchanged. **Discrepancy:** the entered December 31, 2026 expiration/retest date is absent from the saved display. Its persistence is not verified; investigate before qualifying or using this lot. No Record QC action was taken, and no duplicate lot was created. The current browser is the Materials list with no unsaved dialog. The earlier unsaved checkpoint below is historical.

The owner confirmed no second Protocol Administrator account is available and agreed to continue material/equipment preparation while approval stays pending. Live Materials inspection found only Test material, lot 9595959, 5000 ml, Test supplier, Test location, expiration/retest September 20, 2026, with Failed QC and reason Test 1-2-3. Equipment listed no assets. Preserved those records. Opened New lot and entered an **unsaved** Supplier lot: TEST-LIBPREP-20260911-001; new material TEST ONLY library-preparation reagent; existing Test supplier and Test location; arbitrary synthetic quantity 100 mL; synthetic expiration/retest December 31, 2026. The nested Use material action stages the material for creation with the lot; Create material lot has not been clicked. The owner remains on this filled dialog for the next paced save checkpoint. No QC, calibration, consumption or physical work is asserted. Both protocol drafts remain fully saved and unapproved.

The owner resumed the planned test-protocol preparation. Prepared the [TEST ONLY two-protocol library-preparation draft](../fixtures/test-library-preparation-protocol.md) from the current built-in example, protocol guide and definition contract. The owner then requested slow authoring so they can follow, and two separate protocols to demonstrate the service-workflow handoff.

**Current checkpoint: both protocols have all three steps saved and reopened as Draft v1. No unsaved edits remain; neither protocol is approved or connected to a service workflow.** The initial local connection failed, then the owner started the applications. Edge is the signed-in POMS staff session; Chrome is the Customer Portal. Navigate through **Lab ops → Protocols** (`/lab-operations?section=protocols`); `/lab-operations/protocols` alone is not the list route.

Fresh staff UI inspection established HS5Y7DB7 is Received, nine specimens, zero open exceptions, version 2; the specimen list shows Received intake dispositions and the Execution tab says **No protocols assigned**. The assignment dialog had no eligible workflow stage even after selecting an existing specimen; it was cancelled without saving. Protocols shows **No service workflow has been defined** and an older unrelated **Test 1-2-3** identity with Discarded v1. No existing active workflow or new scientific acceptance was established.

Created one local identity initially named **TEST ONLY — Extracted RNA readiness and library preparation**, ID `6e3ca1f8-d64e-40f0-bcd9-ac8b75e56668`, generated key `test-only-extracted-rna-readiness-and-library-preparation`. After the owner's next Proceed, saved the partial initial definition as **Draft v1**, version ID `3cc6216f-c8ca-4e5c-b213-7c5702cd2bbf`, and renamed the identity through **Edit name and description** to **TEST ONLY — Extracted RNA readiness**. The generated key is unchanged. Reopened **Edit protocol** and verified the first step: Required/Operator, explicit confirmation, no repeat, and three required captures: Source container barcode (Barcode), Specimen reference (Text), Identity checked on (Date). No other steps were entered. The current saved editor route is `/lab-operations/protocols/6e3ca1f8-d64e-40f0-bcd9-ac8b75e56668/versions/3cc6216f-c8ca-4e5c-b213-7c5702cd2bbf/edit`. An earlier browser interruption during the name fill was inspected and recovered without creating a duplicate identity.

After the next Proceed, added only **Protocol 1 step 2 — Record simulated input QC** in the same editor. It is Required/Operator, repeatable, requires operator confirmation and an explicit QC outcome, and has three required captures: Synthetic RNA concentration (Number, ng/µL), QC fixture condition (Choice: Clear, Review needed, Unsuitable), and Synthetic QC record reference (File Reference). The displayed criteria use 20/Clear for Pass, 20/Review needed for Hold and 2/Unsuitable for Fail; other combinations require Hold. These are arbitrary software fixtures assessed by the operator, not scientific limits or automatic threshold enforcement. The DOM confirmed the instructions, units, choices, flags and criteria; no Save draft was clicked. Step 1 remains unchanged. Number and Choice conditional authoring controls were populated, but persistence, validation variants and execution behavior for Step 2 have not been tested.

On the owner's next Continue, saved the two-step partial Draft and reopened it through **Actions → Edit protocol**. The protocol remains **Draft v1** with the same identity/version IDs. The reopened Step 2 retains its Required/Operator settings, repeat and confirmation flags, all three required capture types, ng/µL unit, Clear/Review needed/Unsuitable choices and complete QC criteria. No Step 3 or new execution was added. The earlier unsaved checkpoint above is historical.

On the next Proceed, added only **Protocol 1 step 3 — Review a prior input QC hold**. Its requirement is Conditional, condition is “Perform when step 2 history contains a Hold or Fail, even if a permitted repeat now passes; otherwise skip with a reason.” Role is Supervisor, operator confirmation is required, repeat is off, and no additional QC gate/resources are configured. Its single required capture is **Review rationale** (Text). Instructions require resolution of the current Step 2 blocker before this review and explain that review cannot override a blocker. DOM inspection confirmed these fields. This is operator-assessed prose, not an automatically evaluated condition; because the step requires Supervisor, an eligible Supervisor also records any skip assessment. **Step 3 has not been saved.**

The owner then requested the role-neutral label **Confirmation required**. Builder, approval preview, execution-history copy and Phaeno help were updated without changing confirmation flags or role rules. During the live update/documentation refresh, the editor returned to its saved two-step state; the 55-field before/after comparison detected the missing unsaved third step. After the owner's next Proceed, restored Step 3 through the visible controls, compared all 55 values/flags equal to the original, saved Draft v1, and reopened it. **All 55 values/flags persisted exactly**, with three **Confirmation required** controls. No duplicate step or version was created. The earlier unsaved checkpoints above are historical.

On the owner's next Proceed, created **TEST ONLY — Library preparation and QC**, identity `a0eee555-dd5b-4ca1-8743-8d35601c0e8a`, key `test-only-library-preparation-and-qc`. Entered only Step 1, **Record simulated library preparation and traceability**: Required/Operator, repeatable, confirmation required, no QC gate. Three required captures are Derived library container barcode (Barcode), Preparation mode (Choice: Simulated), and Preparation record reference (Text). Configured the two input material requirements, derived test library output and test preparation equipment from the fixture. Read-only DOM inspection verified all 20 fields/flags and the resource line separator. A transient detached response during the name fill was inspected before continuing; only one identity was created. **Save initial draft has not been clicked; no Protocol 2 version exists yet.** The editor remains open at `/lab-operations/protocols/a0eee555-dd5b-4ca1-8743-8d35601c0e8a/versions/new` for the owner's review. Next checkpoint is saving this first step and reopening it before adding Step 2.

After the next Proceed, saved Protocol 2's initial draft and reopened it through Actions → Edit protocol. Draft v1 version ID is `2c7f3cd4-7123-4917-8b1f-722b8b9f8bea`. Compared all 20 visible fields/flags before saving and after reopening: 19 were identical; the input materials field changed its newline separator to a comma while retaining exactly the same two requirements in order. All content and settings persisted. The earlier unsaved checkpoint is historical. The owner remains in the saved first-step editor; Step 2 has not been added.

On the following Proceed, entered Protocol 2 Step 2, **Record an optional test observation**: Optional/Operator, nonrepeatable, confirmation required when performed, one required Text capture named Test observation, no resources or QC gate. Instructions explain that an unnecessary observation is explicitly skipped with a reason. Step 1's 20 fields/flags remained unchanged. The owner sent another Proceed during this checkpoint; saved and reopened the two-step Draft v1 with the same version ID. All 33 fields/flags compared exactly equal before save and after reopening. No third step was added. Next is entering Protocol 2 Step 3, Review simulated library QC.

On the next Proceed, entered only Protocol 2 Step 3, **Review simulated library QC**: Required/Supervisor, repeatable, confirmation required and QC outcome required. Added three required captures: Synthetic library concentration (Number, ng/µL), Library QC fixture condition (Choice: Clear, Review needed, Unsuitable), and Synthetic library QC record reference (File Reference). Instructions and synthetic 12/Clear/traceability-reviewed Pass, 12/Review needed or traceability-review Hold, and 1/Unsuitable Fail criteria match the fixture; other combinations require Hold. These are assessed prose criteria, not automatic threshold evaluation. Read-only DOM inspection verified all 22 new fields/flags and exact preservation of the first two steps' 33 fields/flags. **The third step is not saved.** The owner remains in the same Draft v1 editor with 55 fields/flags for review; next is saving and reopening the complete draft.

After the next Proceed, compared the open complete Protocol 2 draft with the prior 55-field snapshot, saved it, and reopened through Actions → Edit protocol. All 55 fields/flags compared exactly equal after reopening. Protocol 2 remains Draft v1 with the same identity and version IDs. Both protocols now have three persisted steps; the earlier unsaved checkpoints are historical. The owner remains on Protocol 2's saved editor. Next is reviewing the complete drafts and the independent approval requirements before connecting approved versions in a service workflow.

On the next Proceed, opened each protocol's **Review and approve** dialog and reviewed the complete ordered definition. Both previews retained the expected roles, captures, conditions, resource requirements, QC criteria and role-neutral confirmation wording. Neither attestation was checked and no approval was submitted; both dialogs were cancelled, leaving the protocol list open. Current API and domain code require a Protocol Administrator whose user ID differs from AuthoredByUserId (`protocol_author_approval_conflict`); this was source-verified, not exercised by submitting a deliberately invalid approval. The dialog describes the formal lock and attestation but does not explain author/approver separation. The next dependency is an independent Protocol Administrator account/reviewer; no account or role changes were made.

Both three-step drafts are complete. Independent approval and a service workflow connecting them as Required stages for the same specimen remain pending. No operational execution, specimen disposition, role assignment, approval or workflow promotion has been saved.

Copy-only validation: scoped ESLint passed for the builder, approval dialog and execution page. Help generation initially hit sandbox EPERM twice on its generated version file; authorized elevated generation succeeded for 56 guides, corpus `024e0e4a8c11`. No automated suite was run. Builder wording and saved recovery were verified in the connected browser; approval/history copy was source-reviewed without approving a protocol or manufacturing execution evidence.

## UI defect found during walkthrough

The owner next requested separate tabs and default hiding of discarded protocols. Added Protocols (default) and Service workflows tabs and a Show discarded checkbox. Discarded-only identities are hidden until requested; empty identities and histories with any non-discarded version remain visible. Connected Edge verified the two draft cards in the default view, restoration/hiding of Test 1-2-3, and keyboard navigation between the panels. Returned to Protocols with Show discarded off. Scoped lint and TypeScript passed; visibility unit expectations were updated but no automated suite was run. Phaeno help and owning/test plans were updated. Approval remains pending an independent Protocol Administrator.

After approval-preview review, the owner requested Actions remain at the first row's trailing edge. Protocol card headers now use a two-column grid with a shrinkable, wrapping text column and an intrinsic-width Actions column. Scoped ESLint passed. Live Edge DOM geometry confirmed all three protocol cards' Actions top coordinates matched their title coordinates, with buttons inside the cards and no card overflow. No automated suite or separate narrow-viewport check was run for this adjustment. Both protocol drafts remain saved and unapproved; independent approver availability is still pending.

The owner's screenshot showed the six Work tabs overflowing a fixed-height bar and overlapping the panel. `LabWorkOrderPage.tsx` now overrides the shared horizontal height variant, uses 36px minimum-height triggers and two/three/six responsive columns, and names the tab group. Subsequent connected Edge inspection verified 122/82/42px bar heights at 375/950/1280px and an 8px gap before the visible panel; targets stayed inside the bar, phone width had no horizontal overflow, and ArrowRight changed Execution to Lineage with visible focus. Initial panel measurement selected an inactive hidden tabpanel; the corrected query measured the visible panel and agreed with the rendered screenshot. No laboratory action was submitted.

A second owner screenshot showed excessive capture spacing and a detached trash button. `ProtocolVersionBuilderPage.tsx` now groups label/type in a responsive row and Required/remove in a compact footer, with conditional unit/choice fields between. Connected Edge rendering was visually inspected at native and phone/wide sizes. Keyboard traversal reached Type, Required and the correctly named remove button. All 19 unsaved fields/flags compared equal before hot reload and after the checks. No capture was removed, no draft save or next protocol step occurred, temporary viewport sizing was reset, and the separate Work-inspection tab was closed. The owner remains on the original first-step draft.

Scoped ESLint for both modified Lab pages, full frontend TypeScript and whitespace checks passed. The package-manager shim initially failed to locate ESLint; the installed Node entry point succeeded. React review found no new state, data fetching, dependencies or hook-order changes; existing labels, stable capture keys and required-field validation remain. The existing help remains accurate. No application test suite was run. Dark theme, Number/Choice interaction variants, scientific execution and full acceptance remain unrun. No commit, push or deployment was performed.

## Preserved starting point

The [September 10 stage record](2026-09-10-customer-laboratory-stages.md) verified HS5Y7DB7 as Received with nine samples, corroborated by today's staff UI. The [intake correction record](2026-09-10-intake-progress-correction.md) records nine accessioned samples and 18 preserved tubes; no new full tube/database comparison was performed today. Scientific intake acceptance and workflow eligibility remain separate. Do not repeat receipt, accession, the correction, kit ordering or dispatch.

## Resume sequence

1. Resume the protocol list in Edge, keeping the owner's slow, step-by-step pace. Both complete Draft v1 definitions and approval previews have been reviewed; no unsaved edits remain.
2. Establish an independent Protocol Administrator account/reviewer before approval. The author cannot approve the same version. Do not recreate either identity, alter roles or bypass actor separation.
3. After independent approval, review workflow setup. All six steps are saved in POMS; no protocol is approved and no service workflow exists in the last inspected dashboard.
4. Resolve independent protocol approval and local workflow eligibility through supported controls. Do not rewrite a pinned workflow to attach this fixture. Record any need for a separate test-work fixture before preparing it.
5. Select one eligible synthetic specimen and existing source container, assign/start only the eligible test execution, and save step 1 identity evidence. Reopen and compare the saved references. Stop at this first checkpoint before simulated preparation.

## Execution ledger

| Item | Current record |
| --- | --- |
| Environment/revision and API health | Signed-in local Edge UI loaded; exact running revisions and direct health not checked |
| Current Job/scientific intake/hold state | Received, nine specimens, zero open exceptions, v2; listed intake dispositions Received |
| Protocol identity/version and Draft save | Both protocols: complete three-step Draft v1 saved and reopened, IDs above; Protocol 2's 55 fields/flags persisted exactly |
| Author / independent Protocol Administrator | Not established |
| Pinned workflow/version/stage compatibility | No service workflow defined in current dashboard; assignment offered no stages; no pin was altered |
| Selected specimen/source container | Not selected |
| Execution ID/version / assigned operator | Not created |
| Step 1 save/reopen | Definition saved and verified on reopening; this is authoring evidence, not execution; execution remains blocked by controlled setup |
| Step 2 authoring | Saved and reopened in existing Draft v1; types, unit, choices, instructions, flags and QC criteria retained |
| Step 3 authoring | Restored after live refresh, saved and reopened; all 55 fields across three steps compare equal; confirmation label is role-neutral |
| Later protocol steps and execution variants | Not run |
| Physical bench, printer/scanner and provider evidence | Not run; outside this document-preparation checkpoint |

Document verification checks links, step/capture compatibility with the current editor/domain contract, and whitespace. No application suite or laboratory execution was run. This fixture does not mark LAB-01, LAB-03 or LAB-04 Pass.

### Revised retirement verification checkpoint — September 11, 2026

Implemented the revised policy and added manual LAB-07 plus backend/frontend/E2E coverage. Applied local migration `20260911174615_AddWorkflowInvalidation` (nullable invalidation reason/time); regenerated ERD (172 tables, 2600 fields, 395 foreign keys) and help corpus `ebb2d1302697`. Added LabWorkflowInvalidationTests for immutable approved history and independent recovery approval; test project builds, but automated tests were not run. Frontend TypeScript and scoped ESLint passed.

Live connected Edge verified a separate synthetic Production workflow with two stages: target protocol `dcf5ae27-27f1-4010-a862-2528714f0059`, workflow `61b14f55-9498-4bd1-8642-814a096907e1`, original v1 `fa8d11e4-07be-4585-87fa-a3b9079966e1`. The impact warning named the workflow and offered Proceed anyway. Confirming with a reason retired only the target protocol, changed original v1 to Invalidated with both historical stages preserved, and created Invalid v2 `a854932c-5732-4ce9-b8a3-1497f7247af2` with only the retained stage. Review workflow displayed that single Required stage, selected the retained approved protocol, and excluded retired protocols from options. Cancelled review without edits; recovery was not approved or promoted. No queued or in-process jobs were created for this fixture.

LAB-07 step 6 has partial live evidence for a Production workflow with no assigned jobs; step 9 has removal/review UI evidence only, not successful revalidation. Remaining active-sample/queued-work, multi-workflow, cancellation, stale-token, concurrency, cross-role and revalidation branches remain Not run. Test fixtures and seed approvals are synthetic software evidence, not independent scientific approval. Original Extracted RNA readiness remains Approved v1 and Library preparation and QC remains Draft v1. Updated local API uses CodexWorkflowInvalidationFinal on port 44399. No Git mutation, deployment, original work repinning or operational execution.


### Accession before storage and wrapped required labels — local implementation

- Implemented exception-first accession, explicit identified-tube batch acceptance and per-tube storage. Rejected expected tubes retain identity/reason/evidence with no required storage and no available material; non-stored rejections assert no remaining quantity. Existing stored records are not relocated.
- Tubes / Received tubes now opens view-first tube details with lineage. Routine inventory review is removed; Supervisor correction requires an explanation, real retained storage when restoring a non-stored record, and no started-source history. Customer-requested holds remain blocked.
- Applied `20260911205728_AllowRejectedTubeWithoutStorage` to verified localhost `phaeno_ops`. Verified nullable `lab_ops.lab_containers.location` and latest migration history. Existing rejected/Available normalization affected zero records. The ERD contains 174 tables, 2646 fields and 404 foreign keys.
- Backend API and test project compiled in `CodexIntakeBatch` with zero warnings/errors. Frontend typecheck and scoped ESLint passed. Documentation corpus generation/check and diff whitespace check passed. Automated tests were not run; new domain and PostgreSQL batch cases plus updated frontend assertions are authored/compiled. Persisted batch/rejection/race acceptance remains Not run under LAB-13.
- Restarted only the verified local API (PID 34156); `/api/health` returned HTTP 200. No Git mutation, deployment or shared-database operation.
- Read-only POMS checks: Tubes precedes Execution; Received tubes links open correct storage, specimen and lineage; Supervisor correction shows required explanation; shipment lookup exposes existing decisions; identifying TEST-HS5Y7DB7-002 yields exactly one bulk candidate with saved storage and unchecked inspection confirmation. Cancelled that form and cleared the unsaved selection. No intake save or execution start was performed.
- Corrected the shared required marker to inline text with a nonbreaking separator; both long accession checkboxes have readable line spacing and fixed 16px controls. Browser geometry confirmed the marker and final wrapped text share the same Y coordinate (657.5px), separated by 3.40625px, with 19.25px line height. The corrected exception form was left open for owner review with no submitted changes.
- Preserved HS5Y7DB7: Received v4, 18 tubes (1 Accepted / 17 intake not recorded), 0 attempts, 1 execution / 0 started. No existing intake decisions or storage locations were changed.

### Continued local acceptance — September 11, 2026

- Exception submission with no reason and no inspection confirmation was blocked; cancelled without saving a rejection.
- Identified existing TEST-HS5Y7DB7-016 for TEST-008; bulk acceptance listed exactly one candidate and retained FB-1000. Submission without inspection confirmation was blocked. Explicitly accepted this synthetic software fixture; no physical inspection is claimed.
- Re-identifying the saved tube excluded it from bulk acceptance (0 candidates) and reported its existing decision. Shipment showed 2 of 18 tube intake decisions. TEST-008 showed Accepted and one available source.
- Confirmed the previously missing job-wide run-one/failure-fallback instruction through the Supervisor form with a TEST ONLY explanation. Reopened workspace displayed the instruction and removed the policy blocker. No source selected and no execution started; the original Planned execution remains linked for subsequent adoption.
- LAB-13 and LAB-09 have partial evidence only. New destroyed-tube persistence, true multi-tube batch, concurrency, physical inspection and full attempt lifecycle remain unrun. Customer-requested holds remain blocked.
- Before the owner clarified manual UAT, focused backend tests passed 23/23 and frontend tests passed 13/13. These do not establish manual acceptance. PostgreSQL acceptance suite and Playwright suite were not completed.
- Owner requested a global single-action button rule during UAT. Applied shared ActionMenu to all 17 feature consumers; navigation menu remains separate. Six focused component checks passed. Source selection is the preserved next operational step.
- Fresh-page live verification: TEST-008 shows Select source tube directly; it opens the existing source form with TEST-HS5Y7DB7-016 / FB-1000. Cancelled untouched. Existing hot-reloaded tab retained an old component identity; fresh load renders correctly. TypeScript and scoped ESLint passed; documentation regenerated (56 guides, 700a8e8a1802). Full role/theme/responsive matrix remains unrun.

### Source selection and first execution checkpoint — September 11, 2026

- Selected existing TEST-HS5Y7DB7-016 / FB-1000 for TEST-008. Deliberately submitted barcode TEST-HS5Y7DB7-002: server rejected it with “Scan the source tube you selected,” preserving inputs and creating no visible attempt.
- Corrected barcode to TEST-HS5Y7DB7-016 and saved. Attempt 1 became Planned, tube became Selected/reserved (zero available for selection), and the original execution 9c32cbfc-46d6-49cb-8967-4c2a172c4e19 was adopted. No duplicate execution appeared.
- Opened the retained execution, confirmed its selected barcode and started it through the supported form. Execution and attempt now InProgress. Only step 1 was recordable; completion remained disabled.
- Saved step 1 identity evidence: source TEST-HS5Y7DB7-016, specimen ACC-1a7a4ae22a064820a25a0157914f4f63, date 2026-09-11, explicit pinned-instruction confirmation, TEST ONLY note comparing saved POMS records without physical handling. UI displayed Recorded, one history entry, Bill Haack and 2:43:08 PM.
- Step 2 became recordable; step 3 remained blocked by step 2, and Complete execution stayed disabled. Stopped before simulated QC. Partial LAB-09 / execution evidence only; no full lifecycle pass.
- The updated Open tubes header uses CardAction; live title/button top coordinates both 346px and trailing inset 16px. Scoped lint passed. No new automated suite was run for this manual checkpoint.

### QC Hold and confirmation alignment checkpoint

Submitted synthetic QC 20 ng/µL / Review needed / Hold without a reason: rejected with “Explain the QC failure or hold.” Saved with explicit TEST ONLY reason and run-record reference. Execution became Blocked and Attempt 1 OnHold, retaining the same source. Step 3 and completion remained blocked; Repeat remained available. Opened Repeat but have not submitted the passing repeat yet.

Owner flagged the step confirmation checkbox alignment. Matched accession confirmation styling: leading-snug with a fixed 16px checkbox offset 2px from the first line. Live label line height 19.25px, label top 630px, checkbox top 632px. Scoped ESLint passed. Repeat form remains open; no additional QC record saved.

### QC repeat, review and first-stage completion — September 11, 2026

- Saved a permitted repeat for TEST-008 on Attempt 1 / TEST-HS5Y7DB7-016: 20 ng/µL, Clear, Pass, run-record reference and explicit TEST ONLY assessment. Execution recovered from Blocked to In progress; attempt recovered from OnHold to InProgress. No reserve or new attempt was selected.
- Expanded Step history (2) and verified the original Hold (2:44:51 PM) and new repeat Pass (2:49:09 PM), including separate values, reasons, actor and confirmation. Completion remained disabled pending the conditional review.
- Recorded the applicable Supervisor review at 2:49:44 PM with rationale comparing both retained results. All completion blockers cleared.
- Confirmed completion of original execution 9c32cbfc-46d6-49cb-8967-4c2a172c4e19 at 2:50:05 PM with a TEST ONLY note. UI shows Completed and locked evidence; all record/correct/repeat controls removed.
- Returned to TEST-008: intake Accepted, Attempt 1 In Progress, original tube still Selected/reserved, zero available for selection. Actions offers Hold attempt, Close attempt as failed, and Assign Library preparation and QC. The second stage has not been assigned or started in this checkpoint.
- This is partial manual workflow evidence, not a full acceptance pass or scientific validation. No physical measurements or customer hold workflow. No automated suites run this checkpoint.
- QC fieldset spacing was adjusted during the walkthrough: 12px legend-to-criteria and 6px outcome-label-to-control, verified live; scoped lint passed.

### Second-stage assignment and resource checkpoint — September 11, 2026

Assigned Library preparation and QC through the existing Attempt 1 action. New execution 9d846e48-1346-404e-a1c5-5fa1bcb1df78 is pinned to Protocol v1 and the same source TEST-HS5Y7DB7-016. Started through its barcode confirmation. The job shows Processing, first stage Completed and second stage In Progress.

Reviewed preparation requirements: qualified test reagent, test equipment, and derived output container with exact source parent. Material-use form offers TEST-LIBPREP-20260911-001 (100 mL). Output options currently contain only the 18 original received tubes; no derived output is present. Cancelled untouched material form. No consumption, equipment use or preparation evidence was recorded. Next checkpoint: create a derived TEST ONLY container under source 016, then record the execution-linked test resources. No new attempt/source or duplicate first-stage execution.

### Derived output and preparation resources — September 11, 2026

Created one synthetic Library container through New container: PH-L-U9QNAFGL5R-E (2eb7aa87-4b55-44f5-a99e-4d7af474473b), label TEST ONLY — TEST-008 Attempt 1 library output, specimen 1a7a4ae2-2a06-4820-a25a0157914f4f63, parent TEST-HS5Y7DB7-016 (670c6b4b-67f4-401e-8a43-b0ea9cc72825), location FB-1000, synthetic quantity 20 µL. Label preview corroborated barcode, accession, job, parent, location and quantity. Closed without opening system print or claiming physical print.

Recorded 1 mL use of TEST-LIBPREP-20260911-001 (a7363dfd-255a-48d4-905c-fb80b3d35b57), selecting the new output container, against second-stage execution 9d846e48-1346-404e-a1c5-5fa1bcb1df78. Recorded equipment PH-EQP-20260911-SVDMBJUH at the form's current local time 14:56 with explicit synthetic run reference. Reopened execution and verified reagent 1 mL at 14:56:18 and equipment use at 14:56:00 in Recorded material and equipment use.

No physical preparation, instrument run or printing claimed. Preparation step remains Not recorded, later steps and completion remain blocked. Next: capture the output barcode and confirm saved resource traceability in step 1. No additional resource records should be created when resuming.

### Two-stage synthetic attempt succeeded — September 11, 2026

Recorded second-stage preparation with output PH-L-U9QNAFGL5R-E, mode Simulated, run-record reference, resource traceability confirmation and pinned-step confirmation. The note names the exact source, reagent use and equipment. Explicitly skipped the optional observation with a reason; later QC became available only after that decision.

Recorded library QC fixture 12 ng/µL / Clear / Pass with reviewed output/parent and resource evidence, synthetic reference and confirmation. Completion became available. Completed execution 9d846e48-1346-404e-a1c5-5fa1bcb1df78 with a TEST ONLY note.

Returned to TEST-008: Intake Accepted, processing Succeeded, Attempt 1 Succeeded and closed 15:01:37; both pinned stages Completed. Source TEST-HS5Y7DB7-016 now Previously attempted and unavailable for selection (zero eligible); no attempt action control remains. One original attempt and source were used throughout. No physical laboratory work, printing, independent scientific approval or Customer release claimed.

This completes this synthetic success path including QC Hold/repeat recovery and explicit optional-step handling. Full LAB-09 acceptance still requires reserve-after-failure, terminal exhaustion, permissions, concurrency, replay/rollback and remaining variants. Downstream library record, scientific review and handoff are separate checkpoints. No automated suite run in this checkpoint.

### Library linkage and form layout checkpoint

Saved one derived library for TEST-008 linking source TEST-HS5Y7DB7-016, output PH-L-U9QNAFGL5R-E and completed preparation execution 9d846e48-1346-404e-a1c5-5fa1bcb1df78. Libraries shows Prepared with Record QC; separate library QC has not been saved. QC form remains open untouched.

Owner requested single-column Library form and full-width QC Disposition. Live Library selects measured 478px each at successive rows, same left edge. QC Disposition and observations both measure 478px with matching left edges. Scoped lint passed. A temporary empty New library verification form was cancelled; no duplicate library created.

### Library QC and batch discovery checkpoint

Saved separate library QC for PH-L-U9QNAFGL5R-E: Pass, explicit TEST ONLY observations referencing the completed preparation and source/output lineage, and one synthetic library concentration measurement 12 ng/µL copied from retained execution evidence. No new instrument measurement or scientific approval claimed. Library now shows Qc Passed and Add to batch.

Review tab states No scientific approval recorded. Add to batch offered existing draft PH-BAT-20260718-7PKMBTKT. Cancelled without assignment: its purpose/provider and suitability have not yet been inspected. Next safe step is inspect that batch before reusing it, or create an appropriately designated synthetic batch if unsuitable. No batch membership, dispatch, provider communication or release occurred.

### Dedicated batch and duplicate-scan checkpoint

Inspected existing draft PH-BAT-20260718-7PKMBTKT: old Test fixture, External Sequencing, zero libraries. Left unchanged. Created clearly designated TEST ONLY — TEST-008 acceptance 2026-09-11, number PH-BAT-20260911-KNFHQ3Z8, with notes excluding physical shipment, provider processing and Customer release.

Scanned PH-L-U9QNAFGL5R-E into the new draft; success message and one-library count verified. Repeated the same scan; rejected and membership remained one. UAT finding: duplicate error says “Only a QC-passed library can be added to a draft batch” rather than explaining existing batch membership; integrity guard passed, error clarity needs improvement.

Batch remains Draft. No Start, dispatch, provider communication or completion action performed. Preserve this batch and membership when resuming; do not recreate or add the library again. No automated suite run.


### Preparation-batch implementation checkpoint

Implemented the connected tray journey in the [owning plan](../../plans/LAB-WORK-JOURNEY-PLAN.md). [LAB-14](../06-laboratory.md#lab-14--preparation-trays-shared-evidence-and-sequencing-handoff) is the repeatable manual script. Migration `20260911234552_AddLibraryPreparationBatches` was applied only to verified local localhost/phaeno_ops; the ERD and Phaeno help were regenerated.

- **Persisted software verification — Passed:** 15 domain/attempt cases and two PostgreSQL preparation journeys. Separate generated TEST ONLY jobs tested concurrent reservations, unauthorized Customer reads/writes, stale saves, locked tray identity, shared evidence and tube exceptions, idempotent material use, explicit failure and reserve fallback, output creation or existing-output selection, optional final-stage skip, QC reuse and duplicate sequencing rejection. Test records were cleaned by their own identifiers. The optional-stage test initially failed on its fixture lookup; the corrected test passed. This was not a scientific-method validation.
- **Browser fixture verification — Passed:** ten checks across desktop/mobile Chromium. Shared-entry accessibility and full-width select, dark/reduced-motion mobile, exact coverage, failed-tube exclusion, output identity carried forward, uncertain-response retry, and nested resource entry retaining unfinished step values. Five protocol-definition tests, type checking and scoped lint also passed.
- **Signed-in local inspection — Passed for inspected surfaces only:** Library prep opens its batch list and separate legacy job lookup. New tray-format preview updated with rows/unavailable positions. Its select width matched the 446 px field. Cancel saved nothing.
- **Preserved checkpoint:** no new action was taken on TEST-008, PH-L-U9QNAFGL5R-E or its sole membership in Draft PH-BAT-20260911-KNFHQ3Z8. No retrospective preparation tray was added.
- **Still Not run/Blocked:** the full signed-in physical tray/scanner/label journey and owner acceptance; all held/closed Trial race permutations and staff-role combinations; provider processing, shared/production migration and deployment. Customer hold-request workflow remains blocked from implementation.

Final local inspection: after restarting the verified API build, HS5Y7DB7 still shows Processing, nine specimens and no open exceptions. TEST-008 remains Succeeded on Attempt 1/source TEST-HS5Y7DB7-016; PH-L-U9QNAFGL5R-E remains Batched. Returned to Library prep without operational writes. Desktop and dark mobile screenshots were reviewed; required legends now precede the modal actions, checkbox labels wrap together with their markers, and the page has no horizontal overflow. The four affected shared-entry/nested-resource browser checks passed again after the footer spacing adjustment.
