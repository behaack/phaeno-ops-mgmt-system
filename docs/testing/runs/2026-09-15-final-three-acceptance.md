# Final three acceptance cases — September 15, 2026

## Current outcome

**ACC-06 and ORD-03 close for the approved isolated software scope. The ledger is 80/81 closed (98.8%): 39 Pass and 41 Pass (simulated), zero Fail and one Blocked (SYS-06).** The simulated label identifies the reserved test email and local invitation transport; authenticator enrollment, sign-in, role administration, session revocation and HTTP authorization were real.

## ACC-06 — completed step crosswalk

| Step | Evidence |
| --- | --- |
| 1 — Root, protected route and bootstrap | Retain the fresh signed-out root/Job/Trial observations from the [remaining-case run](2026-09-15-remaining-case-acceptance.md), together with the existing pending-bootstrap component guards. This continuation signed out the old isolated browser identity, opened the fresh invitation, and observed no Portal workspace before admission. |
| 2 — Required private MFA | Read-only development-provider settings confirmed required MFA during new-account setup, authenticator and backup-code support, and no SMS. The owner privately created the password, enrolled an authenticator and saved backup codes. Provider readback confirms TOTP, MFA and backup codes enabled. The owner subsequently reported that the email test code was rejected at the authenticator prompt and the current authenticator-app code succeeded. No password, setup key, QR code, backup code or actual authenticator code was recorded. |
| 3 — Controlled expiry and draft | Opened Create Protocol and entered a recognizable unsaved test name/description. Revoked only this test identity's active provider session. The actual Portal returned to Sign in and removed the form without a success indication. After the short-lived signed token expired plus the API validator's clock tolerance, a real HTTP POST of that draft returned 401. Independent database read found zero matching protocols. The owner signed back in privately; reopening the form showed both fields empty. Drafts are not durable across confirmed sign-out. |
| 4 — Pending versus accepted roles | With explicit owner confirmation, actual local User management created UAT Final MFA with Operator and ProtocolAdministrator intent. The invitation remained Pending and there were zero local users for its address before acceptance. After private enrollment, Access unavailable correctly required Continue invitation. Owner acceptance created exactly one active local user. A fresh authenticated session read showed Operator and ProtocolAdministrator enabled, with reviewer, supervisor, access administration, platform administration, Finance and result release denied. |
| 5 — Changed roles on a fresh request | With explicit owner confirmation, actual User management removed Operator, added ScientificReviewer and retained ProtocolAdministrator. Fresh authenticated API reads and the reauthenticated browser reflect these exact permissions. The removed Operator assignment remains inactive at version 2; ProtocolAdministrator remains active at version 1 and ScientificReviewer is active at version 1. Retain the [existing negative controller tests](2026-09-15-session-role-acceptance.md) for unsupported title/provider claims, self-escalation and stale versions. |

Invitation delivery is expressly local/simulated. The initial UI submission used the development logger. After the normal five-minute resend cooldown, the actual resend endpoint rotated the same invitation once into a capture-only test transport; it did not create a duplicate or send an external message. Two early resend attempts correctly rejected the cooldown. The saved invitation is `5199b1f5-1f31-4c2e-8800-22e4c8b2aa38`; accepted local user is `d758f41f-0cfa-4a0c-a5eb-b6324991cc5d`. The test account's final intended roles are ProtocolAdministrator and ScientificReviewer.

Automatic approval review rejected the agent's attempt to enter the reserved email test code. The owner performed verification and all private credential/enrollment steps. The agent did not bypass that rejection. The fixed email test value is not an authenticator code.

Evidence: `tmp/final-three-acceptance/mfa-invitation.json`, `mfa-provider-readback.json`, `before-role-edit.json`, `after-role-edit.json`, `expiry-receipt.json`, `expired-save-result.json` and `recovered-session.json`. The expired test bearer was held only in a Windows-encrypted temporary credential file and removed after the negative check. Evidence retains status and capability fields, not bearer tokens or private MFA material.

Cleanup: used the recovered test account's normal Sign out action and verified the Portal returned to its email sign-in page with no protected workspace. The accepted identity and role history are retained for traceability; no MFA reset was performed.

## ORD-03 - Change-quote implementation and final verification

The prior `quote_not_allowed` result remains historical evidence of the defect. The additive workflow is now implemented: staff issue a separately frozen source/count/price proposal; the Customer organization administrator accepts or declines it. Acceptance preserves original placement and accepted quote, opens only additional sample entry, and finalization appends a versioned Lab authorization and a shipment containing only the added samples. Started work retains all prior specimens and its pinned scientific configuration. Completion includes every accepted quote's charges and tax and still requires all accepted samples to finish.

| Required step | Evidence |
| --- | --- |
| 1 - Submitted profile, correction and revision | `ManualQuoteRetainsCorrectionAndIndependentPriceReview` creates and submits through the Customer controller, requests a specific storage correction through the Commercial controller, updates and resubmits. It checks unchanged revision 1, linked revision 2 with the correction reason and changed profile, zero samples and zero Lab work. |
| 2 - Required line, quantity and catalog guards | The same journey rejects absent lines, an unknown catalog item, the wrong specimen quantity and an inactive item. Existing `UnrelatedSpecimenCatalogItemCannotSatisfyLabServicePricing` rejects an unrelated service line. |
| 3 - Independent pricing and tax variants | With dual control enabled, a deliberately arranged same-actor proposal is rejected; the restored Customer proposal is reviewed by the distinct Commercial Operator. The persisted decision identifies the reviewer. Actual issuance yields a $200 pre-tax quote, then a separately approved 10% tax configuration yields a superseding immutable $220 quote. Existing domain tests retain amended-price reasons and proposal snapshots. No exemption from dual control was introduced. |
| 4 - Current eligible Customer acceptance | The journey rejects superseded, expired and non-administrator acceptance, then accepts the fresh current quote at $220 without creating Lab work. Existing PO and stale/replay checks remain in the regression suite. |
| 5 - Unaccepted scope increase | `ChangeQuotePreservesAgreementAndRequiresCurrentCustomerAcceptance` checks the frozen proposal and unchanged original agreement, stale/repeated acceptance and separate decline. `ChangeQuoteAppendsWorkAndChargesWithoutChangingAuthorizedSamples` rejects added sample entry before acceptance; acceptance then permits only new rows. Previously authorized sample deletion is denied. |

Additional continuation proof: finalizing accepted additions preserves the original roster timestamp, work status and received specimen; creates authorization version 2 and only the additional shipping contents; and combines original and accepted additional pre-tax charges into one $220 invoice ($200 + $20 tax). Completion replay creates no second invoice. Scientific terminal outcomes in this billing fixture are explicitly simulated, not evidence of real processing. A separate journey rejects superseded/expired/unauthorized Change decisions without changed accepted counts or Lab authorization.

### Verification and environment

- Backend results: the broad 74-check run passed 73 and exposed a missing CRM pipeline seed in an existing test helper. The helper now owns its pipeline/stage and cleanup; the one failed test passes on rerun. The additional complete manual-quote journey passes separately, bringing the combined distinct passing total to 75 (see `final-backend-results.json`). Fixture setup was corrected before its final passing run. No failing check is silently counted as passed.
- Frontend: 40 focused component checks pass; TypeScript and focused lint pass. Four browser scenarios pass at 320 and 1440 pixels in light/dark, including required acceptance/PO controls, actual form requests, keyboard checkbox use, axe checks and no page overflow. The APIs in those browser fixtures are explicitly intercepted. Phone screenshots were visually inspected.
- The three-column additive migration was applied only to the owned loopback UAT database on port 5436. EF reports no pending model changes. The ERD, Customer/Partner/Phaeno guides and generated 56-guide corpus were updated.
- The local API on port 7116 was refreshed to the verified application assembly, SHA-256 `70EBCEEA802954283D4AF7C2A3FA6341F328875225A735BDC77C437370700CD3`; health reports healthy and the existing Portal on port 3016 returns HTTP 200. Existing test Jobs and Finance fixtures were preserved. This is local proof, not a deployed-release claim.
- Evidence: `tmp/final-three-acceptance/change-quote.trx`, `change-quote-regression.trx`, `manual-quote.trx`, the targeted rerun in `tmp/scientific-ten-results/scientific-ten.trx`, and `frontend/test-results/change-quotes-Change-quote-review-*` screenshots. Disposable database cleanup is recorded in `tmp/scientific-ten-results/database-cleanup.txt`.

Automatic approval review rejected an exemption from the existing two-person pricing check. The check was retained unchanged and the final manual-quote journey explicitly exercises its rejection and distinct-reviewer success.

## SYS-06 — independent export prerequisite confirmed

**Later September 15 continuation:** the owner approved the scoped rollout. Actual coordinated recovery and restored downloads now pass; backup-only publication, successful production capture, encrypted off-server copy and timer activation are complete. The remaining actual overnight scheduled evidence is assigned to an owner-approved automatic follow-up. See the [current recovery report](2026-09-15-sys06-recovery.md); the paragraphs below preserve the earlier checkpoint.

Read-only GitHub inspection confirms `main` is the default branch and the active workflow inventory contains Deploy Portal Green, Purge Retired Web Operations Records and Recover Production Public API. It does **not** contain Back Up Portal Database and Files. Looking up `portal-backup.yml` reports that the workflow is absent from the default branch. The repository's backup workflow and runbook explicitly require default-branch placement for independent scheduled collection.

This confirms an unresolved scheduled off-server activation gate; no manual run is counted as a scheduled export. The previous 26 backup safety checks and isolated two-file restore remain supporting evidence. Coordinated populated database/private-file capture with actual API recovery, a restored Portal download, and exact release/owner acceptance remain outstanding. No production writer interruption, timer activation, workflow dispatch, deployment or Git mutation was performed.

Evidence: `tmp/final-three-acceptance/github-repository.json` and `github-workflows.json`. The prior ACC-06 evidence used the local governance build; ORD-03 subsequently refreshed the local API as recorded above. Portal is local port 3016, database is the isolated loopback acceptance database on 5436. Source HEAD remains `7df0ccbef62252732ceae877abb4fe7bb9a721dc` plus existing workspace changes; this report does not assert a matching production release.

The remaining approval and operational steps are recorded in the reviewable recovery rollout. SYS-06 remains Blocked until its actual coordinated recovery, independent scheduled artifact and release/owner acceptance evidence exist.
