# Ten further acceptance cases — September 14, 2026

The owner requested ten complete cases in this pass. Baseline: 19/81 Pass; target: 29/81. No case is promoted merely because its prerequisite is prepared. Every required step and variant remains controlling.

## Selected dependency chain

Initial selection: ACC-03, ORD-01, ORD-02, ORD-03, ORD-04, TRI-01, TRI-03, TRI-04, LAB-09 and LAB-14. Final ten closures: **ACC-03, ORD-01, ORD-02, ORD-04, TRI-01, TRI-02, TRI-03, TRI-04, LAB-14 and SHP-02**. The user was informed during execution that ORD-03's missing post-acceptance change-quote workflow remains open; TRI-02 and SHP-02 were completed using the newly available prerequisites. LAB-09's remaining tube-attempt variants also remain open. No criteria were removed from either open case.

Initial authenticated readback at 2026-09-15T04:23Z confirmed zero analysis definitions, zero Assembly profiles, zero Partner Kit offerings and zero Trials. Customer A retains five draft Finance Jobs. The existing Partner has no Lab-service entitlement. System sample/readiness/result destination values remain empty. Evidence: ignored `tmp/uat-closure/ten-case-readiness.json`; actual P-ADMIN, K-ADMIN, C-ADMIN and P-TRIAL-S sessions. These are setup blockers, not new application failures.

## Approved isolated test configuration

The owner explicitly replied **“You have all required approvals”** after reviewing this proposal. This authorizes the bounded isolated setup and test execution below; it is not approval of a commercial scientific service.

- Use the existing **Reference extracted RNA** sample type and **Reference receiving** destination, preserving their current limits and revisions. Use the existing **TEST ONLY — Two-stage library preparation, version 2** workflow. No changes to existing scientific definitions or original walkthroughs.
- Add a separate **TEST ONLY UAT completion analysis** with the existing FASTQ, FASTA and BAM deliverable definitions, coded sample references, and the existing quantity rules. No new biological or clinical claims, acceptance thresholds, laboratory observations or result files. Reference biological source **Human PBMC** comes from the existing standard-order test contract; additional explicitly named TEST ONLY source groups exercise roster grouping without claiming real specimens.
- Review this new definition as eligible only inside the isolated local UAT database. Keep separate synthetic/inactive/future definitions for negative purchase checks; do not flip an existing synthetic definition to evade the gate.
- Add a new test standard offering: USD 100 per specimen, two specimens, existing approved test tax 10 percent where required, seven-to-fourteen-day test turnaround. Total for the small complete-tax fixture: USD 220. Use the existing frozen billing configuration; prepare a separate pre-tax variant. No real purchase or customer communication.
- Configure local sample readiness as exact roster, result destination as governed Portal, and submission instructions explicitly marked TEST ONLY. Limit effective service access to the existing UAT Customer/Partner and Research Departments. Preserve original values and record configuration versions.
- Trial fixture: allowance two originals, extracted RNA, the existing test workflow/destination and named deliverables above, an open thirty-day test submission window, run one tube per specimen with reserve only after failure. Require existing distinct Commercial and Scientific Operations test delegates and Prospect administrator acceptance. Material terms: retain for review; no disposal or physical custody is fabricated. Separate dated/held/closed/replacement variants test software gates only.
- Retain newly accepted snapshots and histories. Retire new offerings and restore any temporary defaults after dependent checks; no deletion of original records. No production deployment, shared migration, external message, provider delivery or actual scientific/physical signoff.

The scientific-fixture review is required by TEST-DATA, which assigns approved operating definitions to the Lab owner and requires properly reviewed eligible configuration for positive purchases. Engineering will make all implementation choices and prepare/run the tests after this product-level decision.

## Execution status

**Ten cases closed in this pass. Total: 29/81 (35.8%); 52 remain.** This is isolated software acceptance, not physical, scientific, provider or release signoff.

### Completed crosswalks

| Case / steps | Connected evidence and result |
| --- | --- |
| TRI-01 / 1 | Actual Company Requests creates Work → Trial Project for the linked Opportunity. Start Trial retains exact Company/request and Back to Company. Repeating the request creates no second Trial. |
| TRI-01 / 2–3 | Commercial staff saves incomplete objective/allowance; separate Scientific Operations session resumes identical shared values with last-editor metadata. No scope approval, Prospect visibility, samples or Lab authorization is created by the draft. |
| TRI-01 / 4 | Two staff sessions edit the same version. First save succeeds; stale save returns 409 with entries retained. Reload/review permits deliberate recovery without silent overwrite. |
| TRI-01 / 5 | Missing scientific/date fields block submission without a request. Completing the approved TEST ONLY scope creates revision 1, clears draft, pins scientific references, and remains 404 to Prospect before approval. |
| ORD-02 / 1–2 | Customer Department admin creates/reopens a two-specimen Human PBMC pricing profile without individual samples or Lab work. Actual organization-admin review displays included processing/assembly, analyses/outputs, version and turnaround; arithmetic is 200 + 20 = 220 USD. |
| ORD-02 / 3–4 | Department-admin commitment returns 403. Organization admin deliberately confirms; delayed response disables dismissal, dropped committed response retains retry, exact replay returns the same version. One accepted quote and unchanged frozen commercial snapshot persist. |
| ORD-02 / 5 | Separate draft is reviewed at 100/specimen, then catalog changes to 110. Placement returns 409 and retains PO. Refresh terms requires fresh affirmation and accepts 242 USD. Catalog restored to 100; prior Customer and Partner snapshots remain 220. |
| ORD-02 / 6 | Entitled Partner Department admin prepares the profile but cannot commit. Partner organization admin accepts 220 with the separately prepared approved test billing configuration. One quote; no Partner Finance UI assumption. |
| TRI-03 / 1–2 | Actual Prospect organization-admin form captures two coded RNA samples, one/two tubes, destination/type and required organism metadata. Duplicate UI validation plus server duplicate/missing-metadata/zero-quantity/fractional-tube variants save no partial roster. |
| TRI-03 / 3 | RUO/no-PHI affirmation and actual UI submission commit once despite controlled response loss; identical retry returns the same samples. Independent database read confirms one authorization, one Lab job, one shipment, two samples and three tube slots, with the approved workflow and run-one/failure-fallback policy. |
| TRI-03 / 4 | Third original returns 409 allowance full. Prospect Department-admin submission returns 403. Original two samples remain. |
| TRI-03 / 5 | Separate before-window, after-window, unaccepted, held and ClosedIncomplete records each return the specific 409 submission blocker with zero samples/work/shipments. The expired-date branch uses explicitly staged temporal acceptance; see boundary below. |

Main Trial `afb1f9bf-9c11-4ecb-8711-2e8267b1b7a6` / `TR-20260915-3F3BCC7F02E5`; authorization `353283e6-2fb5-4836-be3c-204c64f2f827`; Lab job `ddc2773e-2e9b-4f9c-acf1-8d9610da1795`; shipment `ce2f15f5-32b7-429e-88c3-a910c7a65399`. Customer `XJ2AA49M`, Partner `5TAJSVJY`, changed-price `YPN2KHJL`. Read-only PostgreSQL assertions passed at 2026-09-15T05:10Z.

Evidence under ignored `tmp/uat-closure`: `ten-trial-connected.json`, `ten-trial-submission.json`, `ten-orders-connected.json`, `ten-readback.sql/.json`, and `ten-first-three-verified.json`. Journals retain intended writes, actual status/body, actors, versions, completed phases and failed observations. They do not contain credentials. The controlled after-window fixture `899d1452-5320-4383-8a70-5190f40f37dc` was explicitly staged with a 2020 test clock through the domain method to isolate expiry from acceptance. This is **not an actual historical acceptance**; `ten-expired-fixture.json` records the boundary. The main acceptance and all other state transitions use actual authenticated API/UI actions.

### Defects found and corrected locally

- Department selection reset from Research to General during refresh. The session effect now waits for authentication loading before clearing a signed-out selection. Regression fails before correction; three persistence/fallback/sign-out tests and three existing invitation-session tests pass. Actual Customer/Partner Research survives refresh. TypeScript and scoped ESLint pass; Customer/Partner access guides updated.
- Trial lifecycle actions returned 404: route parameter `action` collided with MVC action selection. Renaming the internal parameter to `operation` preserves public URLs, roles and idempotency payload. Real endpoint routing fails before correction and passes afterward for six operations. Actual held/closed transitions and their submission denials now pass.
- Zero-tube roster additions returned generic HTTP 500 without saving. Explicit positive-tube validation now returns 400 for add/edit. A disposable PostgreSQL test fails before and passes after correction: zero/negative entries preserve roster/version and valid retry succeeds. Live zero/fractional/duplicate checks, CSV errors and stale-import recovery pass.

Corrected isolated API started 2026-09-15T05:05:40Z from `tmp/uat-ten-route-build/bin/PSeq.Operations.Api/debug/PSeq.Operations.Api.dll`, SHA-256 `DAD4754C19CC8F3D2DEA8A39B5FD3164E933DE78C23B6343D5C534AC96D5CCCF`; 7116/5436 unchanged. Healthy endpoint confirmed. Three focused backend regressions pass with none skipped. No production deployment, shared migration or Git mutation.

### Remaining work in this pass

This pass's ten-case target is complete. ORD-03 manual quote variants, including missing post-acceptance scope-change implementation, and LAB-09 remaining policy/source variants remain explicitly open in the controlling ledger.

### Additional four closures

| Case / required steps | Evidence and result |
| --- | --- |
| ACC-03 / 1, 3–5 | Retained `acc03-connected.json`: actual Organization/Department/member sessions create/default Department, review exact-email scoped membership, deny final-assignment removal/deactivation, add alternate access, deactivate/reactivate without silently restoring assignments. Original defaults and memberships restored. |
| ACC-03 / 2 | `ten-department-snapshots.json`: actual organization PO required → Research waived → inheritance → original settings. Accepted XJ2AA49M quote/commercial/placement snapshots and historical shipment DTO unchanged at each stage. `ten-packet-immutability.json` independently compares both complete historical packet database rows, including manifest bytes, before/after another supported PO change and restoration; hashes identical. Shipment b5300605-c66e-4ebe-ae16-00a61b110111 retains both packet revisions. |
| TRI-04 / 1–2 | `ten-trial-amendment.json`: new Trial 3a9f32e7-4145-4bd9-8b2a-cc5d6a048afc saves amendment draft without changing revision 1 approval/acceptance/sample facts. Revised scope submission requires two fresh distinct approvals and Prospect administrator acceptance; submission denied at each incomplete gate. Original sample stays pinned to revision 1. |
| TRI-04 / 3 | Actual Scientific Operations hold blocks return-kit mutation (409, no kit) and all seven scientific preparation commands. Actual Operator custody exception remains recordable. Nested transaction defect fixed below. |
| TRI-04 / 4–5 | Actual hold release allows one deliberate move. Explicit software-test failed-original replacement authorization records responsibility/reason; Prospect admin submits exactly one linked replacement against revision 2. Original allowance remains two with one original remaining. Reuse and duplicate authorization return 409; retroactive return-arrangement change returns 409 with scope/history unchanged. Test hold resolved. |
| ORD-04 / 1–2, 4 | `ten-roster.json`: exact three-column template; source-inherited Add, tube-count Edit, cancellation retains row; saved rows omit Expected badge. Populated import disabled with explanatory help while template remains available; reviewed removal of all rows enables import. |
| ORD-04 / 3 | Full group disables Add and target move; other source stays usable. Duplicate/zero/fractional input rejected unchanged. `ten-roster-mismatch-connected.json`: prepared legacy two-Human roster against accepted one-Human/one-other scope has exact total but red excess warning, disabled review, server 409 and no authorization. Additional unmatched-source fixture remains visible with red explanation; three rows retained. Explicit synthetic invalid legacy data, no accepted scope or scientific facts rewritten. |
| ORD-04 / 5–6 | Valid CSV preview and Cancel save nothing. Separate session adds a row after preview; confirmation returns 409 preserving it. Deliberate clear and fresh preview/review saves two rows atomically. Duplicate, barcode-extra-column, wrong count/source, zero/fractional previews identify errors with no partial rows. |
| ORD-04 / 7–8 | `ten-roster-variants.json`: accepted fourteen-specimen, two-source fixture shows bounded ten-row pages, long source wrapping and natural A2/A10 and B2/B10 order. Phone 390 dark/reduced-motion review has no page overflow; accepted group order, summary focus and keyboard confirmation/cancel work. Cancel leaves no authorization. |
| ORD-04 / 9–10 | Two-sample/three-tube finalization commits once despite dropped response; exact replay returns same version/work/shipment and locks ordinary editing. Separate ambiguous-shipping setup rejects finalization 409 without partial authorization or lost editable rows. Retiring only the new ambiguity definitions restores readiness; actual UI retry succeeds. All original shipping configuration rows compare unchanged. |
| LAB-14 / 1–9 | Retained [signed-in preparation run](2026-09-12-lab-14-preparation.md) and [closeout](2026-09-12-laboratory-uat-closeout.md): tray preview/validation, server naming, compatible workflow, empty/ineligible sources, mixed jobs, positions/capacity, move/remove, wrong/correct scan, locked start, source identity and explicit selected-member/shared QC coverage. Controlled simultaneous server-name collision is identified as PostgreSQL engineering evidence. |
| LAB-14 / 10–18 | Same retained connected run: equipment/material quantities and histories, role/prerequisite and repeat/correction guards, failed member retained while another continues, linked reserve at first stage, output identity/quantity/parent checks, optional final-stage decisions, unresolved-member closure denial, successful closure and exactly one sequencing membership. Attempt success does not substitute for scientific result approval. |
| LAB-14 / 19, 21 | [Populated recovery](2026-09-14-acceptance-closure.md#lab-14--populated-recovery-and-customer-isolation): actual lost-create replay, separate Operator/Supervisor stale move with retained draft, one material consumption after dropped response, nested cancellation preserving parent values, phone dark/reduced-motion inspection. Original unfinished batch preserved. |
| LAB-14 / 20 | Earlier actual Customer denies both reads and all seven commands. `ten-preparation-guards.json` adds four connected held/closed Job/Trial fixtures × add/start/move/remove/cancel/output/step = 28 specific 409 responses, complete work/batch readback unchanged after each. Four attempts remain Planned/unstarted; actual Trial hold release permits exactly one move. Job terminal states and reserved trays are explicitly synthetic fixtures; Trial hold/closure use actual authorized UI. |

Independent `ten-guard-readback.sql/.json` confirms four planned attempts, one permitted move, two amendment scopes, one replacement authorization and original/replacement lineage. `ten-seven-readback.json` refreshes new order/quote/authorization/shipment/tube-slot facts. XJ2AA49M has Lab work f4bdb0f0-491e-4f03-980a-8ef4426475a6, authorization 4c78fccf-9b07-4d50-9cbf-7caef777f19e and shipment 42d08454-f4f6-40ab-8513-2424b6c87c38; two samples and three slots. Ignored journals preserve exact requests/statuses and the explicitly synthetic fixture boundaries.

Additional defect: Trial return-kit creation attempted a second database transaction inside the existing Trial guard and returned 500. Shipping packing now joins the existing transaction while acquiring its advisory lock; the outer transaction retains commit/rollback ownership. A disposable PostgreSQL regression fails before and passes after, proving competing-lock exclusion, rollback and standalone ownership. Actual held Trial now returns the intended 409 with no kit, while its custody exception succeeds. API refreshed at 2026-09-15T05:21:03Z to `tmp/uat-ten-shipping-build/bin/PSeq.Operations.Api/debug/PSeq.Operations.Api.dll`, SHA-256 `4B5929455CCB972970742E47D0525FE1E2965B2CB217E49049F49026362EDB50`; focused transaction and routing checks pass (three, none skipped).

ORD-01 in progress found and corrected setup navigation losing unsaved intake details. Setup links now open separately; Refresh readiness retrieves completed configuration without discarding the form. `ten-readiness-probe.json` retains the original failure; `ten-readiness-complete.json` proves Customer/Research/reference/source count/storage survive setup and refresh, then actual Commercial Operator starts pricing while online administrator/billing blockers remain separate. Temporary catalog inactivity restored. The remaining ORD-01 gates were subsequently completed in the final-three crosswalk below.

### Final three closures

| Case / steps | Connected evidence and result |
| --- | --- |
| ORD-01 / 1–2 | `ten-readiness-complete.json` proves the Customer with incomplete setup remains selectable; Department and entered pricing details survive a separate setup tab and explicit Refresh readiness. The missing active specimen catalog item blocks only pricing. Active item restoration enables pricing without re-entry. |
| ORD-01 / 3 | Actual Commercial Operator creates the pricing Job while the Customer has no online administrator and no billing profile. `ten-readiness-gates.json`: quote issuance returns specific `operational_readiness_incomplete` 409 after the local DerivedReadiness flag is enabled. Separate synthetic invoice-precondition fixture returns `billing_profile_required` 409; complete order/version readback is identical and database invoice count zero. This stages only the negative billing gate, not a real acceptance or completed laboratory journey. |
| ORD-01 / 4–5 | Actual administrator purchase attempts for inactive/synthetic/future definitions return 409 and previews list blockers; no quote/work is created. New offering revision 2 becomes the only applicable active family version, revision 1 leaves new selection, and the previously accepted XJ2AA49M complete DTO/frozen 220 USD scope/tax stays unchanged. |
| TRI-02 / 1 | Existing signed-in Commercial primary Bill Haack uses the isolated Trial configuration UI to designate UAT P-TRIAL-S as a temporary Commercial delegate, leaving Note blank. Saved authority names actor, primary linkage and effective time, without creating a scope decision. Its existing Scientific Operations authority remains. No original primary/delegate row is changed. |
| TRI-02 / 2–3 | Retained TRI-03 main journey proves Commercial-first single approval cannot authorize scope. New dual-authority variant exercises the reverse order: Scientific Operations first, then the same person's Commercial approval is rejected 409 with one decision and unchanged version. Separate P-PRICE approves Commercial; two different actors satisfy the gate. This explicitly records which ordering was executed, rather than claiming a second Commercial-first dual variant. |
| TRI-02 / 4 | Actual R-DEPT acceptance returns 403. R-ADMIN opens exact revision/allowance/terms review; unchecked affirmation produces a field error with no request. Deliberate confirmation accepts once; staff and Prospect refresh retain the same accepted revision. |
| TRI-02 / 5 | Separate Trial has Commercial approval then Scientific Operations Request changes. New revision 2 starts with no decisions or acceptance; one fresh approval is insufficient, two distinct fresh approvals restore eligibility. Both historical revisions and their decisions remain. External scope excludes internal values and actor IDs. Temporary Commercial delegation is revoked by the same primary immediately after the dual check, retaining past decisions and original authorities exactly. |
| SHP-02 / 1–2 | Retain the prior five-session location validations, view-first creation, full-width address and Department scope from `shp02-connected.json`. New TEST ONLY Department and legitimately accepted/finalized two-specimen Job make the no-location shipment path available. Actual Add a delivery location opens the correct Customer/Department, required errors block empty submit, and a fictional TEST ONLY address saves as the default. The inbound laboratory destination remains separate. |
| SHP-02 / 3 | `ten-location-return.json`: Return to Lab Job preserves the exact shipment and reopens unsubmitted kit review with the saved default. Separate two-location/no-default state requires explicit selection; missing selection gives a field error without a request. Manage delivery locations and return reopen review without choosing silently. Selecting the second address displays that exact address. No kit order is submitted; independent database count zero. |
| SHP-02 / 4–5 | Retained prior actual default transfer, stale 409 with entered values preserved, cancel/confirmed deactivation, inactive retained history, phone menu/full-width layout, other-Customer/Department denials and member-write 403. New second location retired and first restored as the sole active default. Existing Research locations and stock are untouched. |

New identifiers: Trial dual approval `37bfd257-a4e5-4a33-a563-dccbd5888de7`; requested-changes Trial `96e4a315-1741-4f7f-8d5c-01a6421966e8`; temporary revoked authority `5e13a7c5-d01d-46cc-8c52-bdf7278149af`. Delivery Job `17a0c20f-cec4-4d18-925f-3af90e66f0ac` / `5BZAH38H`, shipment `6810661d-3409-406f-be9a-0ce5a0ded30f`, Department `8367f5c2-6f99-4e48-a8b6-33049b979ff5`. The new Department has only explicitly fictional delivery data and no physical inventory, dispatch or receipt.

**Additional fixes and configuration finding:** address setup carries an explicit resume-kit-review intent. Ordinary location-inventory return still does not open another kit review. The outer embedded Job navigation lock no longer suppresses the kit dialog's own address link; that dialog retains its dirty/pending guard, while sibling shipping controls remain locked. The manual script's generic automatic return expectation is implemented specifically for setup entry, preserving the existing inventory-navigation test. The initial quote attempt succeeded because the isolated runtime had DerivedReadiness off; this is a baseline configuration defect, not proof of an enforcement failure with the flag on. That unaccepted test quote is retained in history, and a fresh attempt with enforcement on returns the correct 409. No Customer accepted it and no invoice/work followed it.

**Verification:** final read-only PostgreSQL assertions at 2026-09-15T06:14Z confirm one accepted main quote, one finalized shipment/two samples/three slots, one Lab job, distinct Trial decision actors with both correction revisions, revoked temporary authority, one active delivery default, zero accidental kit orders and zero billing-gate invoices. `ten-final-readback.json`, `ten-final-gates.sql/.json`, `verify-ten-final.mjs`, `ten-trial-approvals.json`, `ten-location-return.json`, `ten-cleanup.json` retain exact evidence. Focused frontend suites pass: 18 pricing/location checks, 32 kit-dialog checks and 20 shipping-workspace checks, plus the earlier six auth/invitation checks. The new shipping test's initial fixture incorrectly used a promotional order and hidden preparation; corrected to a Customer order with the actual kit surface. Earlier enabled-submit expectations were also harness assumptions: both forms deliberately expose field validation on submit. TypeScript and scoped ESLint pass. React review found no new dependencies, waterfalls or duplicate state; existing query refresh and child dialog guards are reused.

**Cleanup:** all five new offering records are inactive; four temporary service entitlements are ended. Original catalog remains active at 100 USD. Original system instructions/sample/result/shipping configuration restored exactly, with audited version increment. The current API rejects the old empty sample-configuration shape, so exact legacy restoration used a guarded isolated fixture update after recording the rejected supported attempt; no new operational configuration was invented. All original Trial authorities compare byte-for-byte at DTO level; only the temporary authority adds retained revoked history. Original accepted quote/scope snapshots and packet manifests remain unchanged. The corrected local API uses the same shipping binary with DerivedReadiness enabled from 2026-09-15T05:49:58Z; healthy at 7116. No Git mutation, deployment, shared migration, provider message or real scientific/physical signoff.

**Open implementation gap:** ORD-03 requires an immutable post-acceptance scope-increase quote. Current quote issuance only admits QuoteInPreparation/QuoteIssued, fixes quantity to the original requested count, and lacks a proposed-scope lifecycle after acceptance. Implement proposal/versioning, separate Customer acceptance and affected-work gating before closing ORD-03; do not mutate accepted scope or label a pre-acceptance revision as this variant. LAB-09's remaining policy, same-attempt hold and concurrency/source variants remain scheduled separately. Neither is counted in these ten closures.

**Final closeout check (2026-09-15T06:23Z):** final saved-record assertions, TypeScript, scoped ESLint and whitespace checks pass. The ledger totals independently reconcile to 29 Pass, 8 R and 44 B. Documentation generation/check passes for 56 guides, corpus `bf00a9dae8ff16bdd514601c0ec50f7610c5d43d483e368a5c1ec06ff05c0f3b`. The first live help check correctly detected the API cached the preceding corpus (409); restarting only the owned 7116 API with the same binary/configuration refreshed it. Fresh Clerk-authenticated search returns 200 with the exact corpus hash and the updated guide visibly includes Refresh readiness (`ten-docs-runtime.json`). No business actions were repeated during final verification.
