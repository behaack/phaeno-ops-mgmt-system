# Laboratory UAT closeout

## September 14 targeted case closure

[Current execution](2026-09-14-acceptance-closure.md) closes FIN-02, FIN-04 and FIN-05 for isolated software acceptance, using current signed-in/API/database evidence plus explicit unchanged Finance evidence carry-forward. The controlling ledger now has 3 Pass, 34 primarily remote and 44 gated cases. Current API build/health/schema/settings are recorded; scanner daemon remains unavailable. Original Finance receipts/history and Lab work are preserved; a separate reconciliation and clearly marked paging fixtures are retained. The initial audit below is historical. Next work is the supported Job completion path and named remote gaps; no broad regression rerun is implied.


Status: **Not closed — acceptance prerequisites and unperformed variants remain.**

## September 14 closure reconciliation — stop broad regression batches

The 81-case closure reconciliation is now the controlling next-work checklist. No whole case has complete release-scoped evidence for Pass: 37 have primarily remote remaining work and 44 have named prerequisite gates. These scheduling groups are not failures or completion percentages. FIN-02/04/05, LAB-14 and WEB-06 are closest to bounded closure; FIN-03 still lacks original issued-PDF preservation evidence. All prior partial results remain valid within their recorded scope. Next work must close named script steps or identify a concrete defect/blocker, not accumulate another broad automated count. No tests or operational writes were run for this reconciliation.

## September 14 file safeguards, shared controls and quote recovery slice

**54 backend and 100 distinct frontend checks passed; zero failures. One backend check was skipped on Windows.** Backend selection covered FileScanningTests, FileStorageTests, FileServicesVerificationTests, OperationalFileDownloadTests, API response/envelope tests, controller route materialization, module boundaries and Portal metadata. The UnixSymlinkFact deliberately skips LinkedAreaCannotReadWriteOrDeleteAnotherDirectory on Windows because symbolic-link creation requires a separately enabled host privilege. That Unix-specific boundary remains unverified in this run; no host privilege was changed.

File coverage includes exact scanner stream bytes and complete verdicts, unavailable/disabled scanner rejection, cancelled requests, storage round trips, unsafe keys and roots, partial-write cleanup, verification readback corruption and cleanup failure, and immutable full-response completion evidence. Scanner tests use ephemeral loopback protocol fixtures, not the live ClamAV engine. Storage tests use their own temporary roots, not saved UAT files. This does not repeat earlier real-scanner acceptance or prove Customer byte delivery.

The first frontend group passed 52 checks across receipt upload/download and API clients, navigation/branding, sidebar, required fields, searchable/multiple selections, action menus and dialogs. The additional group passed 48 distinct checks across journal exports, quote PDF recovery, quote extension, resumable drafts, configuration views and quote issuing/reissuing. It also repeated the five shared dialog checks after adding the missing description to a synthetic test dialog; that accessibility warning is resolved and scoped lint passed. Only that test fixture changed; no product code or permissions changed.

Quote cases cover the displayed issued PDF revision, persistent download errors and retry, expiration/extension decisions, failed request retry keys, dirty-entry dismissal guards, reviewed versions and quote prices, and pending-save interaction guards. Journal export retains its selected date range without posting entries. Two non-failing test-environment warnings remain: an asynchronous React update in the optional-line quote test and JSDOM scrollTo in configuration views. They do not establish browser geometry or scroll acceptance. No new browser suite was run in this slice; earlier browser results remain separate.

Evidence: ignored tmp/uat-resume-20260914-results/files-boundaries-20260914.trx (54 passed, one skipped), frontend 52/52 initial and 53/53 follow-up including five repeat dialog checks. Saved financial and laboratory workflows were not operated. Real Unix link isolation, provider/scientific lineage, Customer downloads, physical validation and final acceptance remain open; the 81-case UAT pack is not closed.

## September 14 Finance rules and Web Operations recovery slice

**62 backend, 68 distinct frontend and 10 desktop/mobile browser checks passed, with zero failures or skips in the selected checks.** Backend selection: WebsiteApiTests, WebsiteCrawlerTests, WebsiteDocumentTextExtractorTests, WebsiteNotificationQueueMonitorTests, PaymentImportBatchDomainTests, PSeqOrderToCashDomainTests, QuotePdfRendererTests and UnconfiguredQuickBooksGatewayTests. This covers Website parsing/search/document extraction and queue monitoring, import/domain rules, decimal invoice adjustments, partial receipt allocations, reconciliation independence/history, and quote PDF layout/retained totals. Quote rendering tests are not legitimate end-to-end invoice issuance acceptance.

Frontend selection: dashboard tests and pseq-order-to-cash API tests (22), plus FinanceCorrections, FinanceCloseoutReport, LabInvoiceCapability and PSeqOrderToCashPanels (46). Covers independent Finance capability gates, corrections with reviewed versions, same-Customer invoice search/paging, failed allocation and billing drafts, closeout evidence and Web Operations filtering/recovery. A passing dashboard keyboard test emitted a React act warning because it directly focused a tab outside the test update boundary. Wrapped that focus in act; all seven tests in the file then passed without the warning, and scoped ESLint passed. This is the only test-source edit; no product behavior changed.

Browser home and web-ops-recovery suites passed unchanged on Chromium desktop and mobile. Checks exercise internal/external branding and navigation, single mounted Web Operations panels and keyboard tab movement, exact-recipient review with stale-version refresh before retry, and pause/resume preserving queued work and the reviewed reason. Existing accessibility, narrow-layout, dark/reduced-motion and error assertions passed where included. All email recovery and queue-control requests were intercepted in the isolated mocked browser server; no real message was sent or live worker paused.

Evidence: ignored tmp/uat-resume-20260914-results/finance-web-operations-20260914.trx (62/62) and tmp/uat-resume-20260914-browser-web-home. Cleanup confirmed zero listeners on temporary browser port 3019; git diff --check passed. No financial fixtures were seeded or changed; the saved Finance walkthrough was not replayed. The PostgreSQL notification-processing fixture was not selected. Real mailbox delivery, invoice issuance, provider/scientific lineage, signed-in Customer downloads, physical validation and final acceptance remain open. The 81-case pack is not closed by this supporting software evidence.

## September 14 CRM, people and account access slice

**94 backend, 104 distinct frontend and 12 distinct desktop/mobile browser checks passed, zero remaining failures or skips in this selection.** Backend covered account audit/authorization/domain, session/external identity, Clerk provisioning and verified-email resolution, invitation message construction, department access, CRM attention/company/outreach/requested relationships and relationship-management rules. The invitation sender uses a stub HTTP handler; this is not mailbox delivery or a real Clerk acceptance journey.

Frontend covered 55 CRM checks and 49 organization/invitation checks: request completion recovery, current reviewed versions and draft retention, independent failed collection recovery, association and merge target searches, contact outreach evidence, lifecycle confirmation, entitlement source restrictions, invitation identity/session transitions and department intent. The CRM group initially passed 54/55. Its remaining test expected editable recipient fields and an old Role dropdown. It now verifies the fixed selected-person identity, absence of editable text fields, current administrator radio action and unchanged contact/email/department/admin payload. All seven checks in the affected file passed on retest.

Browser suites crm, crm-company-recovery and people-departments initially passed 6/12; all six failed desktop/mobile variants passed on focused retest. Repairs are limited to tests: retain section=requests after Company creation; verify the implemented disabled Send invitation gate with visible missing-department guidance and no request; provide the recovery fixture an explicit synthetic Phaeno administrator session so the permission-scoped People query actually runs. No production permission guard was changed. The previously recorded difference between this disabled-submit behavior and the generic submit-validation guideline remains open; this is current-behavior verification, not acceptance of a product exception.

Browser coverage includes Company creation without Portal-access writes, Lead detail navigation, approved Won Opportunity handoff to a locked Customer order, People/Sales separation, department conflict recovery and lifecycle confirmation, and three independently failed collection reads recovered with keyboard retry. Existing accessibility, focus, narrow-layout, dark/reduced-motion and no-duplicate-write assertions passed where included in these suites. APIs are intercepted in a separate mocked browser server; no real Company, membership, invitation, opportunity or department was changed.

Evidence: ignored tmp/uat-resume-20260914-results/crm-account-group-20260914.trx (94/94), tmp/uat-resume-20260914-browser-crm-people and -retest browser artifacts. Four test/fixture files changed; scoped ESLint, frontend TypeScript and git diff --check passed. Cleanup confirmed zero listeners on temporary browser port 3019. Real provider/scientific lineage, signed-in Customer downloads, physical work and final acceptance remain open; the 81-case UAT pack is not closed.

## September 14 documentation, access and provisioning slice

**34 backend, 12 frontend and 22 distinct desktop/mobile browser checks passed; no remaining failures or skips in this selection.** Backend covered DocumentationSearchTests, DataProvisioningDomainTests and DataProvisioningProfileTests. Frontend covered documentation search/registry, the provisioning page, curated catalog dialogs and source-sample draft discard. Browser coverage comprised documentation, documentation-search, customers and data-provisioning suites on Chromium desktop and mobile.

The browser cases exercise Customer/Partner/Prospect/Phaeno guide visibility and cross-audience denial, rendered Partner assembly guidance, topic navigation, focused search and Back restoration, section links, stale-corpus recovery, short-query suppression, outage versus empty-result feedback, CRM Portal-access review and legacy Company routing, and Phaeno/Prospect data workspace visibility. Source-sample discard still requires a reason and sends the expected version before scoped navigation.

Initial frontend result was 11/12 and browser result 16/22. Three test files were updated for current behavior: the direct Discard draft button; Company links retaining section=requests, Add existing person and the current empty request view; and the Prepare included Assembly navigation label with its current included-case guide headings. Focused retests passed the component case and all six failed browser variants (two legacy-link cases in the intermediate retest, four remaining cases in the final retest). No product or authorization behavior changed. Scoped ESLint and git diff --check passed. Cleanup confirmed zero listeners on temporary browser port 3019.

Evidence: ignored tmp/uat-resume-20260914-results/documentation-provisioning-20260914.trx (34/34); browser artifacts in tmp/uat-resume-20260914-browser-access-docs, -retest and -final. These browser suites use the separate mocked 3019 server and intercepted APIs; access approval and provisioning views do not establish real account provisioning, scientific content readiness or Customer file delivery. No real invitations, operational records or role changes were made. Full provider/scientific, signed-in Customer, physical and final acceptance gates remain open.

## September 14 commercial, Trial and department browser slice

**40 distinct desktop/mobile browser checks passed, zero remaining failures or skips.** The commercial/Trial group initially passed 22/28; six focused retests passed after updating two outdated text selectors (the renamed mock-mode notice and source-specific Add sample to Yeast action). The department/history/dialog group initially passed 10/12; both invitation retests passed after aligning the old submit-and-focus expectation with the currently implemented no-department gate. These changes affect only three existing browser specs; no product, authorization or data model change.

Covered Customer/Partner configured price commitment before samples; reuse of a purchased Assembly case through interrupted upload; staff timing/case review; mock Customer/Partner/Phaeno workspace navigation; Trial scope/material terms, Prospect acceptance, sample-submission conflict recovery, renewed acceptance after changed scope/failed refresh, superseded/closed result history, exact Company request handoff and floating-choice Escape handling. Department cases verify scope-limited administration, organization-wide discovery, history clearing on context/role change, defaults conflict review, explicit invitation department/admin intent and unsent-draft recovery. Shared dialogs retain click targets after blur validation and keyboard focus. Existing accessibility/reflow and selected dark/reduced-motion assertions passed.

Invitation guideline discrepancy retained for follow-up: OrganizationInvitationDialog and its current component tests disable Send invitation and immediately display the no-department explanation after all departments are deselected. The general UI/UX guideline says validation errors alone should not disable submission. The browser test now verifies current behavior and zero requests before selecting access, then verifies the reviewed invitation payload. This does not resolve the guideline discrepancy or certify it as a new approved exception; no access rule was weakened.

Evidence is in ignored `tmp/uat-resume-20260914-browser-commercial`, `tmp/uat-resume-20260914-browser-commercial-retest`, `tmp/uat-resume-20260914-browser-departments` and `tmp/uat-resume-20260914-browser-invitation-retest`. Existing mock sessions and intercepted APIs ran on separate loopback 3019; no actual invitations, payments, uploads or permission writes. A retest launcher used the repository root once and exited before running; retry from the frontend package completed. Scoped ESLint and whitespace checks passed. Temporary server stopped. Saved baseline remains 22 work orders, one scientific approval, nine trays/version sum 68; ScientificReviewer active and temporary Operator inactive. These browser fixtures do not replace signed-in Customer, provider, physical or final acceptance.

## September 14 desktop and mobile browser workflow slice

**46 distinct browser checks passed; two intentional mobile label-print cases skipped.** These existing Playwright suites use real React components with deterministic sessions/API responses on a separate loopback server at 3019. They do not use the reviewer session or advance retained UAT records.

| Group | Result | Coverage |
| --- | --- | --- |
| Preparation and protocol execution | 28 passed (40.9 seconds) | Shared/per-tube evidence, failed-tube exclusion, output identity, uncertain-save same-command retry, definite-conflict new-command retry, material/equipment eligibility, unfinished step preservation, Supervisor-only steps, keyboard focus/cancellation, typed QC hold/correction/skip/completion. Desktop and mobile. |
| Release/retention and shipping insert | 10 passed, 2 intentionally skipped | Frozen dates, grace/cutoff actions, retained manifests/receipts, scan-target separation, print styling and laboratory/stock-kit label identity. Mobile laboratory/stock-kit print tests are explicitly excluded; desktop counterparts pass. |
| Transportation inventory retest | 8 passed (14.7 seconds) | Location receipt independent of cancelled originating Job, exact container claim through conflicts/failed refresh, Member read-only history, wrong-container scan value preservation. Desktop and mobile. |

Initial transportation run failed all eight cases because its fixture HTML reached the application router as Not Found. Corrected only the test harness to serve that exact fixture document, retaining the loopback host restriction and API interception. An intermediate retest exposed its missing React refresh preamble and was stopped; added the same initialization used by the other fixture pages. The clean final eight-case run passed. No product behavior or authorization change. Scoped ESLint passed for the changed spec. The passing suites include their existing automated accessibility/reflow checks and selected mobile dark/reduced-motion variants; this is not complete WCAG or signed-in acceptance.

Artifacts remain under ignored `tmp/uat-resume-20260914-browser`, `tmp/uat-resume-20260914-browser-shipping-release` and `tmp/uat-resume-20260914-browser-inventory-final`. They include screenshots and browser-generated print PDFs, not physical printer/scanner evidence. The original failed/interrupted inventory evidence is retained separately. The temporary 3019 server stopped. Readback remains 22 work orders, one scientific approval, nine trays/version sum 68; ScientificReviewer is the only active lab role and temporary Operator remains inactive. The reviewer workspace was not operated in this slice. Full scientific/provider lineage, real Customer downloads and physical/final acceptance remain open.

## September 14 release, download and retention grouped continuation

**56 backend and 31 frontend checks passed, zero failures or skips.** The first backend group passed 44 checks, including six governed-result PostgreSQL journeys and domain/decision/download checks. All 31 frontend checks passed across seven file-management, result-package, release and retention files. An additional 12 managed-release checks passed, including ten disposable-database journeys, in 2 minutes 34 seconds. This is software and database evidence, not completed Customer/provider UAT.

Covered frozen organization policy at release; full response completion versus partial/cancelled/failed responses; missing/late evidence and grace; cutoff denial before storage; file and ZIP scope; actual database commit-time admission/completion boundaries; independent active-ZIP revocation; concurrent warning/grace polling without duplicate notices; current-admin notification recovery and worker activation isolation; preservation/lease holds; retry after simulated partial deletion; shared-object preservation; quarantine and receipt authorization; same-workflow reissue links with original dates retained. Frontend cases cover member/staff action separation, policy inheritance, retention dates, package context and stale-version recovery.

The only source change is the managed-retention test harness allowing the existing `phaeno_ops_lab06_uat` source name alongside `phaeno_ops`, still requiring loopback. It creates/migrates only generated `pseq_retention_test_<GUID>` databases and drops those exact databases in finally. Existing commit-timestamp tracking was already on; no server setting changed. File storage/deletion and notification providers are test doubles. Real database transaction timing and independent connection revocation were exercised, but no real retained file deletion or external message delivery occurred. Synthetic release setup does not supply missing scientific lineage for saved UAT packages.

Evidence: ignored `tmp/uat-resume-20260914-results/release-download-group-20260914.trx` (44/44) and `managed-release-group-20260914.trx` (12/12). Frontend used the installed Vitest entrypoint for `src/features/file-management` and the four result-release/package/retention test files. Cleanup found no generated retention database. Saved baseline remains 22 work orders, one scientific approval, nine trays with version sum 68; retained packages remain versions 3/2/3, unreleased. ScientificReviewer is still the only active lab role, temporary Operator inactive. No application behavior, shared migration, Git operation or deployment. Provider transfer, Customer browser download acceptance, physical validation and final signoff remain open.

## September 14 shipping, packing and accession grouped continuation

This slice finishes with **72 distinct backend checks and 107 frontend checks passing**, including four new return-kit feedback regressions. Backend initially passed 69/72; frontend initially passed 104/107. The three backend failures were test assumptions: global queue counts included retained UAT records, rollback verification assumed no pre-existing notifications, and tube accession expected the older null specimen acceptance timestamp. Queue checks now count the generated organization's records (still proving all 261 are returned), rollback compares the notification count before/after, and intake asserts recorded acceptance while the fixture without quoted turnaround still has no completion estimate. All three focused retests pass. Three frontend failures were stale assertions for the explicit tube-policy confirmation argument and the current directly visible deactivation button; both affected files then passed all 29 checks. No backend product rule changed.

The backend selection includes all partial `SampleShippingPostgresTests` plus `SampleShippingDomainTests` and `SampleShippingContainerTests`: container compatibility/capacity, packing/reset, kit stock and request dispatch/receipt, tenant guards, immutable packet revisions, registered-tube crosswalk, receipt/accession and replay. Fixtures use generated identities on the owned loopback 5436 database with scoped cleanup. Simulated notification senders and controller-recorded physical actions are not actual delivery or bench evidence. TRX files: ignored `tmp/uat-resume-20260914-results/shipping-group-20260914.trx` and `shipping-retest-20260914.trx`.

Frontend group covers stock kits, kit requests, shipping configuration, Job shipping/sample/tube views, source capacity and return-kit feedback (12 files). TypeScript and scoped ESLint pass. UAT-20260914-01 is **fixed and retested locally**: ReturnKitFulfillmentPanel only shows an empty queue after successful loading, and does not hide a failed query as an optional empty panel. Four tests cover failure with both panel visibility modes, pending loading and successful empty results. Live reviewer at 3016 shows the assigned-role error without the false empty message. At 390px the error text remains readable; Right/Left arrows move between receipt tabs and update the route. Viewport reset. No signed-in operational writes or permission changes.

Readback remains 22 work orders, seven protocols, three workflows, one scientific approval, nine preparation batches and version sum 68. The three retained packages remain versions 3/2/3 and unreleased; ScientificReviewer is the only active lab role, temporary Operator inactive. Reviewed the Phaeno receipt/accession guide: the existing sequence remains correct; this feedback correction adds no new operational instructions. Shared migrations, production release, actual provider/file transfer, Customer downloads, physical acceptance and final UAT remain unperformed.

## September 14 grouped handoff, shipping and Trial verification

Second larger slice on current source: **66 distinct backend and 162 frontend checks pass across the initial groups and focused retests**. These are software checks, not fully accepted UAT cases. The running reviewer UI/API retain the earlier documented build distinction.

| Run | Result | Scope |
| --- | --- | --- |
| Commercial handoff and Trial backend group | Initially 62 passed, 4 failed, 0 skipped (66 total) | Authorization, configured offerings, roster/capacity, cancellation, Trial approval/submission/hold/release/retention and operator journey. |
| Trial release/retention retest | 3 passed, 0 failed/skipped | Partial/whole release and conversion; current laboratory readiness; warning/grace/hold/cleanup/reissue. |
| Updated operator journey | 1 passed, 0 failed/skipped | Generated authorized order through receipt/accession, source selection, confirmed execution start, library, evidence, QC, batching, simulated custody/delivery and independent scientific approval. Persisted ReadyForRelease, two containers, no Customer release. |
| Shipping and Trial frontend group | Initially 155 passed, 7 failed (162 total, 16 files) | All 143 shipping checks passed; seven Trial checks used obsolete required-label selectors. |
| Trial frontend retest | 19 passed, 0 failed (4 files) | Selectors tolerate current required-marker spacing. Existing SearchableSelect React act warning remains; no failing assertion. |

Test-only corrections: Trial ready-package fixtures explicitly arrange the successful specimen outcome required by release readiness. This tests release/retention rules, not laboratory execution. The operator journey instead uses the actual source-selection command and generated execution, confirms its source barcode at start, then creates derived material. Its former outer rollback transaction cannot contain commands that own their transactions. It now creates/migrates a generated `pseq_handoff_test_<GUID>` database on loopback and drops that exact database in finally. Common-fixture default cleanup remains unchanged. No product guard, application behavior, dependency or shared schema changed.

Backend evidence is in ignored `tmp/uat-resume-20260914-results/handoff-trial-20260914.trx`, `trial-release-retest-20260914.trx` and `operator-journey-retest-20260914.trx`. Initial filter selected `LabOperationsCommercialHandoffPostgresTests`, `TrialProjectPostgresTests` and Trial non-Postgres classes. Frontend used the installed Vitest Node entrypoint for `src/features/sample-shipping src/features/trials`, then retested `src/features/trials`.

Live Independent Reviewer inspection covered Receive shipments, Kits sent and Accession samples. Receipt explains Operator/Supervisor access and withholds receipt controls. Accession has an empty queue and distinguishes opening an insert from recording arrival; no lookup or operational write was submitted. Kits sent denies access but also renders empty-queue text, as recorded below. This does not establish populated shipping or physical scanner/printer acceptance.

Cleanup readback: 22 work orders, seven protocols, three workflows, one scientific approval and nine preparation batches. All nine IDs/statuses/versions match the baseline, version sum 68. No generated handoff or Trial-preparation database remains. ScientificReviewer remains the only active lab role; temporary Operator access stays inactive. Saved UAT records were not advanced or published. Actual provider transfer, Customer publication/download, physical work and final acceptance remain open under Closure gates.

### UAT-20260914-01 — Reviewer kit access error also shows empty queue

Update: **Fixed and retested locally** in the shipping/packing/accession continuation above; original observation follows for history.

Severity: Low. Status: **Open, observed locally**. As Independent Reviewer at local 3016, select Receipt & accession then Kits sent. The settled page reports “Return kits could not be updated” with an assigned-role explanation, and also says no shared shipments are ready. A denied request cannot establish an empty queue. Expected: role restriction without implying a successful zero-result load. Authorization prevented access; this is a feedback issue. No product fix or new regression assertion was added in this testing slice.

## September 14 grouped software verification and saved-lineage trace

At the owner's request for larger slices, ran a grouped laboratory software checkpoint against current checkout `25bcfbf3db8b9d0807f574f447d4d238b07cca64` plus the existing working-tree tests. This combines supporting automated tests with signed-in record inspection; it does not convert those checks into full manual-case acceptance. The retained UI 3016/API 7116 build identity remains as documented below. Test compilation used `tmp/uat-resume-20260914-build`, separately from that running API.

| Run | Result | Scope |
| --- | --- | --- |
| Broad laboratory backend selection | Initially 157 passed / 1 failed / 0 skipped, 11 seconds | Laboratory-related domain, contract, authorization, workflow, intake, attempt, preparation and progress checks; eight explicit PostgreSQL journeys. |
| Persistence retest after correcting the stale assertion | 11 passed / 0 failed / 0 skipped, 2 seconds | All `PersistenceTests`, including the formerly failing mapping assertion and the database ERD completeness check. |
| Laboratory frontend selection | 48 passed across 15 files, 30.38 seconds | Scanner behavior, receipt/accession, protocol definitions/approval, typed QC, material date handling, print-confirmation semantics and preparation access. |

There are **168 distinct passing backend checks across these runs, plus 48 frontend checks**. These are automated checks, not 216 UAT cases. The only initial failure was a stale test: `PSeqOperationsDbContextMapsCompleteLaboratoryModelWithoutCommercialForeignKeys` expected 30 laboratory entities, but the current model contains 36. Updated the count and added explicit table assertions for specimen attempts, attempt receipts, four preparation entities and role-invitation intents. Retained the lab-schema and no-Commercial-foreign-key checks; all 11 persistence tests pass. No application model, migration or runtime behavior changed.

The eight PostgreSQL journeys passed: authorization atomicity/idempotency/conflicting reuse; safe amendments and received-specimen guards; full versus partial cancellation; organization-scoped projection lookup; replay-safe monotonic projection without publication; independent scientific approval gates without publication; destroyed-tube/no-storage plus atomic batch acceptance; and forced concurrent preparation-name collisions with stable retry identities. Their generated fixtures and scoped cleanup were inspected before execution. Only `127.0.0.1:5436/phaeno_ops_lab06_uat` was configured; no migration was applied. Test-only provider/projection calls are not evidence of an external pipeline or real custody events.

Backend command selected `(FullyQualifiedName~Lab&FullyQualifiedName!~Postgres)` plus `LabOperationsProviderPostgresTests` and the three explicit scientific-review, batch-accession and concurrent-preparation methods. Evidence is retained in ignored `tmp/uat-resume-20260914-results/lab-broad-20260914.trx` and `persistence-retest-20260914.trx`. Frontend command was `node node_modules/vitest/vitest.mjs run src/features/lab-operations --reporter=default`; the initial `pnpm exec vitest` launcher failed to resolve its executable, then the installed Node entrypoint ran successfully. No dependency install or new browser-fixture suite was needed.

Signed-in Independent Reviewer traced the preserved resource tray through specimen `578c75e7-30b6-4001-b4cd-5057e7e464ff`, completed execution `5b3255a8-0e49-4946-af80-a81a3b0327e0`, library container `7a0e6d94-5609-4462-97ce-79262d673a08` / `PH-L-ZC3W65F9DT-9`, and its source `3a06e41f-b6e1-4e8e-8937-428605331350`. The specimen retains cancelled attempt 1 with its reason and successful attempt 2 pinned to workflow v2. Execution is explicitly locked, links back to its preparation tray, and retains named evidence, authors, one 1 mL material use and three equipment uses. Output and source pages link to each other and the same specimen, retain their recorded storage, and withhold operating controls. The source explains that processing history prevents rewriting intake. No write, print confirmation, custody action or scientific approval was submitted.

Before/after database readback matches for all nine saved preparation IDs, statuses, versions and history counts. Counts remain 22 work orders, seven protocols, three workflows and one scientific approval; no generated concurrency tray/workflow remains. The resource tray stays Complete/version 18/history 18. No new lab role was granted. Earlier staged work remains untouched; this slice changes only the mapping test and verification documents. Full frontend/backend suites, external transfer/provider processing, Customer publication/download, physical validation and final acceptance were not claimed. Continue with unperformed signed-in write variants or the approved integration setup when available; do not recreate completed trays or publish synthetic packages.

## September 14 reviewer configuration and operational-control boundaries

Continued in the existing Independent Reviewer session at 3016, with ScientificReviewer as its sole active lab role. Verified these live UI boundaries without submitting writes:

| Surface | Observed reviewer behavior |
| --- | --- |
| Receive shipments | Operator/Supervisor requirement is explicit; shipment-receipt scan controls are absent. |
| Lab configurations / Protocols | Existing approved protocols are readable; creation, revision and approval controls are absent. |
| Workflows | Historical Retired/Invalidated/Invalid and current Production states are readable; authoring/promotion controls are absent. |
| Tray formats | Original Active 2 × 3 format with five usable positions is readable; create/edit/retire controls are absent. Right Arrow from Workflows selects Tray formats and updates the route. |
| Materials | Quantities, QC results/reason, storage and dates are readable; new-lot and QC write controls are absent. |
| Equipment | Calibration/status and Show retired filter are visible; create/retire controls are absent. |
| Sequencing batches | Status filter and saved batches remain visible; scanner, new batch, Start, sendout and custody write controls are absent. Designated LAB-14 batch remains Draft with two libraries; TEST-008 remains Draft with one. |

Direct new-version routes were also exercised using existing database identities and the application's route definitions: protocol `0167462e-b39d-4c62-8c53-3b945fd539a7` displays **Protocol authoring unavailable**, and workflow `fab61845-c3f4-4a5a-9b8d-f196943241b2` displays **Workflow authoring unavailable**. Both explain that an active Protocol Administrator role is required and expose no authoring form. The desktop Lab ops link returns from the denied builder to laboratory operations.

Database readback confirms the protocol still has only Active v1 and the workflow only Retired v1/Production v2; no new revision was created by navigation. The temporary Operator assignment from response-recovery testing remains inactive. This is signed-in UI visibility/direct-route denial evidence, not direct mutation-endpoint authorization or the full staff/Customer matrix. No record, role, product code, runtime setting, Git state or deployment was changed by this pass. Earlier staged work was left untouched; this continuation adds documentation only.

Requested the location of approved non-production pipeline/transfer configuration if it is now available, without requesting credentials. No configuration location had been supplied during these checks. The integration gate remains open; do not replace the intentionally unused endpoint with synthetic successful provider events to claim end-to-end completion. The reviewer tab is retained on laboratory operations.

## September 14 scientific-review dialog on narrow and short screens

Continued signed-in LAB-06 negative-review UI acceptance on existing `TEST-LAB06-GATE-UNCLEAN` (`846b4e7e-fdeb-4df3-a3b1-1bc850967256`) at 3016 as Independent Reviewer. No role change or approval submission.

- At 320 × 740 in System/light theme, Approval initially focused the package selector. The failed package stayed excluded, the clean-package prerequisite was explicit, and Save remained disabled. A long TEST ONLY cancellation summary was entered by keyboard and retained while resizing. Tab moved from the summary to Cancel, skipping disabled Save.
- The 288 px-wide dialog stayed between x=16 and x=304; top/bottom were 26.5/713.5 px. Document scroll/client width was 320/320 px. At 320 × 568, the dialog stayed between y=16 and y=552 with its body scrolling internally. Screenshot inspection confirmed the header/close action and footer/Cancel remained accessible, with the required legend in the footer and required marker beside the package label.
- Shift+Tab from Close reached Cancel; Enter cancelled and restored focus to Record scientific approval. Reopening showed an empty summary. Forward Tab cycled selector → summary → Cancel → Close → selector; reverse Tab from selector reached Close. Escape again dismissed and restored opener focus.
- At 390 × 668 in dark theme, the dialog stayed inside x=16..374/y=16..652, document scroll/client width was 390/390, and Save remained disabled. Screenshot inspection showed readable explanatory text, full-width fields and visible selector focus. Closed without submission.
- Keyboard End on the laboratory tab list selected Review and wrote `tab=review`; refresh retained Review and “No scientific approval recorded.” Restored System theme and default viewport. Database readback retained ScientificReview/version 1, zero approvals and zero work events. Reviewer is still the only active laboratory role; the earlier temporary Operator assignment remains inactive.

This adds live narrow-screen form, focus containment, cancellation/reset and route-persistence evidence. It does not establish scientific approval success, direct API rejection, exact contrast ratios, live reduced-motion support or full LAB-06 acceptance. No application change or automated suite was needed. The reviewer session is retained on this work's Review tab.

## September 14 completed-preparation keyboard and reflow continuation

Continued LAB-14 step 21 on retained completed resource batch `6218afc4-d4c2-4437-a6ce-7e1bd46313c2` (`PSeq-20260912-160957`) at UI 3016 as the existing Independent Reviewer. No operating role was added and no laboratory write was submitted.

- At 390 × 844 in the initial System/light theme, document scroll width equalled client width (375 px); the collapsed workspace had no page-wide horizontal overflow. Mobile navigation appeared in the user menu, with no duplicated desktop navigation.
- Keyboard Tab reached the tray region, tube disclosure and specimen/output links. Enter expanded the tube and its three effective-evidence entries. The output remained identity-confirmed, 20 uL, Batched, with reused preparation QC and its existing sequencing assignment. Menu Escape restored focus to its opener.
- At 320 × 740 in dark theme, expanded tube evidence and all 18 history entries retained document scroll/client width of 305 px. The tray alone needed horizontal scrolling (280 px content in a 241 px region). ArrowRight on the focused tray moved its scroll offset to 23 px while document width stayed unchanged; ArrowLeft restored direction toward the starting position. The focused evidence disclosure had a visible outline in the screenshot. Labels, retained reasons and traceability links wrapped in their cards.
- The same expanded workspace in light theme had equal document scroll/client width (320 px at measurement), with readable text and visible menu focus. At 1440 × 900 in light theme, scroll/client width was 1425 px and primary navigation returned to the desktop toolbar; the desktop user menu omitted the mobile Workspace links.
- Restored the original System theme and removed the viewport override. Database readback remains Complete/version 18 with 18 history records. ScientificReviewer is the only active lab role; the prior temporary Operator assignment remains inactive.

This is bounded signed-in read-only keyboard/reflow and visual theme evidence, not a full accessibility certification or the writable-form/nested-dialog matrix. Reduced motion was false in the current browser and was not changed; its live variant remains Not run. No exact contrast-ratio audit, physical hardware test, automated suite or application-code change was performed. A browser locator initially treated native history disclosure as a button; inspecting its DOM and using the native disclosure text resolved that test-tool mismatch, with no product defect observed. The retained reviewer tab remains on this completed workspace for continuation.

## September 14 saved-response recovery — passed with temporary access removed

Prepared one new empty tray, **TEST ONLY - response recovery - 20260914**, ID `20d513e8-bd5d-4707-9e9f-001d9cbfbdb5`, in the retained isolated `phaeno_ops_lab06_uat` database. Its starting state was Draft/version 1, zero members and zero command records, reusing the existing Active tray format and Production test workflow. Domain-model fixture setup uses a null human actor and the explicit `TEST-ONLY-response-recovery-fixture-20260914` audit request. No existing sample, tray, specimen, output, scientific package or approval was changed. This fixture represents no bench work.

Prepared a separate UI at `https://localhost:3018` and loopback-only proxy at `127.0.0.1:7119`, forwarding to the existing isolated API 7116. The proxy permits reads and only the exact cancellation of this empty tray with reason `TEST ONLY response-loss recovery; empty tray, no laboratory work`, plus an identical retry. It forwards the real write, records its safe outcome, then drops only the first successful response. Any other business write through this test proxy is rejected. Existing UI 3016/API 7116 configuration is unchanged. Helpers/config/logs are in ignored `tmp/response-loss-uat`; the Vite configuration is in ignored `frontend/node_modules/.cache/response-loss-vite.config.ts`.

Setup verification: helper build/setup completed, proxy syntax check passed and proxied API health returned 200. Before the temporary grant, the signed-in Reviewer saw the named empty Draft tray with read-only guidance and no operating controls. Initial folder/build permissions required successful sandbox escalation; initial navigation preceded Vite readiness and was retried after startup. These were setup issues, not product defects.

The owner explicitly approved the bounded temporary Operator grant to the existing Independent Reviewer (`a1b30a3a-9939-4b92-82fc-6ecc5390f50d`) in this isolated database, the empty-tray save/retry, and immediate removal. Assignment `aa071933-9b1f-43fc-90b8-7dd09e542cbf` was added, then deactivated immediately after the retry succeeded. The grant enabled Operator actions throughout the isolated database; the proxy confined submitted writes to the named fixture. No provider identity, production/shared role, authentication policy or existing approval changed.

Signed-in observed results:

- Refresh exposed Operator controls on the empty tray; Start preparation was disabled. Opened Cancel draft batch, entered the exact test-only reason and submitted once.
- The real API saved Cancelled/version 2 with HTTP 200/success true. The proxy then discarded that successful response. The dialog stayed open, preserved the exact reason and focused field, and displayed: “The action could not be saved. The latest batch is shown; review before saving again.” This is the existing generic error wording; the proxy evidence establishes that persistence had actually succeeded.
- Retried once without changing the form. Both requests used request ID `1159bbbd-e5da-42ff-84ea-7f9de6c15faa`, version 1 and identical action/reason/confirmation payloads. The retry returned HTTP 200/success true with the same batch ID, Cancelled/version 2. The dialog closed and Batch history showed exactly one cancellation with the retained reason.
- Independent PostgreSQL readback confirmed Cancelled/version 2, zero members and one command record. The completed resource tray remains Complete/version 18 with 18 history records; the preserved reservation-release draft remains Draft/version 2 with one member and two records.
- After deactivating the exact temporary assignment, database readback showed ScientificReviewer as the only active laboratory role. Navigating to the preserved Draft displayed explicit reviewer-only guidance and no operating controls. The temporary 3018 UI and 7119 proxy were stopped, their listeners were absent, and the test tab was closed. The original 3016 reviewer session is retained.

Evidence helpers and safe command outcomes remain in ignored `tmp/response-loss-uat`, including `proxy-evidence.json` and the inactive assignment identity in `role.json`. This is a real signed-in browser/API/database lost-response retry on an empty cancellation, using the retained historical API build described below and the current frontend. It does not cover populated preparation saves, uncertain creation, distinct-user races, physical execution or every network-failure variant. No product source change, new automated test, full suite, deployment, Git mutation or shared migration in this pass. Full UAT remains open.

## September 14 Trial preparation guards and reviewer keyboard check

Continued testing without advancing the saved incomplete scientific fixtures. Added `TrialProjectPostgresTests.PreparationCommandsRespectTrialHoldClosureAndScopeCurrencyWithoutPartialWrites` for LAB-14 step 20. This exercises real preparation controllers and PostgreSQL in a new disposable local database, with generated CRM/Trial acceptance/submission records and a synthetic reserved tray. The test identity is generated only in that database; existing logins and roles are unchanged.

Seven commands (add, start, move, remove, cancel, output and step) are rejected for each of eight conditions: held Trial, Cancelled, ClosedIncomplete, Completed, Declined, Expired, outdated approved scope and outdated accepted scope. **All 56 checks pass** with 409 / `trial_work_unavailable`. After every rejection, complete tray/history readback, Trial scope/status/version, Job status/version, attempt state/version/times, tube quantity/state/version, library count and work-event count remain unchanged. With valid Trial status and matching scope restored on this generated fixture, moving the reserved member from A1 to A2 succeeds and creates exactly one command record. This control confirms the negative cases are blocked by Trial eligibility rather than a universally unusable tray.

The initial test arrangement failed before the guard because its rollback transaction conflicted with the controller's own transaction. Corrected the test harness by creating/migrating a randomly named `pseq_trial_preparation_test_*` database on loopback, allowing normal controller-owned transactions, then dropping that exact database in finally. Existing Trial tests retain their rollback transaction behavior. No product-code defect was found, and no migration was applied to the retained UAT or any shared database. Direct fixture status edits isolate guard behavior; they are not legitimate Trial completion or physical laboratory evidence.

Validation:

- New PostgreSQL guard test: **1 passed, 0 failed, 0 skipped**, 1 minute 32 seconds including disposable database setup.
- Related existing regressions: **4 passed, 0 failed, 0 skipped**, 11 seconds: CRM approval/acceptance/submission-to-Lab handoff, shared scientific Trial-hold guard, required-final preparation and optional-final/existing-output preparation.
- Current source compiled with no reported warnings/errors using `tmp/uat-resume-20260914-build`. No full application suite ran. Final database inspection found no remaining `pseq_trial_preparation_test_*` database and no generated `TEST ONLY preparation` workflow in retained UAT.

Also continued signed-in UI testing as the existing Independent Reviewer on `TEST-LAB06-GATE-UNCLEAN` (`846b4e7e-fdeb-4df3-a3b1-1bc850967256`). Unlike the ingestion Job, this saved negative fixture is already in ScientificReview. Its Approval dialog excludes the failed package, shows the explicit no complete/checksummed/malware-clean package message and disables Save. Initial focus is the package selector; Tab reaches the optional QC summary, then Cancel. Escape closes the dialog and returns focus to Record scientific approval. No approval request was submitted. This is reviewer-only UI prevention and bounded keyboard evidence, not a direct approval-endpoint rejection or full WCAG matrix.

Readback confirms the negative work remains ScientificReview/version 1, its package Failed/version 1, zero approvals and no release. The three preservation-table packages also retain their states, versions, original approval and null release timestamps. The reviewer session remains available. Updated backend/E2E plans and the owning Trial plan; whitespace verification passed. No production mutation, Git mutation, deployment or application behavior change.

Trial preparation guards now have explicit controller/database evidence. Signed-in lost-response retry and broader held/closed command variants, full scientific lineage, actual transfer/pipeline integration, Customer publication/download and physical/provider acceptance remain open; existing synthetic packages remain unsuitable for publication.

## September 14 reviewer sign-in and access continuation

The remote owner asked Codex to perform the reviewer sign-in. Recovered the existing test credential from its original setup conversation and completed normal sign-in plus the development test-account verification challenge in a separate Codex in-app browser. The visible account menu confirms **Independent Reviewer** / `independent.reviewer+clerk_test@example.com`. The existing Edge administrator session was not signed out or changed. No credential reset, new account, role grant or invitation was needed; no credential was copied into repository files.

This supersedes the pending sign-in request below: **the reviewer session is available**. Read-only database inspection confirms its sole active laboratory role is ScientificReviewer.

Signed-in observations:

- Both the failed package `d14354fb-36ef-4801-8184-adf6cde706e2` and clean package `3c42f211-a219-421d-a7bc-dd07c6dba5e7` direct detail routes show **Result package unavailable** with the explanation that result release or file-management access is required. No package detail or release control is exposed. This is the current package-page role boundary, not a scientific rejection test.
- The reviewer can open `TEST-LAB06-FILE-INGESTION`, work `ec79d627-3001-4a91-802e-d1a65cffd716`. It remains **Awaiting Specimens**, with zero specimens and zero open exceptions. Review shows **No scientific approval recorded**; no approval action appears. A clean file alone does not make this work eligible.
- Independent database readback retains clean package ReadyForReview/version 3, failed package Failed/version 3 and prior metadata-only approval ReadyForRelease/version 2 with its original approval ID. All release timestamps remain null.

No scientific or operational writes were submitted. These checks resolve the login prerequisite and establish reviewer access/eligibility presentation; they do **not** prove approval rejection of a failed package on an otherwise eligible work order. Further review acceptance requires an eligible designated scientific journey. Preserve the current packages and use the signed-in reviewer session; do not request another manual login unless that session expires. Full lineage, transfer and Customer publication gates remain open. No new automated tests were needed for this read-only continuation; whitespace verification passed.

## September 14 continuation — preparation command guards

Resumed from the saved checkpoint on checkout `25bcfbf3db8b9d0807f574f447d4d238b07cca64`. The retained LAB-06 database, API and UI were stopped. Restarted only the existing loopback PostgreSQL cluster at 5436 with commit tracking, the saved API launcher at 7116 and UI launcher at 3016. API health returned 200; the connected Order operations page loaded. The API still uses the historical Finance upload build named below, while the focused tests compiled current source into `tmp/uat-resume-20260914-build`. These are distinct build identities. The local scanner at 3316 is **not running**; this pass made no scan or upload claim and did not change scanner configuration.

The current Edge account is Bill Haack. Requested the existing Independent Reviewer sign-in at `https://localhost:3016`; no reviewer handoff was received during this pass. The new UAT tab is retained for that handoff. Existing owner tabs were not navigated.

Extended the two generated-fixture PostgreSQL preparation journeys in `backend/test/LabPreparationPostgresTests.cs`:

| Added check | Observed result |
| --- | --- |
| Customer attempts start, move, remove, cancel, output and step commands | Each rejected with 403 / `lab_capability_required`; the complete tray readback, including history, remains identical. |
| An already-reserved Job is OnHold, Cancelled or ReadyForRelease; attempt start, move, remove, cancel and output | All 15 combinations per journey rejected with `execution_work_unavailable`. Complete tray readback, Job version and attempt versions remain identical; no execution starts or library creation. |
| Continue the generated fixture after guard checks | Both existing positive journeys still pass, including required-final and optional-final/existing-output handling, replay, resource accounting, QC and sequencing membership rules. |

The new matrix ran in both workflows: 42 rejection checks within two existing tests. Status setup/reset uses direct database fixture arrangement **only on each test's newly generated Job**; it is not evidence of legitimate closure/resumption, signed-in Customer writes, Trial closure or physical execution. These tests call real controllers and PostgreSQL with supplied test identities, not a browser authentication flow.

Final focused run: **3 passed, 0 failed, 0 skipped**, including the existing independent scientific-approval/non-publication regression; duration 11 seconds after compilation. Current API/test build completed without reported warnings or errors. Command: `dotnet test backend/test/PSeq.Operations.Test.csproj --artifacts-path tmp/uat-resume-20260914-build --no-restore` with a filter for `PreparationMixedTrayEnforcesReservationsEvidenceFailureAndQcHandoff`, `PreparationOptionalFinalStageAndExistingOutputPreserveQcHandoff` and `ScientificApprovalEnforcesGatesAndIndependentApprovalDoesNotPublish`, and the reference connection fixed to the isolated 5436 database. No full suite ran.

Post-run readback confirms all three retained packages in the preservation table below have exactly their prior states, versions and approval references, with null release timestamps. No generated `TEST ONLY preparation` workflow remains. Existing scientific, shipping and Finance fixtures were not advanced. Updated the living backend test plan; documentation whitespace verification passed. No application behavior, role assignment, migration, Git mutation or deployment changed.

LAB-14 step 20 now has stronger controller/database coverage. Its signed-in write variants, Trial-specific guards, full keyboard/theme matrix and the scientific/provider/Customer-download closure gates remain open. Resume at the Independent Reviewer handoff; restore the existing real scanner before any later scan/upload acceptance.

## Saved pause — owner requested commit, push and API/UI redeployment

The owner paused acceptance here and authorized committing/pushing the accumulated application fixes and evidence, followed by production API and Portal UI redeployment. This release authorization did not close UAT or activate disabled workflows. Deployment evidence is recorded separately in [the historical UAT fixes release](../../operations/portal-uat-fixes-release-2026-09-12.md).

The release is complete on application source `26839b4c7739ca1e5f3934335c9f5a758cd6a55c`: API workflow `34736875788` succeeded and Portal production `dpl_3AgHRLYfVrm1NogVQYFv6abvY6gs` is Ready. Health, database ping, assets and bounded signed-in production checks passed. This supersedes prior local-only deployment statements for the included fixes, while all acceptance limitations below remain in effect.

Resume with the existing Independent Reviewer sign-in at https://localhost:3016. The last observed account was Bill Haack; reviewer sign-in has not been confirmed. Open the failed checksum package and clean ingestion package below to finish review visibility/eligibility checks, preserving their current versions and all prior approvals. Then work through the explicit closure gates; do not repeat completed fixture setup or treat a clean file as evidence of specimen execution.

The retained LAB-06 runtime uses UI 3016/API 7116 and the isolated `phaeno_ops_lab06_uat` database on loopback port 5436. Its launcher is the ignored local `tmp/finance-scanner-uat/start-scanned-api.ps1`; it uses the tested Finance upload API build and real local scanner. LAB-14 UI 3014/API 7114 and original UI 3000/API 44399 remain separate. Keep their fixtures and settings intact. Local helpers, test credentials, file bytes and generated search-index state are excluded from the release.

This ledger consolidates the existing software-only laboratory run and its result-package continuation. It does not expand the run to all 81 cases, turn supporting automated tests into signed-in acceptance, or authorize production activation. The chronological [LAB-14/LAB-06 evidence](2026-09-12-lab-14-preparation.md) and separate [release/Finance evidence](2026-09-12-lab-production-verification.md) remain authoritative for exact actions and fixture identities. September 12 is the local run date; later UTC timestamps fall on September 13.

## Verified work

| Area | Evidence and current disposition |
| --- | --- |
| Preparation setup and execution | Signed-in tray configuration, independent protocol/workflow approval, mixed partial tray, scoped captures/QC, resource use/rejection, correction/repeat, terminal failure and reserve exhaustion recorded. Saved batches remain complete. |
| Preparation identity and custody | Output identity and quantities, QC reuse, sequencing membership and duplicate rejection, draft cancellation/reservation reuse, format retirement and frozen-layout preservation recorded. Sequencing stays Draft; no provider dispatch. |
| Preparation concurrency | Signed-in two-view stale output rejection and preserved entries; PostgreSQL competing reservations and resource replay. New forced simultaneous creation/name-collision test passes, including two distinct creates and stable retries. |
| Scientific guards | Signed-in missing/incomplete/unclean package, contributor, exception, execution and specimen-outcome checks recorded. Synthetic independent approval pins one package without releasing it. This is bounded evidence, not a complete connected lineage journey. |
| Real local file handling | Retained exact 158-byte file stored/read/hashed; actual ClamAV clean verdict; normal callback reaches ReadyForReview; duplicate callback rejected. External transfer was not exercised. |
| Multi-file rejection | Unknown/duplicate artifact references leave database state unchanged; clean malware verdict cannot override a checksum mismatch; package fails with one Clean/one Rejected file; repeated callback preserves failure. |
| Known result defects | UAT-20260912-01 concurrent registration and UAT-20260912-02 minimum-width overflow fixed/retested locally. Other role/navigation/layout/Finance defects and their local dispositions are in the separate release/Finance run. No production retest implied. |
| Help/recovery | Phaeno search, correct guide, query restoration, no-match state, keyboard topic navigation and 390px rail checks pass. Local stale corpus recovered by reloading only the owned UAT API. |
| Customer access closeout | Existing Customer Chrome session on separate local 3014 tab cannot open the Lab index or known preparation record. Cross-audience Phaeno guide is unavailable. Direct preparation loading defect UAT-20260912-07 fixed and retested; four focused cache/access regressions pass. Original Customer Job tab untouched. |

## Closure gates

| Gate | Status | Owner and concrete next action |
| --- | --- | --- |
| Remaining signed-in role checks | Partial | September 14: Independent Reviewer sign-in is available; package detail denies reviewer-only access. Saved failed-package ScientificReview fixture excludes the package and disables approval Save. Positive lineage remains blocked below. The separately approved temporary Operator grant for empty-tray response recovery was removed immediately after that check; ScientificReviewer is again the only active lab role. |
| Full positive scientific lineage | Blocked | Lab acceptance owner and Codex need a separate designated test journey advanced through supported receipt/specimen/execution/provider-custody steps. The retained ingestion work is AwaitingSpecimens with zero specimens. Its Clean file cannot supply those missing facts. Preserve completed preparation fixtures; do not force their milestones or fabricate provider events. |
| Actual object-storage/pipeline handoff | Blocked | Integration owner supplies the approved non-production transfer/pipeline endpoint and its test configuration through the normal environment setup. Current transfer URLs point to intentionally unused loopback port 1. Exercise upload, final immutable file identity and actual callback provenance before claiming this boundary. |
| Customer publication/download acceptance | Blocked | Requires the preceding complete package/independent approval plus an authorized release-manager session and scoped external test member. Test exact bytes and individual/ZIP completion, interrupted response and access denial. Neither retained metadata-only approval nor clean-but-unexecuted ingestion package is eligible as a shortcut. |
| Remaining LAB-14 manual variants | Not run in full | Signed-in empty-cancellation lost-response retry passed; populated-save/uncertain-create variants remain open. Completed-record keyboard/reflow checks passed at 320/390/1440 px with bounded light/dark evidence; writable forms, nested actions and live reduced motion remain open. Remaining signed-in held/closed Job/Trial commands and unauthorized Customer write attempts are unperformed. Customer index/direct-record read denial has live evidence; controller/PostgreSQL guards remain supporting evidence. |
| Final acceptance decision | Blocked | Product Owner/Scientific/Operations testers review exact case evidence and disposition outstanding gates. No acceptance signature or waiver is inferred from the instruction to continue. |

Physical bench, printer/scanner hardware, actual provider receipt, production deployment and recovery-drill signoff remain separate acceptance scopes. They are not asserted by this software-only run. The downstream Finance completion/invoice gap described in the separate Finance run also remains open; this ledger does not certify FIN-01 or change its business rules.

## Final engineering checkpoint in this pass

Added `backend/test/LabPreparationCreationConcurrencyPostgresTests.cs`. Two independent PostgreSQL connections are deliberately held at the global creation-allocation advisory lock. Existing base names are seeded only for a bounded test-specific timestamp window; the test proves both requests were waiting, then releases the lock and verifies collision resolution, distinct IDs/names, retained notes, stable retry IDs/names, exactly two command records and zero members. No application/machine clock changes. All generated records are scoped to the new workflow and removed in finally.

Commands used `PSEQ_OPERATIONS_REFERENCE_CONNECTION` for the owned 127.0.0.1:5436/phaeno_ops_lab06_uat cluster and `--artifacts-path tmp/uat-closeout-build`:

- Focused creation-concurrency test: Passed 1, Failed 0, Skipped 0; API/test build completed without warnings.
- Connected preparation required-final and optional-final/existing-output journeys plus scientific-review-gate regression: Passed 3, Failed 0, Skipped 0, using the already-built test assembly.
- Independent cleanup readback found zero TEST ONLY creation race workflows. Documentation whitespace verification passed.

These four backend tests close the forced-creation regression gap and revalidate connected server rules. They do not close the manual/provider gates above. No full frontend/backend suite, new dependency, model change, migration, Git operation or deployment was performed.

## UAT-20260912-07 — Customer preparation link never leaves loading

Severity: Medium. Status: **Fixed and retested locally**. Opened a separate Chrome tab at local 3014 using the existing external session, which visibly identifies Johns Hopkins University / General and the Portal shell. The Lab index displays assigned-role denial. Direct navigation to the preserved preparation record instead remained Loading preparation batch with Reload after the request window had settled. The page's query was disabled for the Customer, but its empty-data branch incorrectly treated disabled access as pending loading.

PreparationBatchPage now shows Preparation unavailable and the assigned-Phaeno-laboratory-role explanation, with Back to dashboard. It withholds cached batch details while access is absent or the session is unresolved and disables supporting resource/tube queries in those states. No backend authorization change. Four focused tests pass for denied access, cached staff data, unresolved session and genuine authorized loading. User guides were reviewed: their existing role boundary remains accurate; no new workflow or guide text is needed.

Live Customer retest shows the explicit permission explanation and no record details/operations. Enter on Back to dashboard returns to the Portal dashboard. A direct Phaeno scientific-approval guide route is also unavailable with Customer-only navigation. This verifies UI audience/read boundaries, not attempted authenticated write denial. Original Chrome Job tab 106832132 was never navigated or modified; the additional test tab is closed after verification.

Final frontend verification: all four access tests passed; pnpm run typecheck and scoped ESLint on the changed component/test passed. Whitespace/link-path checks passed. The existing Edge package tab remains preserved for the requested Independent Reviewer handoff; no response to that session request has been received in this pass. UAT remains open for the explicit gates above, not for an unreported test failure.

## Preserve for continuation

| Fixture | Current saved state |
| --- | --- |
| Resource preparation 6218afc4-d4c2-4437-a6ce-7e1bd46313c2 | Completed in LAB-14; preserve its 18-entry history and assigned library. Not used by this pass's temporary tests. |
| Ingestion package 3c42f211-a219-421d-a7bc-dd07c6dba5e7 | ReadyForReview/entity version 3; original clean 158-byte artifact; no approval/release. |
| Independent metadata-only package 9af5b43e-60bf-4700-af20-374dc5d6e140 | ReadyForRelease/entity version 2; original approval; no release. Do not publish. |
| Checksum-negative package d14354fb-36ef-4801-8184-adf6cde706e2 | Failed/entity version 3; retained Clean/Rejected pair; no release. Do not repair in place. |

The last three states were independently reread after the closeout regressions. Existing UAT services/storage and the browser handoff remain available. Further acceptance must resume from these saved facts instead of repeating setup or prior mutations.
