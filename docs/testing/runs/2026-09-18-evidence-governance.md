# Evidence governance verification — September 18, 2026

Scope: the owner's separately approved indefinite internal evidence preservation, mandatory scientific evidence minimum and different-Supervisor review of on-behalf performance/attribution changes. Implementation is local, uncommitted and not deployed. Earlier traceability and restore evidence remains in its original run records.

## Results

- Backend: 61 focused cases passed in `artifacts/sample-investigation-20260919/governance-final.trx`; one existing Unix-only symlink check was skipped on Windows. This includes the PostgreSQL governance journey, domain evidence/profile validation, immutable model rules and storage registration.
- Additional PostgreSQL regressions: the reserve-tube result chain and shared preparation output journey passed, bringing this checkpoint to 63 distinct passing backend cases. The three-case run (including an earlier version of the governance journey) is `governance-postgres.trx` in the same artifact directory. The final governance journey was rerun after its last change.
- Frontend: 43 cases passed across step timing, history display, execution, preparation and preview isolation. Previewing another performer uses fictional data and does not query the staff directory.
- Browser: 14 cases passed across desktop/mobile protocol execution, light/dark sample investigation and independent performer/time review. The new review cases were rerun after their final rendering change. These are simulated API fixtures, not live authorization or physical/scientific acceptance.
- Full solution build passed; subsequent backend changes compiled through the passing test run. Frontend type checking, touched-file lint, generated user documentation, ERD/model consistency and whitespace checks passed.

## Meaningful paths exercised

The governance database journey preserves a private QC report when deletion is attempted and permits deletion of unrelated customer result bytes. It records a proposal, rejects a changed retry and self-approval, rejects a wrong-sample review and stale competing approval, accepts an independent decision, retains the original protocol JSON, and proves that a later proposal carries its approved predecessor's attribution. It rejects new results with an unprofiled or incomplete analysis, then accepts a fully documented run with an explained de novo reference exception. On-behalf capture creates a pending proposal; rejection leaves it unverified; an approved correction resolves it. Deletion of a recorded decision fails.

Browser checks cover required-field feedback, actual-person/time submission, disabled self-review, independent approval, visible previous/proposed attribution, responsive layout and accessibility. A heading-order defect in the review modal was caught and fixed. The storage DI fixture was updated to include the required evidence-preservation database dependency. An accidentally broad test selector included an unrelated kit acceptance test whose disposable-database guard rejected this fixture; it was stopped and replaced with explicit traceability case selection. No claim is made that the whole repository suite ran.

## Database and documentation

Additive migration `20260919050429_AddLabEvidenceGovernance` was applied to verified local `localhost:5432/phaeno_ops` and disposable `127.0.0.1:55441/traceability_reference`. It adds two review tables and one nullable requirements snapshot; no historical performer, scientific fact or source link was invented. EF reported no pending model changes. The ERD contains 197 tables, 2,884 fields and 452 foreign keys. Three Phaeno guides and their search summaries were updated; 56-guide corpus regeneration and consistency checks passed.

## Remaining acceptance and activation boundaries

Both prospective traceability/scientific switches remain default-off. No shared/production migration, Git mutation, deployment or production setting change was performed. Production cutoff/in-flight rules, actual producer mapping and external-file availability, bench/scientific acceptance, and hosted backup/recovery still need their own evidence. The earlier synthetic local restore is not hosted recovery. Internal uploaded reports are preserved indefinitely; external raw/reference files remain with their producing systems.
