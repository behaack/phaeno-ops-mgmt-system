# Acceptance closure execution — September 14, 2026

## Latest closure slice

[Guided-evidence and retirement execution](2026-09-14-guided-evidence-retirement-uat.md) closes LAB-04/07 and fixes/retests duplicate stages in empty workflow recovery. Overall isolated software acceptance is **19 of 81 cases**; the broader ten-further-cases request is complete. Earlier tube-intake, material/equipment and versioning evidence remains valid. The original next-ten selection below remains three completed order/CRM/shipping/Website cases; its seven remaining cases are not relabeled passed.

## Earlier ten-case batch checkpoint

[September 14 next-ten execution](2026-09-14-next-ten-uat.md) records CRM-06, SHP-01 and corrected-local WEB-01 closure, SHP-02 partial controls, six current order/shipping gates and the newer owned API/Website build identities. Whole-case isolated acceptance at that checkpoint was 12 of 81. The historical baselines below remain dated evidence.

## Baseline and boundaries

Current local UI 3016 proxies API 7116. API now runs the current source build under tmp/uat-resume-20260914-build (HEAD 25bcfbf3db8b9d0807f574f447d4d238b07cca64 plus retained working changes), replacing only the owned older Finance binary. Build passed. API health returned 200. All current source migrations are applied; one retained historical Website-submission-language migration is also recorded. No migrations were applied or removed. Exact binary hash and settings are in ignored tmp/uat-closure/baseline.json.

Business roles, dual control and native receivables remain enabled. Outbound provider URLs remain unused loopback port 1; scheduled retention/deletion/Kit lifecycle remain disabled. ClamAV remains configured, but its Docker daemon is unavailable; scanner-backed new uploads must fail closed. This is a blocker for new upload/provider checks, not a reason to relabel prior real scanner proof. No existing owner browser tab, role, private credential, scientific package or accepted Job was changed.

Carry-forward decision: Finance controller/domain/API and Finance UI source have no diff from commit 26839b4, which recorded the September 12 Finance corrections and acceptance checkpoint. Those dated connected observations are retained; current source build and targeted signed-in readbacks supply continuity. This does not carry forward unrelated production, physical, provider or scientific acceptance. All browser continuations use actual Clerk identities/API/database; mutations are limited to the separately recorded reconciliation and isolated paging fixtures below. Credential material remains in process memory from existing encrypted local test credentials.

## FIN-04 — Pass for isolated software acceptance

| Script step | Evidence |
| --- | --- |
| 1 Invalid/valid preview | Finance run Populated Finance acceptance: invalid row rejected, valid two-row $325 preview, no receipts before confirmation. |
| 2 Changed input | Same checkpoint: input change removes confirm and requires another review. |
| 3 Second operator | Finance role separation: second Cash preview and direct confirmation denied with payment_import_preview_owned and unchanged receipts. |
| 4 Confirm/retry | Original confirmed import has exactly two receipts, $325; repeated unchanged file rejected without another import. |
| 5 Source/suggestions/totals | Today: original protected import evidence contains the two exact source/Customer/reference/amount rows summing $325. Separate $7 import source evidence and three same-Customer suggestions read; no allocations exist for that receipt and all eight receipt readbacks remain identical before/after. Screenshot confirms selected invoice survives a no-match search. |

The original $3 unconfirmed preview was not confirmed. Today was read-only; initial harness expected the old Allocate payment opener, then resumed using current Allocate to invoice without repeating any transaction. Evidence: tmp/uat-closure/fin04-readback.json, finance-selected-search.png, finish-fin04.mjs; historical source [Finance run](2026-09-12-lab-production-verification.md), including Populated Finance acceptance and Finance role separation. This closes the scripted import case; it is not an invoice-generation or scanner-format acceptance claim.

## FIN-05 — Pass for isolated software acceptance

| Script step | Evidence |
| --- | --- |
| 1 Unbalanced submission | Historical $75 main reconciliation initially bank $70: visible difference and submit disabled. |
| 2 Reasoned draft revision | Historical bank correction plus today's distinct editor changes period September 12 to 13, adds the retained $25 receipt to the $1 receipt, and balances bank/ledger at $26. Exact before/after period/receipt IDs and editor/time/reason retained. |
| 3 Cancellation | Historical separate $25 draft cancelled with reason; underlying receipts unchanged. |
| 4 Independence | Historical creator/contributor gates plus today's CashReview editor (different from creator/submitter and receipt contributor) receives HTTP 409 on approval. Submitted record remains identical after denial. |
| 5 Independent report | Existing CashReconciler approves; fresh readback remains Approved/version 4. Actual browser downloads text closeout; parsed report and downloaded text match $26/$26/$0, two source receipts, one draft change and distinct approving/editor identities. Screenshot inspected. |

New retained batch 189e1ccd-2516-4768-8037-3561bb659a9c / REC-20260912-DF2AE2A8. Five guarded authenticated API commands: create, reasoned edit, submit, rejected editor approval, independent approval. No financial receipt/allocation changed. All eight receipts and the three prior reconciliations compare unchanged before/after. New batch links existing synthetic $1 receipt 5d0b4cda-4c82-4fc5-a44c-24b12738f644 and $25 receipt 74fa9bd8-0b66-43aa-be9d-cff1d0244fa2. Preserve its history; do not rerun the mutation helper.

Harness continuation was read-only after all commands succeeded: refreshed the reviewer list after another session's approval, then compared JSON report contents rather than JSONB whitespace/key ordering. Evidence: tmp/uat-closure/fin05-editor-journal.json, fin05-closeout.txt and fin05-approved.png. No product correction was needed for these gates.

These three case passes apply to the stated isolated acceptance baseline with explicitly carried-forward unchanged Finance evidence. They are not the final 81-case release signoff. All other cases remain open until their own requirements are met.


## FIN-02 — Pass for isolated software acceptance

| Script step | Evidence |
| --- | --- |
| 1 Receipt and protected evidence | September 12 real-scanner checkpoint: clean $1 receipt, exact 83-byte authorized download, Billing-only 403 and anonymous 401. The script calls for one approved format; exhaustive formats are not implied. |
| 2 Unavailable/rejected scanner | Finance exceptions and real-scanner checkpoints: unavailable scanner and EICAR fail closed, retain entered values and create no receipt; clean replacement saves once. |
| 3 Partial allocation | Original $250 import receipt allocated $100 then $120 to $220 invoice; expected zero invoice balance and $30 unapplied read back before later explicit reversals. |
| 4 Invalid combinations | Finance exceptions checkpoint: over-allocation, cross-Customer and currency denials, unchanged balances; today's search/paging creates no allocation. |
| 5 Split/multiple receipt and selection | Historical split/multiple receipt allocations reconcile. Today: dedicated 26-invoice Customer, first page 25/second page 1, selection on second page retained after Previous and a no-match search. Screenshot inspected; receipt/allocation readback unchanged. |

Paging fixture scope TEST-ONLY-FIN-PAGING-20260914: Customer 01361b8e-0b08-4b69-96d8-857ce513f1af, receipt 4f9a7cad-473a-40e2-b441-501900a6ded1 ($1), 26 arithmetic invoices ($10 each). These are isolated synthetic database fixtures with deliberately unavailable PDF keys, not invoice-issuance or receipt-upload proof. Original eight receipts compare unchanged. Preserve the new fixture; do not rerun its guarded setup. Its database transaction committed before the helper could write a manifest; recovered IDs by read-only query, without duplicating data. The read-only browser harness initially waited for native option visibility; corrected to wait for attachment, then all checks passed. Evidence: tmp/uat-finance-paging/manifest.json and Program.cs; tmp/uat-closure/fin02-paging-results.json and fin02-paging.png.


## Verified local gap fixes and FIN-06 continuation

- **Completion transaction defect reproduced and fixed.** Before correction, a PostgreSQL trigger rejecting the final idempotency insert left a committed $110 invoice. Complete now uses the existing atomic idempotency transaction and per-order lock. Its Job, invoice/outbox, notices and replay response commit together. Failed execution removes the new PDF only after a server read proves it is unreferenced; uncertain cleanup retains the bytes. The same injected failure now leaves the Job InProgress, no invoice/notices/events, and no orphaned PDF. A deliberate retry creates one invoice with Net 30/$110 snapshot; same-key replay returns the same version/status and does not write another PDF. Actual renderer bytes and hash checked. Evidence: CompletionFinalSaveFailureRollsBackInvoiceAndSafeRetryReplaysOnePdf; ignored completion-before.trx (expected reproduced failure) and completion-after.trx (Pass). Both generated local databases were dropped. This is a disposable software transaction test using a synthetic legacy-terminal sample; it does not close FIN-01 or replace a legitimate Lab journey.
- **Stale billing reminder fixed.** After approval, the saved-profile approval reminder is removed; the saved approval badge/date remain. The interaction test performs save, refresh and approval. No live billing profile was edited again.
- **Missing Finance attention links fixed.** Invoice/receipt/reconciliation items now link to the existing owning detail routes only for the corresponding Finance capabilities. Roles and backend authorization did not change. Actual Billing, Cash and independent Reconciler sessions followed four links successfully and displayed the correct record. The Reconciler has no invoice/receipt links. Desktop and 390px screenshots inspected; narrow page scroll/client widths both 390. All nine receipts and four prior reconciliations compare unchanged.

For the attention test, the owned local API now runs the completion build at tmp/uat-completion-build/bin/PSeq.Operations.Api/debug/PSeq.Operations.Api.dll, SHA256 0C5786A2E802856E5D18F14B572FE83FA76B2129901C7D932E0E52DDE03A285F. AttentionOperations is enabled only here. Retention/deletion workers and outbound providers retain their previous disabled/loopback configuration; scanner daemon remains unavailable. Prior disabled attention rendering was captured before enabling. List synchronization creates/refreshes local attention metadata. One new synthetic draft reconciliation e81e4aab-86e5-41d7-91ac-13709bbf4555 / REC-20260914-F14A65E8 links only the $1 paging receipt against $2 bank total, yielding $1 difference. It remains Draft/version 1. No cash is applied, reversed, submitted or approved. Keep the journal; creation already succeeded and must not be repeated. The continuation reads the existing journal instead of creating another batch.

FIN-06 step 5 attention navigation is now verified; the full case remains open for the explicit unavailable legacy-connector comparison in an appropriate authorized session. Historical aging/export/filter/retry evidence remains applicable to unchanged Finance collection code. Source inspection found that general attention items intentionally remain until operator resolution; the wording and implementation were not changed to auto-resolve them. No new auto-resolution requirement was invented.

Validation: one new PostgreSQL failure/retry journey passed after reproducing the defect. All 41 targeted Finance panel/disabled-capability checks passed; TypeScript and scoped lint passed. Updated Phaeno Finance help and generated both UI/API search manifests. Documentation generator initially hit a Windows file-write error; the unchanged generator computation wrote temporary outputs, copied to the two exact generated targets, then its normal check passed (56 guides; corpus d2c5cd3f211e1fe836827b5d7208df1bd1118d9f3474491500f9771e4665661a). Actual signed-in search returns the same corpus and the updated Finance guide. React review: role-derived links, existing router/query state, semantic anchors, no new effects or dependencies. No production deployment, shared migration, role change or Git mutation.

Evidence: tmp/uat-closure/fin06-attention-journal.json, fin06-attention-disabled.json, fin06-narrow.png, fin06-UnappliedCash.png, docs-search-readback.json and current-baseline.json. Initial browser continuation expected a button/detail label while the existing Back to Finance control is a link; corrected the harness and verified actual record identifiers. No transaction was repeated.

## Remaining stopping conditions

The three full-case passes remain FIN-02/04/05. The supported Lab-to-Commercial completion handoff is still absent; an open Product Owner question asks whether final material-exhaustion processing failure can close/invoice or needs a commercial decision. Processing failure must not become intake rejection or fabricated success. Existing successful preparation alone is not completed scientific output.

Reusable signed-in credentials were available for four Finance identities; the preserved in-app session was the independent Scientific Reviewer. Remaining remote work needed the role/audience sessions in the [identity setup record](2026-09-14-uat-identity-execution.md). No credentials of existing real people were reset, no prior temporary reviewer Operator grant was repeated, and no mock session stood in for connected acceptance. Physical/device facts, controlled scientific/provider lineage, authorized destination delivery and isolated retention/restore prerequisites remained distinct packages in the historical reconciliation. None was waived by this run.

## FIN-06 — Pass for isolated software acceptance

| Script step | Evidence |
| --- | --- |
| 1 Aging boundaries | Retained September 12 connected Billing run checks all eight boundary dates at its recorded UTC aging date; parsed CSV and saved invoice facts reconcile to every bucket. Later dates are not compared to the old bucket expectations. |
| 2 Customer/record return | Retained signed-in Finance filter/detail/Back verification and current role-scoped attention links; owning record identity and filter/section preserved. |
| 3 All-Customer exports | Retained independently parsed invoice/receipt/aging exports include Customer B despite the Customer A display filter. |
| 4 Section failure/retry | Retained connected failed-section/supporting-choices recovery distinguishes error from empty and keeps independent sections usable. |
| 5 Attention and unavailable connector | Today's Billing/Cash/Reconciler attention routes and previously captured disabled label pass. New dedicated P-ADMIN queues one local catalog synchronization using a fixed idempotency key. With RealmId blank and provider endpoints on unused loopback port 1, the worker stops after one attempt at NeedsAttention and explicitly records no external document/payment status. The real legacy queue shows that error and recovery. P-ADMIN is denied native Finance reads; no Finance role was added. All 40 invoice and nine receipt records compare deeply equal before/after in PostgreSQL. |

Retained new integration message: b470e3cd-d371-478e-ad86-f52df188c59f. Do not queue it again; the creation journal records the successful Pending result and the worker's NeedsAttention readback. The endpoint's existing envelope returns HTTP 200 with Pending, despite its controller attempting 202; UI does not claim completion. This transport-status inconsistency is recorded separately from FIN-06's business-state assertions. No external connection or real payment occurred. Evidence: ignored `tmp/uat-closure/fin06-connector.json`, `fin06-connector.png`, `fin06-financial-before.json`, `fin06-financial-after.json`; previous Finance aging and recovery artifacts remain linked in the September 12 run.

## WEB-06 — Pass for isolated software acceptance

| Script step | Evidence |
| --- | --- |
| 1 Four audiences and search | Actual dedicated Phaeno, Customer, Prospect and Partner Clerk sessions. Known phrases unapplied/shipping/Trial/Kit return the correct audience's guides. Internal title is POMS; external titles are Portal. |
| 2 Direct guide boundaries | Each of four sessions attempts the other three audiences' getting-started URLs: all 12 show Documentation unavailable and no article. This is UI audience policy, not confidentiality of bundled help. |
| 3 Guide/workspace return | Each opens a real indexed result and matching section, follows adjacent guide, returns to the original guide, returns to search, opens Dashboard and returns. Query, guide identity and organization remain correct. |
| 4 No-match, outage, pending identity | Each distinguishes real no-match from an injected 503 and uses Try again to obtain real results. Separately, delay a real Phaeno response, sign out through the user menu, sign in as Customer in the same browser context, then release the old response. Signed-out screen contains no results; the Customer search shows only Customer IDs and no Phaeno guide links. |
| 5 Keyboard, touch and layout | Internal topic groups allow one expansion and open the active group. All four sessions operate the non-modal rail with keyboard/Escape and touch at 390px, with dark mode/reduced motion and focus return. Independent page-width checks show no horizontal overflow. Screenshots inspected. |
| 6 Published locale | External corpus contains only en-US; no unsupported locale selector is advertised. Phaeno corpus uses its documented unlocalized US-English representation. All four audiences reread the final corpus after help regeneration/API reload. |

**Fixed a real narrow-screen defect:** an external organization/Department row increases header height beyond the sidebar's fixed offset, covering the Documentation button and intercepting taps. Header now publishes its measured height, and the shared sidebar/edge tab and guide-anchor clearance follow it on resize. Actual Customer, Prospect and Partner taps pass after the fix. Added one browser regression, exercised on both browser projects at 390/320/768px; the complete focused Documentation navigation suite passes 10/10. Shared sidebar/search component checks pass 4/4; TypeScript and scoped lint pass. This scoped regression evidence supports the connected acceptance and does not replace it.

Reviewed/updated the four getting-started guides to describe the tab below toolbar and organization details. Generated corpus now has 56 guides and hash `50f599990a05` (full hash in generated manifest and current readback). The same owned API binary reloaded the new corpus; no deployment, migration, auth-policy change or existing business write. Source artifact generation used the same generator with output redirected to ignored temporary files, then copied the two exact outputs back; normal `--check` passed. Final audience search readback is in `cohort-current-readback.json`; detailed pre-copy navigation observations remain valid because only the explanatory guide sentence/review date changed.

Evidence: ignored `tmp/uat-closure/web06-results.json`, `web06-pending-session.json`, four `web06-*-narrow.png` files, `web06-pending-session.png`, and `docs-regression`. Dedicated test setup and scope are documented in the [identity setup record](2026-09-14-uat-identity-execution.md). Remaining Lab checks used these authorized sessions; original historical/physical/provider gates remained unchanged.


## LAB-14 — Populated recovery and Customer isolation

Actual Clerk Operator and Supervisor sessions used the LAB-06 isolated copy at 3016/7116. The earlier LAB-14 3014 runtime and its database were not changed. Two new explicitly TEST ONLY Jobs/tubes, a virtual equipment item and a 100 mL material lot were prepared with audited engineering fixture code. These are software fixtures, not observed receipt, physical QC, scientific approval or Commercial billing lineage. The reviewed workflow remains `6f93c876-17f1-4bb4-935b-8dfa7a77a8eb`; format `3bfbae9d-c968-4490-a8e7-076801dbb2fd` is reused.

- **Create recovery:** the first real creation committed, then its response was dropped. Unchanged UI retry reused the exact request and returned one batch `a85c03b8-9414-4ba2-8852-d21497d3fa44` / `PSeq-20260914-210012`, with one creation history entry. Two sources were then added once.
- **Step 19, populated conflict:** Operator prepared A1 → A3; distinct Supervisor moved A2 → B1 first. Operator's stale save returned 409 and left the Supervisor's saved batch unchanged. A reviewed retry preserved A3, used the refreshed version and a new command ID, and committed once. Exactly two move records exist.
- **Steps 10/19/21, nested resource recovery:** after starting and recording the software-only source identity step, the unfinished shared step retained 20 °C / 20 minute values and first-tube-only coverage. Nested equipment cancellation preserved those inputs. Real material use committed before an injected lost response; retry retained the identical request ID, batch/resource versions and 1.5 mL quantity. Exactly one consumption exists, with 98.5 mL remaining and one material history entry. Nested success preserved parent values. The unfinished shared step was cancelled without saving scientific evidence. The 390 px dark/reduced-motion form had no page overflow and was inspected visually.
- **Step 20, Customer:** actual Customer member reads of the preparation index/detail returned 403. All seven attempted commands (add/start/move/remove/cancel/output/step) returned 403 `lab_capability_required`; complete independent Operator readback stayed unchanged after each denial.

Current new batch is InProgress/version 8, with eight history entries and sources at A3/B1. Source tube IDs are `3fcb3f82-59d6-4129-85cb-ef0835eaa822` and `100c2981-3c23-4a21-8516-00da298138c4`. Material lot `26567a04-f330-4dfc-a24c-0d95332a33bc` is version 2 / 98.5 mL. Preserve this unfinished batch. Original resource batch `6218afc4-d4c2-4437-a6ce-7e1bd46313c2` remains Complete/version 18; original draft `0e7a8741-bf17-4aad-ba56-30ef0c092ebe` remains Draft/version 2. No output, sequencing assignment or result release was added.

Evidence: ignored `tmp/uat-closure/lab14-recovery-fixture.json`, `lab14-create-recovery.json`, `lab14-populated-recovery.json`, `lab14-populated-narrow.png`, and independent database consumption/readbacks. **LAB-14 remains open for connected held/closed Job and Trial commands and final complete crosswalk reconciliation.** The engineering PostgreSQL guard matrix remains supporting evidence, not a substitute for those connected variants.

## Trial extracted-RNA eligibility defect — reproduced and fixed locally

The actual Scientific Operations test identity could read Trial configuration, but its sample-type choices were empty. Database readback showed active `Reference extracted RNA` (`bb67974b-dd3a-4eff-9cfc-4d1b927cbd58`) with material class `extracted_rna`. Trial configuration and submission normalized spaces/hyphens but omitted underscores, unlike the canonical material code already used by Lab workflows. Both checks now recognize the underscore spelling without changing stored definitions, quantities, approval rules or scientific criteria.

The existing PostgreSQL batch-submission regression was extended to use the canonical code. It failed before the fix because the type was absent, then passed through configuration, accepted scope, two-sample submission, one Lab authorization, one shipment and exact tube-slot counts. The existing spaced-label approval/submission regression also passed: **2 passed, 0 skipped**. These fixtures ran inside rolled-back transactions in the isolated local database; no reviewer authority or production workflow replacement persisted. Evidence: `trial-rna-before.trx` and `trial-rna-after.trx` under `tmp/uat-closure`.

The owned API was refreshed from `tmp/uat-rna-build/bin/PSeq.Operations.Api/debug/PSeq.Operations.Api.dll`, SHA256 `FA16B5DDB51FF4DF495BA82032AECA22926D2AEA3AE099D5551177CD6022EB97`, retaining the earlier isolated flags/storage/disabled-provider settings and current documentation corpus. Fresh actual P-TRIAL-S configuration now includes the saved RNA type with its unchanged unit/limits. The eligible analysis list remains empty (the database has no analysis definitions), so approved Trial scope/submission and its connected preparation guard cannot proceed with a reviewed eligible analysis yet. No synthetic flag was changed, acceptance criteria invented, Trial approval fabricated, or historical package repurposed. Existing Phaeno/Prospect Trial guides accurately describe extracted-RNA-only submission; their user instructions need no change for this spelling fix. This does not close TRI-01–06 or LAB-14.


## ACC-03 — Connected Department controls complete; historical accepted snapshot remains open

Actual Customer A Organization admin, Research Department admin and Research member sessions exercised the five-step workflow. The new Department `b52481c4-7f98-4d71-9699-644dc7185a4c` / TEST ONLY ACC03 Closure was created through the UI, made default through its consequence review, and the original General default restored. Organization PO required → Research waived → Research inheritance was persisted and checked through fresh member sessions: effective values were true → false → true, with the matching override/default source records.

Research administration displayed only Research settings and existing-member management. Actual protected writes to Organization settings, Operations settings/access, ordinary-member settings and Organization-role promotion returned 403. Final Research-assignment removal and stranding-Department deactivation returned 400; complete independent Department/member/configuration readbacks were unchanged. The capability named `canChangeMemberRoles` includes permitted Department roles; it is true for a Department admin and is not evidence of Organization-role authority. The protected Organization promotion endpoint was explicitly denied.

The Organization admin reviewed alternate access in the new test Department, then removed the member's Research assignment. Research admin found the existing member by exact email and reviewed restoration of the same assignment ID, with no admin promotion or new invitation. Deactivate/reactivate of the new Department retained its revoked assignment; reactivation did not restore user access. Cleanup restored the original Organization defaults, General default and Research-only ordinary membership, and retired the temporary Department while preserving history. The narrow dark/reduced-motion member dialog was inspected. No Company or existing user was deactivated.

All 44 retained quotes and 14 shipment rows compare identical before/after. However, Customer A's five retained quotes are SyncPending with no accepted timestamp, and it has no retained shipment. Those records cannot prove the script's specific accepted-quote/shipping snapshot preservation in the affected organization. **Do not mark ACC-03 Pass yet:** reuse this completed control evidence and finish that single historical-snapshot comparison with an eligible accepted fixture. Evidence: `acc03-connected.json`, `acc03-department-admin.png`, `acc03-history-before.json`, `acc03-history-after.json` and `acc03-history-customer-links.json` in ignored `tmp/uat-closure`. Harness corrections handled the required includeInactive query, scoped capability semantics and version-only removal request; no duplicate create/access mutation was repeated.


## CRM-02 — Pass for isolated software acceptance

The approved non-admin CommercialOperator identity P-PRICE performed the Sales responsibility. The bare P-SALES Phaeno membership was correctly denied CRM access under current backend business-role rules; it was not promoted. No additional identity, role or Organization access was granted.

| Script step | Connected evidence |
| --- | --- |
| 1 Create/work/qualify | UI missing display name and missing decision explanation prevented submission. New → Working → Qualified persisted, with explanation and the actual Commercial actor/time in saved CRM activities. |
| 2 Duplicate review | Creating a Company that matches the existing controlled Company returned 409 `crm_lead_company_duplicate`. No automatic merge or additional Company, Contact or Opportunity was committed; fresh Lead readback remained Qualified and unchanged. |
| 3 Existing Company conversion | UI explicitly selected the existing Company, created one Contact and one Opportunity using the active default pipeline. Converted history retains all resulting IDs and qualification evidence. Company has no Portal access. Exactly one Company/Contact association, Opportunity and initial stage-history row exist. |
| 4 Refresh/retry | Original-version and current-version repeated conversion were rejected without changing the converted Lead or creating duplicates. Ordinary converted editing was rejected. After the queue fix, All statuses → converted Lead → Back to leads reopens retained history; automated desktop/mobile coverage also exercises the explicit Converted filter. |
| 5 Disqualify/terminal behavior | A real direct update reproduced an improper successful edit of a Disqualified Lead. The domain guard was fixed. A fresh Lead was then disqualified with a reason through the UI; actual profile-edit and conversion attempts returned 409, with complete saved readback unchanged. No Edit/Convert action appears, and the reason/actor/time remain visible. PostgreSQL regression additionally rejects Working, qualification and replacing the original disqualification reason. |

**Defects fixed:** (1) Converted Leads were automatically inactive but the queue excluded inactive records even under All statuses. The queue now includes retained history; ordinary terminal edit restrictions remain. (2) The UI considered Disqualified Leads immutable, but the backend guarded only Converted. The shared ordinary-mutation guard now covers both. The existing explicit Company/Contact merge relinking methods remain supported.

Retained records: Company `3ae91103-f3d4-441d-bcf8-a5490af2c3c7`, converted Lead `1d36d87c-d2e6-473e-9a44-31ac25608f50` (version 4), Contact `a1a4df0b-0717-4835-ab33-32baed21bb3d`, Opportunity `86233f00-00bd-4593-b62c-e77cc35ca5e4`, and verified Disqualified Lead `03a0eaf7-df70-4b7a-ba5f-d6d682a99793` (version 2). The original defect fixture `0be91ea5-2a9b-4624-a446-5be549845345` retains the improperly accepted TEST ONLY denied edit as evidence; it was not rewritten or erased after the fix. Reuse these records for subsequent CRM work; do not repeat conversion or create another Company.

Organizations, Organization memberships, Lab work IDs, Lab Service order IDs and Trial IDs compare unchanged before/after the CRM journey. No Portal access, invitation, outreach, entitlement or executable work was created. Four saved decision/conversion activities identify the actual Commercial test actor. Screenshots of both final workspaces were inspected.

Focused verification: the new persisted terminal-edit regression failed before correction, then passed together with the existing conversion/merge-identity test (**2 passed**). Converted-history and existing Lead navigation checks passed on desktop/mobile (**4 passed**). TypeScript, scoped lint and generated-help consistency passed. These counts support the exact connected crosswalk rather than replacing it. The Phaeno Lead guide now explains reopening converted history.

Current API: `tmp/uat-crm-build/bin/PSeq.Operations.Api/debug/PSeq.Operations.Api.dll`, SHA256 `687D55B2DF764AFDC8E4B54802C7D3B6BAD2FA034A8C6C36CF3E051E26FE6F42`; current 56-guide corpus `b1b88dd9cb3254d8de5709e732e8a5f47416d8c4202768ffdcbc62505223727c`. Same isolated database/flags/provider/storage boundaries. The only additional documentation content after WEB-06 closure is the Lead-history instruction; no documentation audience, navigation, search or locale behavior changed.

Evidence in ignored `tmp/uat-closure`: `crm02-connected.json`, `crm02-noncrm-before.json`, `crm02-noncrm-after.json`, `crm02-saved-evidence.json`, `crm02-converted.png`, `crm02-disqualified.png`, `crm02-terminal-before.trx`, `crm02-terminal-after.trx`, `crm02-regression/`, and `crm-final-api-baseline.json`. The first duplicate read comparison exposed ordinary .NET/PostgreSQL timestamp precision, corrected by comparing fresh saved readbacks. The case is closed for isolated software acceptance, not final deployment signoff.


## CRM-03 — Pass for isolated software acceptance

Actual P-PRICE (non-admin CommercialOperator) and P-ADMIN sessions completed the connected Opportunity flow. One non-default TEST ONLY pipeline and three TEST ONLY Opportunities were added through supported commands; the existing default pipeline and Companies were not changed.

| Script step | Connected evidence |
| --- | --- |
| 1 Create and buying team | UI created a USD 1,000 Customer pursuit, EUR 700 pursuit and USD 300 pursuit in a second pipeline. Each has a distinct read-only number and one initial stage-history entry. A primary buying-team Contact was selected through the searchable association dialog. The existing active association was excluded from choices, and direct duplicate association returned 409 with one retained relationship. |
| 2 Progress and outcome reason | Discovery → Qualified saved once but exposed the response defect below. After correction, missing Lost reason returned 400 without changing the Opportunity/history; reasoned Lost and Abandoned variants persisted. Stage history retains prior/new stage, actual actor, time and reason. |
| 3 Cross-pipeline and reopen | An active second-pipeline stage was rejected with 409 and unchanged complete Opportunity/history readbacks. Lost → Qualified and Abandoned → Discovery preserved earlier closed transitions while clearing the current closed outcome. |
| 4 Won without work | The Customer pursuit moved to Won with five retained history entries. Independent full-row comparisons found both Companies, all 13 entitlements, 69 Lab Service orders, 24 Lab work orders and two Commercial outbox rows unchanged. Won created no access, entitlement or executable work. |
| 5 Board/table/export/Reports | Both pipelines' filtered board and table displayed matching identities, numbers and stages. Actual admin filtered CSV contained exactly the two requested default-pipeline CRM03 records; the second-pipeline record was excluded and non-admin export was denied. Reports counted all three open Opportunities, including EUR, while USD open amount was $300 and weighted forecast $60. The original zero-amount CRM02 Opportunity explains the third open count. No currency conversion was implied. |

**Defect corrected:** after committing a stage move, the controller attempted to mark its loaded non-null Stage navigation unloaded. EF threw, producing HTTP 500 after a successful save. The controller now returns a fresh untracked read of the saved Opportunity. PostgreSQL proved that the original failed-response move had committed exactly once; it was not repeated. The connected run resumed at its saved Qualified stage. A rollback-scoped regression reproduced the original exception and then passed saved-stage/probability/version readback, stale replay without duplicate history, loss and reopening. An intermediate test expectation was corrected to use the existing concurrency exception type; this was not another product defect.

Retain Customer Won Opportunity `52a389bc-e222-4aaa-9d68-6cc6979a0524` (`OPP-20260914-AC2462437D`), EUR Opportunity `7e130e54-e768-4a1a-ab83-c760d03933ac`, second-pipeline USD Opportunity `8265601b-25ee-4cf9-afee-d8c896632d64` and buying-team association `f696f223-a17b-4b52-90cb-19eaf0d22906`. The Won record is available for a separately reviewed order handoff; the CRM-only Company is not an accepted Prospect Trial scope.

Evidence in ignored `tmp/uat-closure`: `crm03-connected.json`, `crm03-before.json`, `crm03-after.json`, `crm03-filtered.csv`, `crm03-won-history.png`, `crm03-reports.png`, `crm03-stage-before.trx`, `crm03-stage-verified.trx`. Screenshots were inspected. One transient Clerk login timed out before setup and succeeded on retry without changing identities. The first board locator was corrected to account for the card's full accessible name; no business write was repeated.

## CRM-04 — Pass for isolated software acceptance

Actual P-PRICE and P-ADMIN sessions reused the CRM02 Company, with no new identity/access grant or outbound message.

| Script step | Connected evidence |
| --- | --- |
| 1 Internal and manually recorded Email | Both UI activities persisted with actor/time and appeared in the timeline. The Email action only recorded a CRM activity; browser writes were limited to the reviewed CRM endpoint and the controller contains no delivery command. No outreach or invitation was requested. |
| 2 Restricted activity | P-ADMIN recorded one Restricted note. P-PRICE's timeline and direct activity list omitted it; direct update, deactivate and Restricted-create attempts returned 403. There is no individual activity GET route (404/405 observed). Admin fresh readback remained unchanged. |
| 3 Blocked and completed | A linked overdue task rejected an empty Blocked reason through native UI validation and HTTP 400. A reasoned Blocked transition and completion persisted. The completed task remains in the queue/history with no ordinary Update action. Independent database readback identifies the actual completing user. |
| 4 One recurrence | Completing the weekly task created exactly one next occurrence, seven days after the original due/reminder times with the same Company and owner. Original-version and current-version completion retries returned 409 without changing the original or creating another occurrence. The remaining disposable successor was cancelled through the UI; both completed tasks and their timeline entries remain. |
| 5 Attention links | Before completion, Home overdue and due-within-seven-days links each displayed the corresponding test task. After cleanup neither terminal task/successor appears in these attention lists. Next-action link displayed the matching active Lead and excluded converted/disqualified Leads. A controlled age fixture verified two stale open Opportunities across both pipelines and excluded an equally aged Won record; selecting the second pipeline reduced the list correctly. Dashboard, actual filtered API and rendered records agree. |

The age fixture temporarily changed only `updated_at` on the three newly created CRM03 TEST ONLY Opportunities to 31 days earlier. A guarded restore ran immediately after the read-only browser check; all three original timestamps and versions compare exactly equal before/after. No stage, actor, amount, history or existing operational record was rewritten. This is explicit software boundary evidence, not a claim that 31 days elapsed. The first supporting stale API probe used the wrong optional parameter and is marked superseded; the final `staleOnly=true` probe supplies the populated acceptance evidence.

Retained Tasks: completed overdue `1cac0c7b-b47d-4c17-9711-2059c1ca7071`, completed recurring `2927fd73-69c5-4824-b4fb-addabc39b948`, cancelled successor `1c222988-66b4-4188-98cc-9f2ab30a51f9`. No disposable CRM04 task remains open. The initial harness expected the wrong validation status and an actor field absent from the DTO; actual database evidence supplied the actor. These harness corrections did not alter product behavior or repeat accepted writes.

Evidence in ignored `tmp/uat-closure`: `crm04-connected.json`, `crm04-saved-evidence.json`, `crm04-attention-verified.json`, `crm04-completed-history.png`, `crm04-stale-all-pipelines.png`, `crm04-clock-fixture.json`, and identical `crm04-clock-before.json` / `crm04-clock-after.json`. Completed history and all-pipeline screenshots were inspected.

Latest API for both final journeys: `tmp/uat-crm-stage-build/bin/PSeq.Operations.Api/debug/PSeq.Operations.Api.dll`, SHA256 `52EB17CDD4207B8AFA3E0CC3B7F9E64B5584FB6C2C5B94E8F4E676D6087F3DE6`; unchanged 56-guide corpus `b1b88dd9cb3254d8de5709e732e8a5f47416d8c4202768ffdcbc62505223727c`. Runtime remains isolated 3016/7116/5436 with the same disabled/unconfigured external-provider boundaries. No migration, dependency, auth-policy, Git or deployment mutation was performed.


### Populated CRM table readability checkpoint

The Opportunity directory and Reports tables wrapped short column headings mid-word with long test names. Their headings now stay together; existing table containers handle horizontal scrolling. Real signed-in populated views passed at 1440, 768 and 390 pixels with no page-level horizontal overflow, no page errors, and non-wrapping headings. TypeScript and scoped lint pass. Evidence: `tmp/uat-closure/crm-table-layout.json`, `crm03-table-readable.png`, `crm03-reports-readable.png` and `crm-final-typecheck.log`. No added test duplicates the styling implementation. Existing Opportunity/Reports guides were reviewed; controls and user steps remain accurate.


## CRM-01 — Pass for isolated software acceptance

Actual non-admin CommercialOperator P-PRICE and separate P-ADMIN sessions completed the entire Company/Contact script, including its outreach variants. All new records were marked TEST ONLY; no invitation, sales message, service access or order was requested.

| Script step | Connected evidence |
| --- | --- |
| 1 Company create/edit | Created Company through Companies, opened its view-first workspace and edited relationship stage/source. Company has no Portal access. Existing Companies and all 13 entitlements, 69 Lab Service orders, 24 Lab work orders and two Commercial outbox rows compare unchanged against the preceding saved baseline. |
| 2 New/existing People | New person created a durable Contact and Company relationship together. Add existing person selected the retained CRM02 Contact and saved a Company-specific title. Two active relationships existed, without changing the existing Contact's identity or other Company association. |
| 3 Relationship edit/end | Updated the relationship title, reviewed the effective-date explanation, and recorded its end. End dates include the selected day, so a separately reviewed already-ended date variant proved the inactive historical row. The original unrelated Company relationship and full Contact readback remained unchanged. |
| 4 List return and recovery | Search/page context survived Company detail and Back to companies. A no-match query showed the empty result; a deliberately unavailable list response showed an error rather than empty results. Restoring the response recovered the query. |
| 5 Selector and dirty draft | First Escape dismissed search choices and restored input focus with the title intact. Second Escape originally discarded the whole form without confirmation: a real defect. After the fix, declined confirmation preserves the draft, accepted dismissal discards it, and reopening resets title and primary selection. Connected tablet/phone checks repeat this with no writes or page overflow. |

**Outreach crosswalk:** the new Contact started Not established. UI rejected missing evidence and a future evidence date before any save. A clearly fictional Staff review decision enabled Allowed for software testing; changing the email without a new decision reset it to Not established. A reasoned Suppressed decision persisted. All three decision/reset activities retain before/after evidence, the recording staff member and time; direct deactivation attempts returned 409 and complete history readbacks remained unchanged. The screen explicitly explains that other sending tools are not automatically blocked.

Three new isolated import-era fixtures represented legacy Permitted, OptedOut and DoNotContact. Only their `communication_preference` was initialized through a guarded transaction checking exact new IDs, names, actor, version one and absent evidence. Legacy Permitted remained Not established/needs review; the other two remained Suppressed and not outreach-eligible. These are synthetic legacy-state fixtures, not edits to existing Contacts or invented real consent.

P-ADMIN reviewed a merge of the newly created suppressed Contact into a newly created Allowed duplicate target with the same replacement email. The non-admin merge attempt returned 403. The target stayed Suppressed; the source became an inactive alias, the Company relationship moved to the target and all original activity IDs/body/actor/time survived. Independent database readback found exactly one merge audit with the selected target and no Contact-to-Portal identity links. The Contact directory, details and actual admin CSV agree on all five test Contact states, including the inactive source and legacy records.

Retain Company `eaeb8d7c-e372-4a9a-a741-ee957d091b60`, authoritative Contact `11508af1-8481-45b5-ad61-a5489da2c2d2`, inactive merged source `78c9d82e-7310-4d55-bdb2-d3edf4760ad3`, and ended relationship `e0facddf-6119-4dfe-84b9-e9c94ecc0580`. The original CRM02 Contact and its other relationship remain unchanged. Do not repeat creation or merge.

**Fix and verification:** Company People now applies the existing navigation/unsaved-draft guard to Add existing person, guards Cancel/Escape during editing and pending saves, and unmounts the dialog after close so discarded primary selection cannot leak into a new form. The focused Company workspace suite passes **8 tests**; TypeScript, scoped lint, generated documentation consistency and actual signed-in layouts at 768/390 pass. The initial unit harness was corrected to wait for enabled loading state; an intermediate Cancel-handler edit was corrected before final validation. No dependency, API, auth-policy, schema, Git or deployment change was made. The staff Company/Contact guide documents the dismissal behavior.

Evidence in ignored `tmp/uat-closure`: `crm01-connected.json`, `crm01-final.json`, `crm01-saved-evidence.json`, `crm01-noncrm-after.json`, `crm01-contact-export.csv`, `crm01-outreach-history.png`, `crm01-legacy.png`, `crm01-merged-history.png`, `crm01-draft-layout.json`, `crm01-draft-390.png`, `crm01-draft-768.png`, `crm01-draft-regression.log`, `crm01-typecheck.log`, `crm01-docs-runtime.json` and `crm01-api-baseline.json`. Phone draft and merged-history screenshots were inspected. The case is closed for isolated software acceptance; final release and external-provider/physical evidence remain separate.

The owned API was restarted to load the updated help corpus, with binary SHA256 unchanged at `52EB17CDD4207B8AFA3E0CC3B7F9E64B5584FB6C2C5B94E8F4E676D6087F3DE6`. The real signed-in search endpoint returned HTTP 200 and current 56-guide corpus `728f30a75273a7a6954bb7a4e127132566971ad902ca4314d167982860fa0579`. Same isolated UI 3016/API 7116/database 5436 and provider boundaries apply.
